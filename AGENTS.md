# 本项目协作规则

## 插件交付与 Tauri 启动准则

- 本地开发和内部测试可以继续使用普通前后端服务，通过浏览器访问。
- 放入 Accio Work 的正式插件交付包必须使用 Tauri，用户界面必须在 Tauri 桌面窗口中运行；不能以打开普通浏览器或要求用户手动访问 localhost 作为正式交付方式。
- 用户于 2026-09-10 最新要求先在 Accio 对话中渲染 HTML 启动页，点击“启动工作台”后自动启动后端与 Tauri 窗口；MCP 连接本身先待命。此条取代此前“加载即打开窗口”，仍不要求用户执行脚本。
- 保留现有 Node.js 业务后端；采用 Tauri 不意味着必须把业务后端重写为 Rust，也不意味着必须迁移 React。用户于 2026-09-08 已确认本版采用 React + Tauri + Node.js；后续保持此栈。
- 正式验收必须验证打包后的插件加载、后端自动启动和 Tauri 窗口内的真实业务链路；本地浏览器验证不能代替插件交付验收。
- 上述为用户确认的交付要求，不代表当前已完成 Tauri 打包或自动启动集成；实现和验收状态必须如实记录。

## 所有 Prompt 的运营方法依据

本插件内新增或修改任何 Prompt（包括 System/User Prompt、Dify Workflow/Chatflow 节点提示词、Agent/Skill 提示词及代码中的提示词模板），都必须先阅读并参考用户提供的 [国际站运营 SOP 正文检索版](docs/references/国际站运营SOP.md)，需要核对格式或歧义时读取 [Word 原件](docs/references/国际站运营SOP.docx)。此规则覆盖全部功能模块，不限于运营待办。

- 先确定对应的 SOP 环节，再将该环节的业务目标、所需数据、判断方法、具体动作及交付成果写进 Prompt；不要只加一句“参考 SOP”。只采用当前功能相关的方法，不把全文无差别塞入每个节点。
- SOP 包含市场定位、产品定位与选品、店铺装修、关键词库、高质量发品、广告与场景流量、优爆品提升、询盘质量跟进及店铺诊断等方法。跨模块场景应按业务顺序衔接。
- 区分“运营方法参考”和“当前业务事实”：SOP 不能证明某店铺、商品或客户存在问题。诊断和建议必须基于当前输入的数据、来源、时间、对象与统计口径；数据不足时明确缺项，不编造结论。
- 原文中的数值阈值、预算、平台规则、时效和效果描述不能直接当成通用现行规则或效果承诺。使用前核对适用类目、赛道、周期与当前平台依据；未核实时注明为 SOP 参考条件，不据此断言达标、必然升品或收益。
- 将建议与实际执行分开；Prompt 不能把提出建议描述为已经修改商品、投放广告、联系客户或完成任务。输出字段应与对应模块的 schema 和后续校验逻辑一致。
- 每次 Prompt 变更在现有开发日志中记录参考的 SOP 章节、如何映射到输入/判断/动作/输出，以及未覆盖的数据或规则。SOP 未覆盖的功能应明确说明，保留相关通用方法，不强套不相关章节。

## 项目维护

- 修改前阅读相关代码与 CONTEXT.md；保持现有技术栈及已确认的业务约定。
- 项目结构或约定变化时更新 CONTEXT.md，关键变更和验证结果写入 DEVELOPMENT_LOG.md。
- 完成代码修改后提醒用户：记得提交 git 喔。

## 跨平台交付与非技术用户安装

- 用户已确认正式支持 Windows（先覆盖 Intel/AMD x64）、Intel Mac、M 系列 Mac，不能将本机 Apple Silicon 包称为通用版本。
- 共用 React + Tauri + Node.js 源码，分别构建对应原生程序与 Node 运行时；按用户2026-09-10最新要求，仅交付Mac（M芯片）、Intel Mac、Windows（x64）三个独立ZIP，不再生成或提供三平台通用版。
- 用户只需导入 ZIP、安装启用并查看工作台；不要求安装 Node、Rust、编译源码或使用命令行。
- 编译、静态检查与目标系统的实机验收分开记录；Windows 同事只负责简单导入与数据查看，故障应由 Accio 读取日志后反馈摘要。

## 本地正式发布管理

- `release-manifest.json`记录当前交付包及冻结SHA256，`README.md`提供当前入口。用户已确认的三个基线包按实际版本保留，不为统一显示而改包内版本或重打同名文件。
- 插件版本独立于Accio Work宿主版本；身份固定为`lsou-workbench`。后续正式交付使用高于冻结记录的`x.y.z`，三平台版本一致；不能只改ZIP修订后缀代替升级。
- 发布前用`npm run release:version -- <新版本>`同步全部入口，顺序构建/打包三平台，再用`release:publish -- --notes "实际变更与验收摘要"`登记。构建源码指纹不一致时重新构建，不能修改build.json绕过检查。
- `npm run release:check`核对当前包。`release:prune`默认只预览，加`-- --apply`才删除被替代包；当前三包不完整时不得清理。展开目录留在构建缓存，不放入release交付入口。
- 本地登记不等于市场发布或实机验收。除非用户另行要求，不上传市场、不重装已安装插件、不进行Skill分析效果测试。
