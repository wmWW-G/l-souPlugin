# 来搜经营驾驶舱设计 QA

## 对比基准

- 用户提供的官网截图：`/var/folders/94/2dlbsm3968j4rqjll_d32qb40000gn/T/codex-clipboard-6e4520c7-0a4f-403a-b796-0cea384e55d6.png`，3456 × 2040 px，包含浏览器框架。
- 官网实时首屏：`design-reference-official-1280.png`，1728 × 934 px；页面状态为 `https://l-sou.com/` 首屏。
- 方形品牌标识：`/Users/garden/YD/l-sou/logo.png`，481 × 480 px。
- 新版透明品牌标识：`/Users/garden/YD/l-sou/新logo.png`，762 × 500 px。
- 桌面实现：`design-implementation-desktop.png`，1304 × 1168 px；CSS 视口约 1304 × 1167，设备像素比 1.6。
- 窄屏实现：`design-implementation-mobile.png`，609 × 1319 px；浏览器视口能力请求 390 × 844，页面实际 CSS 视口约 487 × 1055。
- 全局并排证据：`design-comparison-desktop.png`，2560 × 720 px。官网与实现均从顶部开始，分别按覆盖方式归一到 1280 × 720 后并排。
- 页头聚焦证据：`design-comparison-header.png`，1280 × 440 px。两侧顶部区域分别缩放到 1280 × 220 后纵向排列，用于检查 Logo、字体、导航、品牌色和控件密度。
- 侧边栏桌面实现：`design-sidebar-viewport.png`，1304 × 1168 px；对应 CSS 视口约 1304 × 1167。
- 侧边栏窄屏实现：`design-sidebar-implementation-mobile.png`，609 × 3104 px；浏览器视口能力请求 390 × 844，页面实际 CSS 视口约 487 × 1055。
- 布局改版并排证据：`design-sidebar-comparison.png`，2608 × 1168 px。左侧为改版前顶部导航，右侧为改版后左侧业务边栏，用于检查信息架构、工作区宽度和内容连续性。

对比目标不是复制官网营销首页的内容结构，而是让经营大盘沿用官网的品牌语言，同时保留数据工作台所需的信息密度和完整交互。

## 最终检查

### 字体与层级

- 实现使用官网实测的 `Helvetica Neue / PingFang SC / Microsoft YaHei` 字体栈。
- 页面标题、分组标签、辅助说明、KPI 标签和数值建立了清楚的五级层级；桌面与窄屏均未见标题截断或不自然换行。
- KPI 数字使用更强字重和等宽数字特性，符合数据快速扫读场景。

### 间距与布局

- 官网的白色导航、克制圆角和轻阴影被转译为工作台页头、核心结果带、行动列表和诊断卡片。
- 桌面端由 220 px 固定边栏和右侧工作区组成，6 项核心 KPI 形成单排结果带；1320 px 以下转为 3 × 2，窄屏转为 2 × 3。桌面与窄屏都没有页面级横向溢出。
- 导航按经营中心、增长运营、交易保障、内容资产、组织管理和系统工具分组，当前项用暖橙底、左侧强调线和更高字重标识；未接入模块统一使用“规划”标签。
- 导航在窄屏保持单行横向滚动，避免七个业务入口挤压换行；复杂图表自身保留容器内横向滚动。

### 色彩与视觉令牌

- 主色使用官网实测 `#ff6600`，辅以 `#fff8f3` 暖米色、白色内容面和深灰文字。
- 同行优秀、下降风险和不同业务指标保留独立语义色；橙色只承担品牌、选中态和主要操作，未覆盖错误与成功语义。
- 文本、边框、按钮和选中态对比清楚，未见浅色背景上的低对比正文。

### 图片与品牌资产

- 侧栏直接使用用户提供的方形 Logo；复制后 SHA-256 与源文件一致。
- Logo 使用 `object-fit` 保持比例，没有拉伸、裁切、透明边缘光晕或代码绘制替代品；新版透明 Logo 保留在资产目录但不占用当前数据首屏。
- 官网促销横幅和营销插画没有搬入数据大盘，这是为避免营销信息干扰经营决策而保留的产品语境差异。

### 文案、状态与交互

- 当前产品入口为“来搜国际站经营工作台”，首屏围绕经营结果、同行基准、增长差距和下一步行动，不把内部 Workctl 实现作为产品名称。
- 实测商品运营可切换并返回 20 条当前页数据，切换后右侧标题与 `aria-current` 同步；切回经营总览后 KPI 点击可切换趋势指标。
- 日期、区间、刷新、清缓存、标签页、KPI、筛选、分页、弹窗和控制台保留原有 DOM 标识与事件链路。
- 桌面和窄屏浏览器控制台错误、警告均为 0。

### 可访问性与响应式

- Logo 具有替代文本，统计周期控件保留可见标签，快捷区间有无障碍名称，实时链路采用 `role=status` 和 `aria-live`。
- 键盘焦点有明显橙色外圈；窄屏日期与主要按钮保持至少 44 px，业务导航按钮实测约 46 px。
- 已增加 `prefers-reduced-motion` 处理，减少动效偏好的用户不会被过渡动画干扰。

## 对比迭代记录

### 第一次对比

- [P2] 页头最初写成“数据已连接”，但视觉层没有持续绑定健康接口，可能造成过度承诺。
  - 修正：改为中性的“实时数据链路”，仅表达数据来源性质；真实异常仍由已有请求提示呈现。
- [P2] 窄屏统计控件最初不足 44 px，触控目标偏小。
  - 修正：日期、区间、主要按钮和筛选控件统一到至少 44 px。
- [P2] 动效没有响应系统减少动态效果设置。
  - 修正：增加 `prefers-reduced-motion: reduce` 样式。

### 第二次对比

- 已重新加载真实经营数据并复查桌面、窄屏和主要交互。
- 前述三项 P2 均已修复；窄屏控件实测约 44 px，页面无整体横向溢出，浏览器错误和警告为 0。
- 未发现仍需处理的 P0、P1 或 P2。

### 侧边栏布局第一次对比

- [P2] 1304 px 桌面视口改为固定侧栏后，KPI 最初仍保留 6 × 2 排布，单卡宽度约 159 px，指标名称、数值和同行说明的阅读密度偏高。
  - 修正：把桌面中等宽度断点提高到 1450 px，在当前视口使用 4 × 3 排布，单卡宽度约 245 px。
- [P2] 业务入口改为侧边栏后，需要把导航选中态和右侧内容标题绑定，否则用户仅能从内容本身判断当前页面。
  - 修正：标签切换时同步设置 `aria-current="page"` 并更新 `#workspaceTitle`。

### 侧边栏布局第二次对比

- 重新用 1304 × 1167 桌面视口、约 487 × 1055 窄屏视口和改版前后并排证据完成检查。
- 桌面固定侧栏、右侧工作区、4 × 3 KPI 网格均无重叠或裁切；窄屏侧栏转为顶部横向导航，KPI 为双列，页面无整体横向溢出。
- 商品效果切换返回 20 条当前页数据，页面标题和选中态同步；桌面与窄屏浏览器错误、警告均为 0。
- 未发现仍需处理的 P0、P1 或 P2。

### 行动优先工作台第一次对比

- 对比源为用户选定的 Product Design 方案 1：`/Users/garden/.codex/generated_images/01a060f6-ad6f-7120-be82-d6c369b56db7/exec-0175e679-d874-4647-b090-926864a48497.png`，1487 × 1058 px。
- 实现截图为 `design-action-workspace-implementation.png`；从同一顶部状态取得，按浏览器实际 CSS 视口裁切后的实现证据为 `design-action-workspace-implementation-cropped.png`，并排证据为 `design-action-workspace-comparison.png`。
- [P2] 首次检查发现侧栏收起后视觉文本被隐藏，收起/展开按钮的无障碍名称也随之消失。
  - 修正：按钮增加稳定的初始 `aria-label`，收起态切换时同步更新为“展开菜单”，展开后恢复为“收起菜单”。
- 为保持数据可信度，没有照抄原型中的 23 条待办、负责人、风险项和商品询盘数；现阶段只呈现 3 条可由真实数据推导的运营任务，其余能力明确标注“规划中”。这属于业务真实性约束，不是视觉遗漏。

### 行动优先工作台第二次对比

- 220 px 分组侧栏、六项横向结果带、左侧经营趋势、右侧运营待办、底部三列诊断摘要与选定原型的主结构一致；橙色强调、低圆角、细分隔线和克制阴影保持来搜品牌语言。
- 真实数据已显示：区间累计曝光 11.79 万、点击 2,929、询盘 54、成交 9；回复率和回复时长按末日快照展示，渠道/国家/商品摘要均来自真实只读接口。
- 浏览器实测规划入口只显示边界提示，待办可跳转商品运营，商品页返回 20 行，侧栏收起/展开正常；390 px CSS 窄屏 `scrollWidth` 与视口同为 390 px，页面无整体横向溢出，页面运行日志为空。
- 前述 P2 已修复，未发现仍需处理的 P0、P1 或 P2。

### RFQ 商机单页第一次对比

- 视觉事实源为改版前真实页面 `design-audit-2026-09-03/10-rfq-before.png`；实现截图为 `design-audit-2026-09-03/13-rfq-after-final.png`。两张证据均为 1304 × 1168 px、相同页面顶部状态，并排证据为 `design-audit-2026-09-03/compare-rfq.png`。
- 必须保持的界面表面是左侧业务导航、顶部全局经营筛选、来搜橙色品牌令牌、白色内容面、低圆角和紧凑数据密度；RFQ 内容结构允许从原来的三个标签升级为一张连续运营页。
- [P1] 改版前页面只展示报价权益摘要和工具映射，采购需求、买家信号、报价竞争与历史记录被拆散，运营人员无法从商机池直接判断是否值得报价。
  - 修正：同页增加四项经营指标、搜索与来源/国家筛选、采购需求列表、所选 RFQ 详情、买家行为信号、报价历史和报价权益；右侧详情与左侧选择保持绑定。
- [P1] 首次快速切换站内与站外 RFQ 时，先发出的详情请求可能后返回，造成左侧选中卡片与右侧详情短暂不一致。
  - 修正：详情请求保存本次商机 ID，响应回写前再次核对当前选中项；浏览器连续切换后，列表标题与详情标题均为 `1.43" Round AMOLED Smart Watch`，且状态为“详情已核对”。
- [P2] 权益实时查询本轮等待 125 秒仍未返回，若继续阻塞会让整张商机页看起来没有数据。
  - 修正：采购池、详情和报价历史继续使用实时只读结果，权益单独展示 2026-08-30 已核验审计快照，并在卡片中明确标注日期和本轮实时状态。

### RFQ 商机单页第二次对比

- 顶部截图证明指标、筛选、商机池与详情在同一视口建立连续阅读关系；下半页聚焦证据 `design-audit-2026-09-03/14-rfq-after-bottom.png` 证明报价历史、权益和隐私边界说明完整呈现，没有被长列表截断。
- 真实页面最新刷新显示站内 RFQ 323 条、站外 RFQ 45 条、报价历史 5 条；当前列表仅展示 20 个已脱敏样本，右侧详情不包含买家姓名、账号、RFQ/报价跳转链接或报价正文。
- 已实测来源筛选、国家筛选、列表选择、站内详情、站外详情和快速连续切换；选中卡片与详情标题一致，浏览器错误和警告均为 0。
- “运营优先级”明确标注为页面排序信号，不冒充 Alibaba.com 平台匹配分；未发现仍需处理的 P0、P1 或 P2。

## 后续细化

- [P3] 若以后需要面向手机长期使用，可为趋势图和同行对比图单独设计移动版图形，减少图表容器内横向滚动。
- [P3] 当前统计周期仍集中在右侧页头；后续可在长页面滚动时增加更轻量的悬浮周期摘要，但不影响本轮使用。

### 产品发布批量矩阵第一次对比

- 用户选定的视觉事实源为 Product Design 第 2 版“批量商品矩阵工作台”：`/Users/garden/.codex/generated_images/01a0623c-decd-73a2-a3da-26d9aebb39c7/exec-aa0e1061-c282-4ea9-a369-4894a3ae7a48.png`，1325 × 1187 px。
- 最终浏览器实现为 `design-audit-2026-09-03/16-product-publish-final.png`，1304 × 1168 px；CSS 视口 1304 × 1167，页面宽度与视口一致。归一化事实源为 `16-source-normalized.png`，全局并排证据为 `17-product-publish-full-compare.png`，矩阵与编辑器聚焦并排证据为 `19-product-publish-focus-compare.png`。
- 五个高影响表面均保持一致：左侧既有业务导航未重排；顶部状态带和右上主操作形成发布入口；左侧八行批量矩阵承担扫读、筛选和选中；右侧图片、基础字段、类目必填属性、交易履约和卖点连续校对；底部固定操作栏明确并发 1 的逐条队列策略。
- [P2] 首轮实现的矩阵与编辑器宽度约为 644 / 388 px，右侧字段偏窄，八条商品行也比选定原型短，页面中部留白过多。
  - 修正：最终宽度约为 593 / 439 px，商品行增至约 89 px，缩略图增至 49 px；右侧表单收紧间距后发布队列标题也能进入首屏，整体信息密度更接近选定原型。
- [P2] 首轮已有 5 张图的商品追加图片后可能显示为 `6/5`，新图片还会落在五张可见缩略图之后，用户看不到上传结果。
  - 修正：完成度最高显示 `5/5`，新增图片放在画廊最前面并即时成为可见封面，原有完成度不会因追加图片倒退。
- 没有复制原型中无法核验的 12 个上传、8 个检查通过等示例数字；当前页面展示 8 个本地草稿、4 个通过、4 个待补全，均由草稿字段完整度即时计算。商品缩略图来自当前店铺商品效果接口已经返回过的公开图片。

### 产品发布批量矩阵第二次对比

- 搜索 `earbuds` 实测保留 2 行；点击第二行后右侧标题同步为 `TWS Wireless Earbuds Noise Cancelling`，把标题改成 `TWS Earbuds Pro Noise Cancelling` 后左侧活动行立即同步。补齐物流模板后仍显示“待补全”，因为该条必填属性只有 8/10，状态没有被单一字段错误放行。
- 普通图片多选入口实测支持 multiple，本地图片进入当前商品后完成度仍为 `5/5`，上传只生成浏览器 Object URL，没有向 OSS、WorkCTL 或第三方服务传输。
- 点击“发布已通过的 4 个商品”后出现二次确认；演示队列按顺序完成前两条、第三条失败、第四条继续执行，证明单条失败不阻塞后续任务。点击重试后四条均变为“演示完成”。
- 窄屏证据为 `design-audit-2026-09-03/20-product-publish-mobile.png`。浏览器请求 390 × 844 后实际 CSS 视口约 487 × 1055，页面 `scrollWidth` 与视口同为 487 px；矩阵自身保留 650 px 容器内横向滚动，编辑器顺序移到矩阵下方，没有造成页面级横向溢出。
- 最终桌面浏览器错误和警告均为 0；未发现仍需处理的 P0、P1 或 P2。

### 产品发布浏览器批注第一次对比

- 本轮视觉事实源为用户在 1304 × 1167 CSS 视口中对“关键词”和“卖点”区域的两条浏览器批注；改动前证据为 `design-audit-2026-09-03/16-product-publish-final.png`，改动后实现为 `design-audit-2026-09-03/21-product-publish-editable-fields.png`，两张截图均为 1304 × 1168 px、相同路由和首屏状态，未做密度缩放。
- 全屏前后并排证据为 `design-audit-2026-09-03/22-product-publish-feedback-compare.png`；右侧字段聚焦证据为 `design-audit-2026-09-03/24-product-publish-feedback-focus-compare.png`。
- [P1] 关键词虽然呈现为标签，但实际只由 `<span>` 渲染，没有输入、删除或新增入口，与“可继续补充”的文案矛盾，用户无法完成发品字段校对。
  - 修正：每个关键词改为可直接输入的标签，增加删除按钮；少于 5 个时出现新增输入和“添加”按钮，支持回车或逗号提交，并阻止空值、重复值和超过 5 个。
- [P2] 卖点只有 3 条，无法覆盖用户确认的 5 条发品内容结构，也没有清楚提示每一条可修改。
  - 修正：旧草稿自动补齐为 5 个编辑位；每条增加序号、200 字上限、占位提示和实时字数，现有三条内容保留，并补充两条可继续改写的演示内容。

### 产品发布浏览器批注第二次对比

- 关键词实测从 5 个直接改写第 1 个、删除第 2 个后变为 4 个，再新增 `Voice Assistant` 恢复为 5 个；切换到另一条商品再切回后，修改后的关键词仍保留。
- 卖点实测固定为 5 个输入框，第 5 条改写后切换商品再返回，内容仍保留。页面刷新后恢复 Demo 初始值，符合当前“本地草稿、尚未持久化”的能力边界。
- 字体、字号、橙色强调、灰色标签、表单边框、两列属性栅格和商品图片均沿用选定原型与现有设计令牌；本轮只改变目标字段的可编辑性和卖点数量，没有重排侧边栏、矩阵、交易履约、队列或底部操作栏。
- 最终页面宽度与 1304 px 视口一致，关键词标签可换行，增加两条卖点后页面通过纵向滚动承接，没有产生页面级横向溢出；浏览器错误和警告均为 0。未发现仍需处理的 P0、P1 或 P2。

### 产品发布字段 Schema 校验第一次对比

- 视觉事实源为用户在 1304 × 1167 CSS 视口中框选整个右侧发布编辑器并提出的批注；同状态的改动前浏览器证据为 `design-audit-2026-09-03/21-product-publish-editable-fields.png`，1304 × 1168 px。
- 改动后浏览器实现为 `design-audit-2026-09-03/25-product-publish-schema-guided.png`，1304 × 1168 px；CSS 视口 1304 × 1167、设备像素比约 1.6，未做密度缩放。全屏同画布并排证据为 `design-audit-2026-09-03/27-product-publish-schema-full-compare.png`，编辑器聚焦并排证据为 `design-audit-2026-09-03/28-product-publish-schema-focus-compare.png`。
- [P1] 改动前四类商品共用智能手表的 10 项属性，耳机和手环会显示错误字段；价格和交期还是无法映射到接口的自由文本。
  - 修正：按四个已实跑叶子类目分别建立必填属性配置；平台单选使用原生下拉，平台多选使用已选标签加选项下拉，原产地明确为平台地区选择器。价格改为销售方式、计价单位和可增删的阶梯价数组，交期改为阶梯发货数组，包装补齐成组长宽高和毛重。
- [P1] 改动前完整度、价格库存和物流状态依赖手写布尔值，清空字段或切换类目后仍可能显示“可发布”。
  - 修正：每次渲染均从当前类目属性、整数 MOQ/库存、销售方式条件、阶梯价格、阶梯交期、成组包装和物流模板重新派生本地校验状态；浏览器把第一阶梯单价改为 0 后，列表立即从“已填写 / 可发布”变成“待填写 / 待补全”，恢复有效数字后状态复原。
- [P2] 计价单位、店铺物流方案和原产地都不能让运营人员填写平台内部 ID；若继续放普通文本框，会让用户误以为任意文本都可提交。
  - 修正：原产地使用官方国家选择器；计价单位和运费方案通过当前账号已有商品自动匹配，界面只显示业务名称，内部编码由服务端在入队前复核。当前店铺没有可读取运费方案时使用国际站账号默认设置。

### 产品发布字段 Schema 校验第二次对比

- 字体与层级：沿用现有 Helvetica Neue / PingFang SC 字体栈；新增 Schema 校验条、字段来源标签和结构化分组使用更弱字号与浅色底，不抢标题、商品和主操作层级。
- 间距与布局：保留侧边栏、左侧八行矩阵、右侧编辑器和底部操作栏比例；右侧新增字段继续使用两列网格，阶梯行固定为“名称 / 数量 / 单价或天数 / 删除”，没有破坏桌面首屏的主要扫读关系。
- 色彩与视觉令牌：继续使用 `#ff6600` 作为主操作与必填强调，系统来源用中性灰，已校验提示用浅绿；没有新增不一致的卡片阴影、圆角或高饱和装饰。
- 图片质量：商品缩略图、封面裁切和原有真实资产未改变，没有用占位图、代码图形或新生成图替换。
- 文案与内容：明确区分“本地校验”和平台正式预检，明确 `ladderPrices`、`ladderPeriod`、`shippingTemplateId` 的真实数据形态；五条卖点和可编辑关键词继续保留。
- 浏览器实测 Open-ear 商品对应叶子类目 202061615，并只显示运动耳机 7 项必填属性；把类目切为智能手环后标题即时变为“5 项”；销售方式切为 `batch` 后出现每批数量且可改为 24；桌面错误和警告为 0。
- 窄屏证据为 `design-audit-2026-09-03/29-product-publish-schema-mobile.png`。浏览器请求 390 × 844 后实际 CSS 视口约 487 × 1055，页面 `scrollWidth` 与视口同为 487 px；矩阵在自身容器横向滚动，编辑器顺序落到矩阵下方，没有页面级横向溢出。
- 未发现仍需处理的 P0、P1 或 P2；当前唯一保留边界是平台尚未提供的单位、模板和地区列表，这些已在界面中明确标注，不再伪装成可自由提交的内容。

### Alibaba 原产地控件第一次对比

- 视觉事实源：`design-audit-2026-09-03/33-alibaba-origin-filtered.png`，来自用户明确指定且已登录的 Alibaba 智能手表发品页；实现截图：`design-audit-2026-09-03/35-local-origin-open.png`，来自本地 `?tab=product-publish`。
- 两侧浏览器 CSS 视口均为 1728 × 877，`devicePixelRatio` 均为 2；截图文件也均为 1728 × 877 px，Browser 输出已按 CSS 像素归一化，因此未做二次缩放。展开状态都使用搜索词 `China`；源页面字段尚未选择值，本地 Demo 默认已选择 `China`，该差异属于演示数据状态而不是控件结构偏差。
- 全视图同画布证据：`design-audit-2026-09-03/36-origin-full-comparison.png`。聚焦字段证据：`design-audit-2026-09-03/37-origin-focused-comparison.png`。本轮必须检查搜索框、两条命中结果、选中态和浮层相对字段的位置，因此使用了聚焦证据。
- [P1] 改动前本地原型把 `Place of Origin` 显示为只读的 `China / Guangdong / Shenzhen` 和“选择地区”按钮，错误表达成中国省市三级地址；真实 Alibaba 页实际是可搜索的国家/地区单选，且平台名称使用英文。
  - 修正：默认值改为 `China`，逐项保存 Alibaba 当前页面的 256 个国家/地区名称；控件改成主选择框、独立搜索框、可滚动列表、单选回填和当前值勾选态，不再接受任意自由文本。

### Alibaba 原产地控件第二次对比

- 字体与层级：源页面和本地页面都以中性无衬线字体呈现字段名、输入值和选项；本地沿用现有产品发布页的小字号密度，没有把来源页面的整页字号强行套进窄编辑栏。
- 间距与布局：下拉紧贴主选择框，搜索框位于选项上方，列表在字段下方滚动；右列字段会右对齐浮层，避免突破编辑器边缘。菜单宽度按本地字段可用空间收敛，这是适配现有两列表单的有意差异。
- 颜色与视觉令牌：搜索焦点使用与源页面相近的蓝色边框；本地当前选项继续使用产品发布页既有橙色强调，边框、阴影和圆角保持轻量，没有改变侧边栏和其他页面令牌。
- 图片与资产：该目标控件不包含图片；商品缩略图、品牌标识和既有图标资产均未改动，也没有用新绘制图形替换来源资产。
- 文案与内容：`China` 搜索实测返回 `China` 与 `Taiwan, China`；无匹配时显示“没有匹配的国家/地区”。字段来源明确写为“Alibaba 发品页选项”，同时保留 WorkCTL 不返回该列表的边界说明。
- 浏览器交互实测：展开、输入检索、`Vietnam` 单选回填、回填后必填完成度仍为 `10/10`、恢复 `China`、无结果、Esc 收起和点击外部收起均通过。最终关闭态为 `design-audit-2026-09-03/39-local-origin-restored.png`；浏览器控制台错误为 0。
- 静态与回归验证：`PUBLISH_ORIGIN_OPTIONS` 共 256 项且无重复，包含 `China` 与 `Taiwan, China`；`node --check public/app.js`、`node --check server.js`、Demo JSON 解析和 3 个 Node 集成测试全部通过。
- 未发现仍需处理的 P0、P1 或 P2。保留边界：真实 Alibaba 源字段当时为空，而本地 Demo 为方便批量校对默认填入 `China`；正式发布时仍必须以用户选择和平台最终预检为准。

### 单品与批量真实队列验收

- 页面结构保持侧边栏、左侧商品矩阵和右侧编辑器不变；右侧新增“当前商品操作”，明确只处理正在编辑的 1 个商品，底部操作栏明确只处理左侧勾选项。
- 浏览器实测当前商品“保存当前草稿 / 发布当前商品”同时存在；左侧 8 个商品全选时，底部显示“批量保存草稿（8） / 批量发布（8）”。其中 4 个商品待补全，批量发布按钮正确禁用，批量保存草稿仍可进入确认；取消勾选 4 个待补全商品后，按钮立即变为可用的“批量发布（4）”，确认弹窗只列出剩余 4 个完整商品。
- 单品发布确认弹窗显示 1 个商品、5 张远程图、属性 10/10 和 `publishType=product`；批量草稿确认弹窗完整列出 8 个勾选商品并显示 `publishType=draft`。两个弹窗的最终确认框默认均未勾选，真实执行按钮默认禁用。
- 自动化测试用受控假 WorkCTL 证明两条批量发布严格按 `A 开始 → A 结束 → B 开始 → B 结束` 执行；重复幂等键不会再次启动子进程。当前活动 Accio 服务健康检查为 ready，浏览器验收后 `/api/publish/jobs` 仍为 0 条，未触发任何真实店铺写入。
- 当前保留边界：新选本地图片仍是 `blob:` 地址，接入真实 OSS bucket 前不能进入队列；队列是进程内状态，服务重启后不恢复；账号级计价单位和运费方案从当前店铺已有商品读取，不会把开发者店铺的内部编码打包给其他用户。

### 单品与批量总进度、终态结果验收

- 保持侧边栏、顶部状态带、左侧商品矩阵和右侧编辑器位置不变；在状态带下方新增单一“本次操作”进度组件，不把每个任务再拆成独立大卡片。
- 服务端同一批任务共享 operationId，前端按 position 渲染 1…N。单品严格显示 1 个节点和 `0/1 → 1/1`；批量按照左侧选中数量显示 N 个节点和 `0/N → N/N`，等待、执行、成功、失败颜色和文本均独立。
- 首次真实任务失败时，页面准确显示“本次单品保存草稿 / 处理完成 1/1 / 0 个成功 · 1 个失败 / 100%”，并在结果弹窗保留上游 `failed to parse material`。修正材料结构后，用户再次明确确认同一商品草稿，页面显示 `1/1`、`100%` 和“草稿已保存”，服务端终态为 `saved_draft`，不是前端模拟成功。
- 新发起操作全部终止后自动弹窗一次；刷新页不会重复弹历史结果。正式发布成功态只写“已提交发布，待平台审核”，失败同样弹窗，不会用成功 toast 掩盖。
- 失败根因已按当前 Alibaba Seller Assistant 的 Product.xlsx 参考结构纠正：material 改成 `categoryId + basicInfo / trade / fulfillment / detail`，受控假 WorkCTL 测试会拒绝旧 materialVersion 扁平结构；修正后的真实草稿已由用户操作完成平台验收。
- 根据最新浏览器批注，进度组件从约 138 px 的大卡片压缩到约 58 px 单行状态栏；桌面 1291 × 1167 实测页面 `scrollY=0`、文档高度等于视口，右侧编辑器滚到 `scrollTop=812.5` 时底部批量栏仍在视口内。左侧矩阵和右侧编辑器都具备独立 `overflow-y:auto`，8 条演示商品当前恰好全部可见，增加商品后由左卡片内部承接滚动。
- `node --check server.js`、`node --check public/app.js` 和 4 项 Node 集成测试通过。

final result: passed
