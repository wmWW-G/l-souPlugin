# Development Log

## 2026-09-05 — 五条运营能力接入与验收

- 用户明确要求实现上一轮提出的五点，并确认沿用原生 JavaScript 前端与 Node.js 后端。
- 当天通过当前账号 Workctl 0.1.53 动态 schema 核验工具参数，新增独立 `lib/operations.js` 与合同文件、`public/operations.js`，复用既有进程调用和日志，不引入第三方运行依赖。
- 订单、物流、关税、店铺风险与商品/店铺违规改接实时查询，增加分页、状态/日期筛选及完整结果查看。
- 商品诊断建议按钮可进入整改；支持读取当前商品与平台草稿、质量分、修改标题/关键词/主副图/卖点/公司介绍/FAQ、保存草稿后单独确认提交；实际提交命令为 `submit-draft`。修正商品页固定378条样本标签，使用实时分析数量。
- 素材工坊接入场景/白底/高清/细节/营销卖点图、换色、模特图、图片翻译、视频分镜及视频生成，使用商品原图而非经营缩略图；生成任务的提交、轮询和产物展示分离，保留既有3D资产读取入口。
- 客户页新增实时会话选择、买家背景、消息历史分页、商家资料及商品知识，去掉旧最近会话审计快照的展示依赖。消息分页按真实返回的 `nextPointTimeStamp` 和 `hasMore` 处理。
- 流量页新增渠道趋势、访客还看过的商品、所选真实商品类目的行业供需/趋势/卖家画像及全行业买家偏好；总览新增星等级、赛道能力及平台提升建议。
- 新运营写队列具备同源JSON、确认短语、参数白名单、商品/会话随机引用、同步占用幂等键、同一商品互斥、部分失败计数和不确定结果分类。任务记录及查询凭据按账号隔离保存为0600文件，运行日志记录开始/结束；重启不自动重放写入。原发新品队列未改为持久化。
- 静态检查覆盖 server.js、lib/operations.js、public/app.js、public/operations.js；`node --test test/*.test.js` 共12项全部通过。新测试覆盖五类查询、身份关联、参数拒绝、确认、跨站拒绝、并发幂等、保存后提交、重启恢复、部分失败与生成失败。
- 真实账号已成功调用并核对：合同、物流、关税、风险、两类违规、商品列表/内容/质量分、最近会话/消息/买家背景、商家资料/商品知识、渠道趋势、关联浏览、行业供需/趋势/卖家画像/买家偏好、星等级。买家偏好当前返回真实空对象，保留空状态。
- 浏览器核验真实页面与确认弹窗；隔离8799服务用无网络Workctl替身完成草稿保存（基础/详情各1次）、提交（1次）、图片生成（1次）、结果查询（1次），产物图片显示成功，浏览器错误/警告为0。隔离测试不能等同真实生成或真实编辑验收；真实账号 `/api/operations/jobs` 保持空列表，未执行真实店铺写入或额度消耗。
- 更新 README 与 CONTEXT。此目录当前没有 Git 仓库，未执行 commit/push。

## 2026-09-04 — 四个内部单页改接当前账号真实数据

- 知识库升级为“内容目录 + 完整正文”阅读台：把商家知识响应按真实 `knowledge_id` 拆成独立问答，并与 20 条公共 FAQ、23 条接待策略组成统一目录；支持全文搜索、按来源筛选和逐条点击阅读，右侧保留完整原文、来源、ID、时间、语言、国家和匹配度等接口实际返回的元数据。
- 知识目录中的摘要只用于快速定位，阅读区不截断正文；移动端点击条目进入正文并可返回目录，桌面端保持双栏连续浏览。没有解析到标准商家知识格式时会展示平台返回的整段原文，不因解析格式变化丢失真实内容。
- 删除店铺装修、素材工坊、知识库与接待、账号与权限对 2026-09-02 静态审计快照的页面依赖，新增 `/api/workspaces/<module>` 只读聚合接口。真实业务内容保持原值下发；Gateway token 和运行密钥仍不进入浏览器。
- 店铺装修实时读取公司号、公司资料附件、Accio Work 页面列表和云端页面。当前账号若返回 0 个页面版本，就显示可核对的真实空状态，不再用首页规划草图填充。
- 素材工坊读取当前店铺第一页真实商品（含商品 ID、标题、图片、类目和经营量）、当前账号自有 3D 模型，以及公共 3D 素材库；首屏不再为了取素材扫完整店商品。
- 知识库与接待实时查询商家知识、公共 FAQ、辅助接待和自动接待策略，页面可展开查看策略 ID、说明、正文、来源和修改时间，不再只展示八类主题框架。
- 账号与权限优先复用稳定的经营账号目录显示真实姓名和账号字段；联系人单独通过 `query-contact` 异步读取，旧 `list-contact` 无权限错误不再进入页面。当前接口未返回的管理员、当前登录和角色字段明确显示“未返回”，不再生成匿名成员或四角色矩阵。
- WorkCTL 实时工作台查询增加 35 秒进程组硬超时，能同时结束 CLI 包装器和原生子进程，避免异常查询无限占用本地网关。全店历史图库同步从“服务启动即执行”改为进入产品发布页后按需启动，其他业务页不再被图库预热抢占。
- `node --check server.js` 与 `node --check public/app.js` 通过；现有 5 项 Node 测试全部通过。浏览器已确认新版服务运行在 `127.0.0.1:8787`，当前活动账号的成员工具存在长时间不返回现象，因此页面保留硬超时和稳定账号目录降级，不使用其他账号历史结果冒充。

## 2026-09-04 — 四个内容资产页从审计空状态改为业务工作台

- 修正店铺装修、素材工坊、知识库与接待、账号与权限把技术审计结果放在页面主体的问题。真实空状态仍保留，但只作为页面当前状态，不再替代功能内容。
- 店铺装修页新增首页结构规划、桌面端预览草图、发布前清单、企业/认证/交付/服务四类内容资产，以及从资料准备到页面确认的完整流程；“准备核心产品”可跳转到产品发布页。
- 素材工坊新增商品主图、场景图、颜色变体、模特展示、营销海报、图片翻译、商品视频和 3D 模型八类工具。工具可在页面内切换，创作参数、原始素材要求、资产归档和最近任务均有独立区域。
- 知识库与接待新增付款、样品、报价、交期、包装、物流、售后、合规八类知识主题，提供本地筛选，并把接待流程、23 条策略结构、服务诊断和知识改进闭环连续呈现。
- 账号与权限删除业务页面中的 `query-contact`、`list-contact`、`no privilege` 等底层术语，改为 9 个成员的脱敏目录、经营/客服账号映射、四种待配置角色矩阵、敏感操作保护和联系人正常读取说明。旧入口权限差异只作为中文接入说明，不再冒充业务权限异常。
- 四页继续区分平台已读取事实与本系统待配置方案：角色矩阵、首页框架和知识主题框架均明确标注为规划/待配置，不伪装成平台已保存数据，也没有执行生成、发布或权限修改。
- `node --check` 覆盖服务端和前端脚本；浏览器逐页检查桌面布局、素材工具切换、知识主题筛选入口和账号权限矩阵。

## 2026-09-04 — 装修、素材、知识与账号四页改为业务单页

- “店铺装修”移除页面版本、生成工作流、发布记录三个内部页签，改成“公司资料 → 页面资产 → 生成预览 → 确认发布”的连续准备度页面。公司资料标记为可用，Accio Work 页面、云端页面和发布记录均保持真实空状态；两套页面体系分别呈现，不生成虚假预览缩略图。
- “素材工坊”移除 AI 图片、视频、3D 模型和任务中心四个内部页签，改成从平台能力到店铺自有资产的一条生产线。页面明确区分公共 3D gallery 可用、3D 资格可用，与自有图片任务、自有视频任务、自有 3D 模型均为 0，避免把公共能力冒充自有资产。
- “知识库与接待”移除商家知识、接待策略和服务诊断三个内部页签，改成“知识来源 → 接待策略 → 买家回复 → 服务复盘”的闭环单页。同页展示 20 条公共 FAQ、商家知识查询可用、23 条接待策略、97.34% 首次回复率和 3.74 小时平均回复时长，同时说明知识查询不等于知识库管理。
- “账号与权限”移除账号目录、权限矩阵和操作审计三个内部页签，改成平台账号事实、权限模型缺口、本地操作审计三层治理页。同页展示 9 个匿名成员、100 条脱敏联系人、1 个明确受限接口、0 个已确认权限写接口，以及 123 次只读命令审计的成功/受阻分布。
- 四页均继续使用 2026-09-02 的脱敏审计快照，没有发起创建页面、生成素材、上传知识、保存策略、修改权限或其他写操作。浏览器逐页确认专用主标题存在、内部 tab 数为 0，控制台错误和警告为 0。

## 2026-09-04 — 订单物流与风险合规改为一页式工作台

- “订单与物流”移除订单查询、物流跟踪、关税测算三个内部页签，改成一条连续阅读路径：合同状态 → 物流节点 → 异常核对 → 关税复核。首屏同时展示 30 条交易合同、4 条样本未付款、162 条物流记录和 16/20 妥投状态。
- 页面新增“今天先看这三类状态”，把未付款、待确认收货、关闭/终止放到同一个优先处理区；合同与物流样本分别用真实状态分布刻度呈现，下方台账并排核对，不展示买家、合同号或物流单号。
- 关税测算保留为需要时使用的辅助工具，明确归类结果已返回但金额未保存，不能替代海关结论。订单创建、发货确认、退款、取消和状态修改仍不在页面能力范围内。
- “风险合规”移除风险首页、违规明细、禁限售与拒付三个内部页签，改成“当前快照 → 历史记录 → 专项核对 → 人工处置”的一页式证据链。
- 风险页用双层版式并列展示当前 5 项风险指标均为 0，以及历史累计处罚分 24、1 条 FCC 相关美国市场限制记录；避免把“当前没有风险商品”误解为“没有历史合规记录”。拒付材料、禁限售分析和供应商主体核验分别标注真实为空、未执行和待选择业务对象。
- 本轮只重排既有 2026-09-02 脱敏审计快照，没有调用订单或风险写命令。`node --check public/app.js` 与现有纯数据测试通过；浏览器逐页核对主要区块和数字，控制台错误、警告均为 0。

## 2026-09-04 — 参考商品完整回填与「复制同类」

- 产品发布矩阵收窄左侧商品区，把更多横向空间让给右侧参数编辑器；标题列采用更紧凑的固定比例，并增加独立“操作”列，长标题仍自动换行，不遮挡状态与按钮。
- 每条待发布商品新增“复制同类”。点击后会深拷贝标题、图片、类目属性、关键词、价格、起订量、交期、包装、物流与卖点，生成拥有独立数组和对象的新本地草稿；后续修改副本不会串改原草稿，也不会自动保存或发布。
- 修复“参考店铺已有商品”只带入标题和图片、参数全部空白的问题。服务端在读取模板和图库之外，只读调用 `icbu product list-information`，带回现有商品的 `attr`、交易、履约与卖点；前端再按实时类目 Schema 的 `attrNameId` 映射，过滤已经失效的固定枚举，不使用猜测的默认参数。
- 浏览器公开响应继续只使用短期 `referenceKey`，不返回真实商品号、SKU 标识或本机缓存位置。原商品资料只用于创建新的本地待发布草稿，验收不触发保存草稿或正式发布。
- `node --check` 覆盖服务端与两个前端脚本；2 项纯数据测试和 4 项服务/启动集成测试全部通过。新增断言覆盖真实属性、阶梯价、交期、包装物流、卖点，以及复制后可变数据相互独立。

## 2026-09-03 — 商品图片拖拽排序与移除

- 产品发布右侧图库改为真正的有序图片管理器：拖动图片左上角手柄可调整顺序，拖到第一位自动成为封面；悬停后同时提供前移、后移和删除按钮，窄屏下按钮保持可见。
- 排序、设为封面和删除统一写回 `product.gallery`，并保证 `product.image` 始终等于第一张。最终 `publish-from-json` 沿用这个顺序，不需要修改服务端发布协议。
- 移除图片会同步更新图片完成度和商品状态；若移除封面，下一张自动接替。删除本地上传图时还会清理其上传记录和 blob 预览，已启动的上传完成后也不会把图片重新插回图库。
- 浏览器实测把第 3 张拖到第 1 张、把第 2 张向后移动、删除当前封面全部成功；6 张变为 5 张后左侧同步显示 5/5，控制台错误和警告为 0。本轮只修改页面内本地草稿，没有保存或发布商品。

## 2026-09-03 — 全店历史商品图库持久缓存

- 插件服务开始监听后即在后台读取当前账号完整商品目录，并逐件调用 `icbu product list-information` 的 `images`、`detailImage`、`sku` 组件；不必等待用户打开产品发布页，也不再把经营分析接口附带的一张缩略图当作完整图库。
- 新增账号隔离的持久缓存：活动 Accio space 经过 SHA-256 摘要后形成缓存文件名，内容写入 macOS 用户缓存目录并使用 0600 权限；首次运行读取全部商品，以后优先恢复磁盘数据，只补齐新增、修改或超过 24 小时的记录。
- 全店同步最多并发 4 件并在每批后原子落盘。产品发布首屏不等待全量同步，页面显示已处理商品数、可用商品数和图片数；用户先点到尚未同步的商品时，会优先读取并立即保存这一件。
- “参考店铺已有商品”新增历史图片选择器，区分主副图、规格图和商详图，默认选择最多 10 张主副图，用户可自由取消或补选；第一张作为新草稿封面。读取模板后仍按当前类目最新 Schema 生成固定属性，不会修改原商品。
- 浏览器继续只使用 30 分钟短期 `referenceKey`，图片与模板接口都不返回真实商品号、账号哈希或本机缓存路径；清除缓存会同步删除精确的图库缓存文件。
- `node --check server.js`、`node --check public/app.js` 和 4 项 Node 集成测试通过。测试覆盖主副图、规格图、商详图分类、后台终态、磁盘落盘、活动 space 注入与公开响应脱敏。
- 当前真实账号首轮同步完成 389/389 件，共缓存 6,669 张可复用历史图片，失败 0 件；重启服务后立即从磁盘恢复 389 件和 6,669 张，后台校验的 `refreshedProducts=0`，证明没有重复逐件读取。浏览器实测选中一件商品后展示 16 张旧图、默认勾选 6 张主副图，并成功带入一条本地待发布草稿；页面控制台错误和警告为 0，本轮没有保存草稿或发布商品。

## 2026-09-03 — 发品工作区改为用户主动创建

- 产品发布页不再初始化 8 条示例商品，也不再把当前账号已有商品自动加入发布矩阵；首次进入显示 0 条待发布商品，并明确说明店铺商品只作为参考库，不会产生发布操作。
- “发布新产品”改为两条清晰路径：从零创建时先按业务名称选择账号常用类目，再动态生成右侧 Schema；参考已有商品时先搜索当前账号商品，选择后带入标题、主图、类目和可用文本，创建成功后才进入待发布列表。
- 根据浏览器反馈继续收紧入口：右侧空编辑器移除重复的“发布新产品”按钮；两种创建方式改为更大的非对称双入口，“参考店铺已有商品”置顶并标记“推荐”，突出它能自动带入内容且不会修改原商品。
- 未配置 `PUBLISH_IMAGE_BUCKET` 时不再使用红色报警文案，改为中性说明“管理员尚未开启电脑本地图片上传”；参考商品已有远程图片仍可直接使用，真实能力边界未改变。
- 新增 `POST /api/publish/account-reference`。账号商品真实 ID 只保留在 Node 服务端的 30 分钟内存映射中，浏览器只取得并回传随机 UUID `referenceKey`；缓存清理会同步销毁映射，普通用户无需查看或填写任何平台内部编号。
- 原有右侧动态属性、外部参考链接、图片上传、单品/批量草稿、真实发布队列、质量结果和左右独立滚动保持不变。侧边栏和“运营待办”没有修改。
- `node --check public/app.js`、`node --check server.js` 与 4 项 Node 集成测试通过。浏览器实测空状态为 0 条，参考库显示当前账号商品，选择 1 条后成功生成独立待发布草稿；从零创建显示 10 个业务类目且无数字 ID，控制台错误和警告为 0。

## 2026-09-03 — 发品编排、图片上传与参考商品导入落地

- 草稿和正式发布由直接调用 `new-publish-product` 改为 `publishflow publish-from-json`。服务端仍按单品维护可见进度，并在相邻提交之间维持至少 1 秒间隔；WorkCTL 返回的质量分、扣分原因和结构化失败结果会进入队列摘要。
- 失败任务不向浏览器暴露 `itemJsonPath`、内部错误文件或商品 material。页面只显示图片、标题、属性、价格、起订量、包装、履约和卖点等可理解区域，运营人员修改后重新确认即可生成新任务。
- 新增参考商品导入：用户只需粘贴 Alibaba 商品链接，服务端提取商品号并调用 `publishflow query-template-info-by-id`，前端带入类目、标题和最多 5 条文本；固定属性始终重新读取当前账号对应类目的官方 Schema，不复制参考商品旧枚举。
- 新增发品图片上传：浏览器即时生成预览，服务端校验 JPG/PNG/WEBP 的 MIME、文件签名和 8MB 上限，再调用 `icbu other upload-file` 获取远程 URL。bucket、endpoint、路径前缀只能通过服务端环境配置，不返回浏览器；未配置时上传入口禁用，已有远程图片不受影响。
- 右侧编辑器增加紧凑的参考导入区、图片读取/上传/失败重试状态、发布质量结果和失败补全入口。侧边栏结构、运营待办、左右独立滚动、单品/批量操作与 5 条卖点保持原设计。
- `start.sh` 新增 `publishflow` 与可选上传 Schema 预检。`node --check`、Shell 语法检查及 4 项 Node 集成测试通过；集成测试覆盖上传参数脱敏、参考商品脱敏、发布质量分、失败路径隐藏、批量串行和幂等处理。浏览器使用当前账号实时加载类目规则，未出现控制台错误，本轮没有触发新的真实商品发布。

## 2026-09-03 — 发品页改为当前账号动态类目与官方枚举匹配

- 移除“只能使用四个手写 Demo 类目”的正式发布假设。后端新增只读 `/api/publish/categories` 与 `/api/publish/category-schema`：前者把当前账号 `list-user-category` 的类目树展平为叶子类目搜索，后者联合 `list-attribute` 和 `list-attribute-options` 返回 PRODUCT 属性、必填标记、控件类型和官方选项 ID。
- 当前账号现场返回 8,885 个叶子类目；智能手表 `127684037` 返回 21 项 PRODUCT 属性，其中 10 项必填、20 项带官方选项。浏览器搜索并切换到时尚智能手表 `190000172` 后，表单实时变为 24 项属性、5 项必填，再恢复原智能手表类目。
- 前端在产品编辑器中加入“类目名称或 ID 搜索 + 叶子类目结果”控件；选择新类目后按 `attrNameId` 迁移相同属性，重新生成单选、多选、可自定义输入和产地控件。页面显示必填数与总属性数，并明确区分实时 WorkCTL 规则和加载中的本地草稿。
- 固定单选不再只提交显示文字：序列化时携带 WorkCTL 返回的正整数 `attrValueId`。服务端入队前再次读取或命中实时 Schema 缓存，检查类目属于当前账号、正式发布必填属性完整、枚举文字与 ID 成对匹配；多选继续遵守平台约定，以分号合并并使用 `attrValueId=-1`。
- `demo-data/workctl-demo.json` 保存本轮动态类目数量、两个跨类目验收结果和 ID 策略；不保存账号、凭据或完整店铺商品数据。计价单位与店铺物流模板仍没有可用查询工具，不伪造 ID。
- `node --check server.js`、`node --check public/app.js` 与 4 项 Node 集成测试通过；测试新增当前账号类目/Schema API 和错误枚举 ID 拒绝断言。浏览器实际加载实时规则、跨类目切换成功，控制台错误为 0。

## 2026-09-03 — 发布总进度、结果弹窗与草稿 material 结构修正

- 产品发布页新增“本次操作总进度”：单品按 `0/1 → 1/1` 展示，批量按 `0/N → N/N` 展示，并用 1、2、3… 的节点标出每个商品处于等待、执行、成功或失败。离开当前标签页不会中断服务端队列。
- 每次新发起的单品或批量任务全部进入终态后自动弹出结果；成功、失败和正式发布“已提交但待平台审核”使用不同文案。历史任务不会在刷新页面后反复弹窗，但可以从总进度区域点“查看结果”。
- 服务端为同一次请求生成 `operationId`，并为每条任务返回 `position` 和 `total`；旧任务缺少这些字段时按单品 `1/1` 兼容。单次与批量仍共用并发 1、确认短语和幂等边界。
- 只读核对 WorkCTL 动态 Schema 与当前账号已安装的 Alibaba Seller Assistant 发品规范后，确认 `material` 外层必须是字符串，内部完整商品 JSON 必须使用 `categoryId + basicInfo / trade / fulfillment / detail`。此前自造的扁平 `materialVersion / title / imageUrls / attributes` 结构不属于平台合同。
- 用户实际点击“保存当前草稿”后，WorkCTL 已收到 `publishType=draft`，但上游以 `failed to parse material` 拒绝旧结构，任务被正确记录为 `failed` 且不可自动重试；失败发生在材料解析阶段，不是确认、队列或 JSON 文件传输失败。
- 适配器已改用平台嵌套结构：标题、关键词、属性和图片进入 `basicInfo`，价格进入 `trade`，交期和包装进入 `fulfillment`，5 条卖点进入 `detail.productSellingPoint`。用户随后再次点击“保存当前草稿”，同一商品以 `saved_draft` 终态成功保存到国际站草稿箱，并返回商品 ID；这验证了修正后的材料结构已经被当前平台接受。
- 根据浏览器反馈把完成进度从大卡片压缩为单行状态栏，任务摘要、1…N 编号、百分比和结果入口同排展示；桌面端产品发布页锁定在窗口高度，左侧商品矩阵和右侧编辑器各自在卡片内部纵向滚动，页面本身不滚动，底部批量操作始终可见。
- `node --check server.js`、`node --check public/app.js` 与 4 项 Node 集成测试通过；受控假 WorkCTL 额外断言平台四块结构、图片对象、属性和值类型，以及批量 operation 的 1、2 顺序。

## 2026-09-03 — 接入单品与批量产品发布真实 WorkCTL 队列

- 按用户明确授权，把产品发布页从本地计时器演示改为服务端真实 WorkCTL 串行队列；统一调用 `icbu.product.new-publish-product`，保存草稿映射 `publishType=draft`，正式发布映射 `publishType=product`。
- 右侧当前商品增加“保存当前草稿 / 发布当前商品”，只处理当前打开的 1 个商品；顶部与底部增加“批量保存草稿 / 批量发布”，只处理左侧勾选项。四个入口都先显示商品范围和字段摘要，并要求用户勾选最终确认。
- 新增 `POST /api/publish/enqueue`、`GET /api/publish/jobs` 与失败任务人工重试路由。后端逐条执行、批量并发固定为 1；状态区分等待、执行、草稿已保存、已提交发布和失败，明确“已提交”不等于平台审核通过或已经在线。
- 写路由与只读 `ENDPOINTS` 分离，只接受同源 JSON，请求必须包含确认短语、作用域和幂等键；同一幂等键复用原任务，批量上限 50。`new-publish-product` 非幂等，因此不自动重试，只有 WorkCTL 明确返回可重试时才开放人工二次确认。
- 商品 material 在服务端按白名单重新构造，完整素材通过权限为 0600 的临时 JSON 参数文件传给 WorkCTL，执行结束立即删除；页面和命令日志只保存脱敏摘要，不返回或记录完整素材、网关凭据。
- 当前 WorkCTL 没有计价单位和店铺物流模板查询工具，因此不伪造整数 ID；material 会明确发送现有显示标签，由平台解析或返回真实失败。浏览器新上传的 `blob:` 图片在接入真实 OSS bucket 前会被前后端阻止，已有远程 Demo 图片可进入队列。
- 新增集成测试覆盖：缺少最终确认时拒绝、单品保存远端草稿、两条商品严格串行提交正式发布、重复幂等请求不重复执行。`node --check server.js`、`node --check public/app.js` 和 4 项 Node 集成测试全部通过。
- 使用 `./start.sh` 重新启动当前活动 Accio space，`/api/health` 返回 ready；浏览器实测单品与批量弹窗的范围和文案正确，最终确认框默认为未勾选、执行按钮默认为禁用。验收期间真实队列保持 0 条，没有对店铺执行保存或发布。

## 2026-09-03 — 原产地直接对照 Alibaba 发品页

- 通过用户明确指定且已登录的 Alibaba 国际站发品页，只读核对智能手表类目 `127684037` 的“产地”控件；确认它是“主输入框 + 独立搜索框 + 英文国家/地区单选列表”，不是省/市三级联动。
- 逐步滚动并保存该页面当前可见的 256 个国家/地区名称，原样写入产品发布 Demo；默认值从错误的 `China / Guangdong / Shenzhen` 改成平台实际选项 `China`。
- 产品发布右侧编辑器新增可搜索国家列表、选中态、键盘 Esc/向下操作和点击外部收起；以 `China` 搜索时实测只返回 `China` 与 `Taiwan, China`，选择 `Vietnam` 后能回填且商品必填完成度保持 `10/10`，再恢复 `China`。
- 此列表的来源是用户授权读取的 Alibaba 页面，不是 WorkCTL 返回值；正式发布适配器仍需以平台当时实际接受的 payload 为准。本轮没有在 Alibaba 页面保存或提交任何商品。

## 2026-09-03 — 产品发布字段按 WorkCTL 0.1.53 Schema 重构

- 在当前活动 Accio space 中只读实跑 `list-user-category`、四个叶子类目的 `list-attribute` / `list-attribute-options`，并逐个读取基础信息、详情、交易、履约、样品、定制、特殊服务、预检、新建发布、正式发布和草稿提交 Schema；本轮写命令执行数仍为 0。
- 确认智能手表 127684037、智能手环 127688045、降噪真无线耳机 202055012、运动耳机 202061615 的必填属性不同。产品发布编辑器改为类目驱动配置：平台单选用下拉，平台多选用标签加下拉，原产地显示为系统地区选择器；切换类目会同步重置属性和完成度。
- 移除不可提交的自由文本价格区间与交期，改为 `saleType` 条件字段、数字 MOQ/库存、可增删 `ladderPrices` 阶梯价、可增删 `ladderPeriod` 发货期、成组包装长宽高/毛重和物流属性编码。
- 计价单位、物流模板和地区列表在当前 WorkCTL 命令目录中没有可用查询入口，界面明确标为系统来源并保留演示标签，不伪造正式整数 ID 或可提交的自由文本。
- 状态不再依赖静态布尔值：每次从类目必填值、交易数字、阶梯价、阶梯交期、包装成组约束和物流模板重新计算本地校验。浏览器把单价改为 0 后，列表即时变为“待填写 / 待补全”。
- `demo-data/workctl-demo.json` 已保存四类 Demo 字段、控件策略、命令证据、已知限制和零写操作边界；`design-qa.md` 已补充同画布前后对比、桌面/窄屏和交互验收。
- 静态检查、JSON 校验和 3 项 Node 集成测试通过；桌面 1304 × 1167 和窄屏实际约 487 × 1055 均无页面级横向溢出，浏览器错误和警告为 0。

## 2026-09-03 — 落地「批量商品矩阵工作台」产品发布原型

- 按用户选定的第 2 张 Product Design 原型，在“商品运营”下方增加独立“产品发布”页面；原有侧边栏分组和其他入口顺序不变，“今日运营待办”继续保持原样。
- 页面形成一条连续发品工作流：顶部上传/待补全/检查通过/队列状态，左侧批量商品矩阵，右侧当前商品字段校对，下方逐条发布队列与固定操作栏。
- 支持普通图片多选、文件夹拖放/导入、封面切换、标题/类目/10 项必填属性/价格库存/交货期/包装毛重/物流模板/卖点编辑、搜索筛选、批量勾选和批量补标题。
- 演示数据使用当前店铺商品效果接口中已经返回过的公开缩略图；字段结构来自本轮只读核对的 WorkCTL 发品 Schema 与智能手表类目属性，不保存商品 ID、账号、联系人或凭据。
- `demo-data/workctl-demo.json` 新增 `productPublishing`，固化已核对的基础字段、10 项类目必填属性、交易履约字段、草稿/正式发布类型、逐条任务链路和零写操作边界，便于后续接真接口时继续复用。
- 根据浏览器批注补齐编辑能力：关键词从只读标签改为最多 5 个的原位编辑、删除与补充控件，卖点从 3 条扩展为固定 5 条并显示序号、占位提示和字数；修改继续实时写回当前本地草稿。
- “确认并开始排队发布”会先展示二次确认，再按并发 1 的策略模拟逐条执行；单条失败不阻塞后续任务并可重试。当前所有草稿、上传图片和任务都只存在浏览器内，没有执行 `upload-file`、`new-publish-product`、`publish-product` 或其他写命令。
- 在 `CONTEXT.md` 记录未来正式链路：素材上传与分析、类目属性、发布前检查、草稿/正式发布和任务追踪；真实接入前仍需补齐权限、幂等、超时、失败续跑与脱敏日志设计。

## 2026-09-03 — 将 RFQ 商机升级为列表与详情一体页

- 保持侧边栏结构不变，把原先“商机池 / 报价记录 / 报价权益”三个子标签收敛成一张连续运营页：顶部经营指标，中部商机列表与详情同屏，下方连续展示报价历史和权益。
- 新增 6 个只读 RFQ 端点：站内搜索、站外搜索、站内详情、站外详情、报价历史和报价权益；服务端白名单合计由 30 个扩展到 36 个。
- 以店铺主营词 `smart watch` 完成真实调用：09:30 快照为站内 RFQ 324 条、站外 RFQ 45 条、报价历史 5 条，09:32 页面刷新时站内总量变为 323 条；页面样本分别展示前 10 条、前 10 条和全部 5 条。
- RFQ 详情展示采购主题、描述、国家、类目、采购数量、发布时间、报价竞争、剩余席位、买家历史搜索/询盘/RFQ/报价查看信号，以及站外认证、有效期、贸易条款、付款条款和目的港。
- 页面“运营优先级”只用于排序，并明确不是平台匹配分；计算依据为脱敏后的买家行为、采购量、认证、贸易条款与剩余席位。
- 报价权益实时查询在 2026-09-03 等待 125 秒后仍未返回，因此首屏使用 2026-08-30 已核验审计快照，并明确显示实时状态，避免阻塞商机池和报价历史。
- RFQ 返回会在服务端移除买家姓名、登录账号、RFQ/报价跳转链接和报价正文；`demo-data/workctl-demo.json` 只保存匿名采购样本、汇总、状态和权益快照。

## 2026-09-03 — 补强客户、商品、流量和关键词广告四个经营页面

- “今日运营待办”保持原样，继续预留给 Dify 结构化输出；本轮没有修改待办规则或内容。
- 客户与询盘从单一访客表升级为客户经营页：联合展示近 30 天访客、未读会话、首次回复率、平均回复时长、高意向访客排序、最近会话国家汇总、买家身份、主要国家、进店词和访客明细。高意向分仅用于页面排序，并明确使用询盘、TM、RFQ、联系方式、下单和深度浏览行为，不冒充平台官方评分。
- 商品运营读取完整 378 条商品效果记录，以搜索曝光 P75（4）和全店加权点击率（1.45%）形成四象限：高曝高点 12、高曝低点 101、低曝高点 6、低曝低点 259；同时展示 18 个点击未询盘、5 个零搜索曝光及质量诊断快照，原商品列表降为下钻证据。
- 流量分析合并原市场机会内容，在同一页展示 11.79 万曝光、2,929 点击、1,603 店铺访问、289 个询盘与 TM、来源渠道、国家分布、买家身份、进店词、渠道承接、行业国家、细分类目和需求场景。侧边栏其他入口顺序未改，仅移除被明确要求合并的“市场机会”独立入口。
- 关键词与广告收敛为单页：店铺词库实跑快照包含高询盘词 16、高引流词 20、高 P4P 词 16；实时可售关键词 100 个、次月资源 3 个。问鼎与顶展在 2026-08-03 至 2026-09-01 的公司、关键词、搜索词、商品和达标率效果均为 0，页面明确说明这是投放效果真实空，不是接口未接，也不拿资源词冒充效果。
- 服务端白名单扩展到 30 个只读端点，新增 `/api/dashboard/product-analysis` 与 `/api/demo`。完整商品分页每批最多并发 4 个 WorkCTL 进程；对偶发长时间不结束的质量诊断、TM 最近会话和广告效果查询使用本轮已实跑的脱敏 Demo 快照，避免阻塞页面。
- 更新 `demo-data/workctl-demo.json`，保存四象限门槛和计数、客户画像与会话汇总、合并后的市场机会样本、广告词库与两条产品线效果核验状态；继续排除联系人、账号、订单、商品标识和凭据。
- `node --check server.js`、`node --check public/app.js` 与 Demo JSON 解析通过；本地服务用 `./start.sh` 解析当前 Accio space 和 WorkCTL 0.1.53 启动，浏览器逐页确认无控制台错误。更新前后并排证据保存在 `design-audit-2026-09-03/`。

## 2026-09-02 — 全量 WorkCTL 只读审计并补齐 15 个侧边栏页面

- 保持既有侧边栏的六个分组、入口名称和顺序不变；“运营待办”继续作为总览锚点，本轮不改文案、逻辑或数据，后续由 Dify 结构化输出接入。
- 对 124 个查询 Schema 做全量审计：123 个完成真实调用尝试，112 个成功，11 个因无权限、缺少现成任务 ID、缺少会话上下文或上游超时受阻，1 个仅完成 Schema 核对；生成、发布、发送、删除、预定、绑定等写命令执行数为 0。
- 将关键词广告、RFQ、订单物流、风险合规、店铺装修、素材工坊、知识库与接待、账号与权限八个缺失页面升级为可渲染界面；数字来自真实返回或真实空状态，不计算匹配分、同比、角色数等未返回指标。
- 新增 `demo-data/workctl-demo.json`，保存 29 天趋势、渠道、国家占比、广告资源词、市场国家样本、订单物流状态样本和各模块脱敏汇总；不保存联系人、买家、账号、订单、商品标识、chatToken 或 gateway token。
- 新增 `demo-data/workctl-command-audit.json`，保存命令审计数量、受阻命令与原因，并明确真实空结果不等于接口未接入。
- 15 张生成模型草图只作为探索稿保留；因部分草图出现无来源数字，最终逐页交付改用同一套本地 HTML/CSS/JavaScript 页面在浏览器中直接渲染截图，保证侧边栏与数字可追溯。
- `node --check server.js`、`node --check public/app.js`、两份 Demo JSON 解析检查和 3 项集成测试全部通过。

## 2026-09-02 — 用 workctl-dashboard 替换旧来搜 Plugin 工程

- 按用户明确要求，当前工程不再保留旧的 13-Skill 来搜 Plugin、Plugin ZIP、教程图、结构校验器和旧 Chrome 验收材料。
- 旧工程内容已整体移入 macOS 废纸篓目录 `/Users/garden/.Trash/lsou-plugin-project-old-20260902-Nu6vxv/`，需要时仍可恢复。
- 新工程来源为用户明确指定的历史 Accio space 项目：`/Users/garden/.accio/accounts/7070142663_212003/agents/MID-19142663U1780995-A3D011-0825-A7B153/project/workctl-dashboard/`。
- 当前 Accio 活动 space 实测为 `1729094531_212003`，因此只把指定目录作为只读来源复制，没有修改或启动历史 space 中的源项目。
- 已复制 `README.md`、`server.js`、`start.sh`、`server.log` 以及 `public/` 下的原生 HTML、CSS、JavaScript。
- 新项目采用原生 HTML/CSS/JavaScript 前端和零第三方依赖 Node.js 后端；本轮没有改动其业务代码。
- 当前 `server.js` 仍默认引用历史 space 下的 Workctl `0.1.53` 路径；这不是稳定运行配置，后续需要改成动态定位或由 `WORKCTL_BIN` 显式传入。
- 已重建 `CONTEXT.md`，记录项目入口、数据链路、状态、安全边界和验证方法。
- 已补齐当前工程副本 `start.sh` 的用户执行权限；Node.js 与前端 JavaScript 语法检查通过，启动脚本可拉起本地服务，`/` 返回 HTTP 200，`/api/endpoints` 返回 9 个白名单端点。
- 本轮没有执行真实经营数据查询；当前活动 space 与代码默认 Workctl 所在的历史 space 不同，需先改成当前运行态动态定位后再做真实链路验收。

## 2026-09-02 — 修复经营大盘 Workctl 参数解析失败

- 页面报错为 `unknown flag: --startDate`，并提示 `advisor` 未注册在 `workctl icbu` 下。
- 已稳定复现并确认存在两个前置条件：Codex 启动的服务没有继承 Accio gateway token，动态 `icbu advisor` 命令目录未加载；当前 Workctl `0.1.53` schema 同时要求 `--start-date`、`--end-date`、`--statistics-type`，不能直接使用 camelCase 字段名。
- 新增 `test/server.integration.test.js`，通过真实 HTTP 路由和受控假 Workctl 复现参数错误；修复前测试准确失败，修复后通过。
- `server.js` 新增 `toCliFlag()`，只在构造 CLI 参数时把前端业务字段转换为 kebab-case；端点白名单、`execFile` 和只读边界保持不变。
- 已使用当前账号 `1729094531` 的 runtime 凭据和当前活动 space `1729094531_212003` 的 Workctl `0.1.53` 执行只读 `shop-summary`：退出码 0，返回 29 条记录。
- 已重启当前工程的 `8787` 服务并进行 HTTP 与页面验收；接口返回成功，命令日志使用正确参数，页面“本店 vs 同行”正常显示数据，浏览器控制台无错误。
- gateway token 只注入当前服务进程环境，没有写入代码、文档、日志或前端响应。

## 2026-09-02 — 增加 Accio 运行态智能启动和健康检查

- 将 `start.sh` 改为标准智能启动入口：每次从 Accio Desktop 本地健康状态识别当前活动 space，再读取对应账号的 runtime gateway 配置和当前插件声明的 Workctl 版本。
- 启动脚本会先执行 Workctl 健康检查和经营大盘 schema 检查；Accio 未运行、凭据失效、插件缺失或动态 `advisor` 命令未注册时会提前停止并输出明确原因。
- gateway token 只保存在启动脚本和 Node 子进程的内存环境中，不打印、不写入项目文件，也不通过健康接口返回。
- `server.js` 新增 `GET /api/health`：通过真实 Workctl 子进程返回 `ready` 或 `degraded`，响应只包含可达性、认证状态和白名单端点数。
- 新增 `test/start.integration.test.js`，在隔离的假 Accio 目录中验证活动账号、Workctl 路径和运行态凭据解析；扩充后端集成测试，验证健康检查及敏感值不会进入响应。
- 测试驱动过程已观测到预期失败：旧脚本不会解析当前 Accio 运行态，旧后端对 `/api/health` 返回 404；实现后 3 项集成测试全部通过。
- 已关闭此前手工注入环境变量启动的服务，并只用 `./start.sh` 重新拉起当前工程。实测解析到活动 space `1729094531_212003` 的 Workctl `0.1.53`，`/api/health` 返回 `ready`，经营大盘区间查询返回 31 条真实记录。

## 2026-09-02 — 按来搜官网品牌重构经营面板 UI

- 使用 Product Design 工作流读取用户提供的官网截图、`/Users/garden/YD/l-sou/logo.png`、`/Users/garden/YD/l-sou/新logo.png`，并实时核对 `https://l-sou.com/` 首屏。
- 官网实测主色为 `#ff6600`，字体栈以 Helvetica Neue 和苹方为主；界面由原深色工程控制台重构为白色内容面、暖米色品牌区、橙色主操作和轻量卡片阴影的经营工作台。
- 页面产品名称改为“来搜 · 国际站经营驾驶舱”，增加业务化首屏引导；保留日期、七个业务标签页、12 项 KPI、图表、表格、筛选、分页、弹窗、缓存和数据控制台的既有 DOM 标识及真实调用链路。
- 两张源 Logo 已原样复制到 `public/assets/`，复制后 SHA-256 与源文件一致；方形 Logo 用于应用标识，新版透明 Logo 用于经营概览品牌签名。
- 数据图表切换到来搜橙为主、绿/红为业务语义的浅色调色板；命令控制台保留深色局部表面，以区分业务视图与技术输出。
- 完成桌面和窄屏响应式检查：窄屏采用双列 KPI、统计控件至少 44 px、导航单行滚动，并增加减少动态效果支持。
- 浏览器实测商品效果返回本页 20 条、共 378 条；经营概览 KPI 可切换趋势指标，浏览器错误和警告均为 0。
- `design-qa.md` 记录官网与实现并排证据、页头聚焦证据、首轮 P2 修正和最终通过结论。

## 2026-09-02 — 重构为左侧业务边栏布局

- 继续使用 Product Design 工作流，将桌面端原顶部七项导航改为 240 px 固定左侧业务边栏，右侧形成独立数据工作区；品牌、实时链路状态和业务入口集中在左侧，统计周期与页面标题留在右侧顶部。
- 七个入口按“经营分析 / 客户与团队 / 系统工具”分组；切换标签页时同时更新选中态、`aria-current` 和右侧工作区标题，原数据请求、筛选、图表和表格链路不变。
- 在 1304 px 桌面视口将 12 项 KPI 调整为 4 × 3 网格，避免侧栏占用后卡片文字过挤；960 px 以下侧栏自动转为顶部品牌区和横向可滚动导航，680 px 以下保持双列 KPI 与 44 px 以上触控目标。
- 浏览器实测桌面无页面级横向溢出，商品效果切换后标题与选中态同步并返回 20 条数据；窄屏实际 CSS 视口约 487 × 1055，无页面级横向溢出，浏览器错误和警告均为 0。
- 新增侧边栏版桌面、窄屏与改版前后并排截图；`design-qa.md` 完成第二轮布局验收并保持通过结论。

## 2026-09-02 — 落地 Product Design「行动优先工作台」方案 1

- 按用户选定的第一张原型，把经营总览重构为“六项核心结果 + 经营链路与趋势 + 今日运营待办 + 三类诊断摘要”，保留左侧边栏与右侧内容区的总体布局。
- 左侧导航改成经营中心、增长运营、交易保障、内容资产、组织管理和系统工具六组业务信息架构；现有 7 个只读数据页继续可用，8 个尚未接入的入口统一展示“规划”，点击只说明边界，不展示模拟数据。
- KPI 从 12 张碎片化卡片收敛为 6 项核心经营结果。曝光、点击、询盘、成交按区间累计；首次回复率和平均回复时长使用区间末日的 30 天滚动快照，避免错误累加。
- 总览并行调用真实 `shop-summary`、`shop-flow`、`shop-region` 和 `shop-product` 数据；三项可执行待办由点击率、访问转询盘率和询盘转成交率推导，另外两项明确标为规划中。
- 侧栏支持收起为 68 px 图标栏并触发趋势重绘；补齐收起/展开按钮的动态 `aria-label`，窄屏继续切换为顶部横向业务导航。
- 使用选定原型与真实实现的同状态并排图完成 Product Design QA；渠道、国家和商品内容使用真实返回值，不复刻原型中的示例数字、负责人和订单风险。
- 静态语法与 3 项集成测试通过；浏览器实测规划入口提示、商品待办跳转、商品 20 行数据、侧栏收起/展开及窄屏无整体横向溢出。

## 2026-09-05 — 客户与询盘三栏工作台

- 开始：用户批准将报表与沟通区重新组织，沿用原生 JavaScript + Node.js。
- 完成：客户页首屏改为紧凑指标带与三栏会话工作台；原有访客分析保留在客户分析视图，移除空的最近会话跳转卡和“实时查询”伪指标。
- 客户列表加入本页搜索、时间、选中态及非零未读角标；选择客户自动读取消息与背景，独立显示错误并支持刷新。版本号隔离不同客户和重复读取的异步响应；历史消息合并去重。
- 商品目录按需加载，买家画像摘要与全文分开展示，避免长文本掩埋操作。样式限定客户页，支持三栏独立滚动与窄屏换行。
- 验证：12 项现有 Node 测试通过；浏览器已验证真实会话消息/背景同步显示与分析视图切换。未调用消息发送或平台写操作。

- 用户后续调整：客户分析切换项移到左侧，设为首次打开及再次加载客户页的默认视图；会话工作台保留右侧入口。

- 修复高意向访客双列布局：分层标签由行末改为紧邻客户名称，两列增加 28px 间距，防止左列分层与右列评分混淆。

## 2026-09-05 — 商品四象限筛选与弹窗修改

- 删除“全部商品效果”界面及其自动查询，删除编辑区独立全店商品选择器。
- 四象限返回全部匹配商品，前端每页 20 件，切换象限从第一页开始；后端发放精确商品引用，点击列表商品直接打开修改弹窗并读取线上内容。任务记录移到页面，保存与提交仍分开确认。
- 商品漏斗与质量诊断压缩为右侧六个紧凑条目，减少首屏占用。
- 12 项原有测试通过，新增同名商品引用隔离与无效编号测试通过。浏览器真实验证低曝高点返回 4 件、列表点击打开正确商品弹窗，线上内容成功读取；未执行平台保存或提交。

## 2026-09-05 — 商品编辑直接读取当前字段

- 参考产品发布的商品参数读取方式，扩展 product-info 查询属性、交易与履约字段。
- 弹窗自动填入标题、关键词、卖点、公司介绍，直接展示可选择替换的主图；属性、价格、起订量、库存和履约以当前资料展示，详情图及 FAQ 保留展开阅读。
- 加载中禁用表单与保存；失败可重新读取。请求版本隔离旧结果，保存按读取基线只提交差异，未修改不会创建写任务。
- 运营接口 7 项测试通过，语法检查通过；真实页面验证标题、卖点和公司介绍自动填充，6 张主图直接展示，17 项属性、阶梯价格和交期成功返回；未修改时保存被本地拦截，无真实写操作。

## 2026-09-05 — 商品编辑对齐发布页布局

- 将商品弹窗改成左侧六段导航、右侧独立滚动表单、固定底部操作栏；分区为产品图片、基础信息、类目属性、交易信息、履约与包装、商品详情。
- 复用现有字段节点及事件，保持自动读取、差异保存、任务确认逻辑；属性与交易履约仍标明只读。字段样式、分区卡片与发布页保持一致。
- 已通过 JavaScript 语法检查，浏览器进行加载和导航验收。
- 浏览器验收通过：真实商品标题 128 字符、17 项属性保留，导航点击定位交易信息并同步选中态，底部保存持续可见；无改动点击保存显示“尚未修改商品内容”，未创建写任务。

## 2026-09-07 固定业务界面改进第一轮

- 开始：用户要求先Git再改；初始化仓库并提交现状基线 `1e19e2b`。
- 完成：总览独立指标、待办空状态、绩效三视图及排序、国家代码显示、订单日期/状态和当前页汇总、部分开发文案清理及锚点避让。
- 验证：前端语法检查通过，现有13项测试全部通过；本机健康接口ready，浏览器已确认总览数据和绩效切换。
- 异常：受限测试环境引发Node内部断言和本机服务连接失败；允许测试本地监听后全部通过。未改业务写入链路。

## 2026-09-07 流量分析优化

- 开始：用户确认实施渠道整合、趋势联动、数据口径及行业分区。
- 完成：本店/行业页内切换、独立指标、渠道排序与同源每日趋势、搜索词表格、国家图自适应和占比说明、行业商品选择懒加载。保留既有原始查询与业务边界。
- 验证：node语法检查及git diff --check通过；现有13项测试通过。浏览器观察到系统推荐按询盘4条排首位，点击后趋势标题/指标同步；行业中文国家排行正常。
- 限制：渠道人数为日累计、跨日跨渠道未去重；行业排名及热度比较周期未由当前字段完整提供，页面明确以平台定义为准。

## 2026-09-07 流量页连续展示

用户要求本店与行业在同页直接看完：去掉页内切换和行业隐藏状态，进入流量页即独立加载行业数据。本店在前、行业在后，保留各自统计口径、渠道联动及按需展开的商品选择器。此记录取代此前双视图切换说明。

## 2026-09-07 本店与行业主题对照

按用户纠正，将国家表现/行业国家供需、进店搜索词/行业需求场景分别并排放到主分析区，渠道与趋势随后展示。保留所有原容器ID和查询逻辑，行业不再独立堆在底部。两侧独立标注周期，不按行匹配或计算跨口径差距；窄屏才按主题依次排列。本记录取代此前上下两大区域说明。

## 2026-09-07 移除重复渠道查询

按用户标注移除自定义渠道趋势查询的界面、事件绑定和空容器；保留主区渠道联动趋势及底部行业查询。insights安装判重改用ops-market，避免重复创建行业区域。

- 2026-09-07：按用户标注移除渠道趋势下方“查看每日数值”入口及明细表；保留折线图、数据说明和悬停数值。

## 2026-09-07 星级自动加载与紧凑呈现

进入经营总览由stars()自动只读加载最新星级，保留刷新按钮与失败重试。renderStars()优先展示平台标记最优赛道：展示/评定星级与统计日期摘要、赛道分数和平台升星差距、商机/交易指标及平台建议两列卡片、基础门槛标签；币种与百分比保留平台displayText。赛道切换复用当前结果。已现场核验自动返回1星、63分及距2星7分，交易赛道可切换到复购率指标；语法、差异检查及13项现有测试通过。

## 2026-09-07 运营待办Dify Chatflow

按用户指定dify-workflow Skill创建 `dify-chatflows/lsou-operations-todo.chatflow.yaml`，7节点DAG，接收业务对象事实快照，LLM生成建议，代码绑定原始证据和安全页面入口，支持6轮窗口追问。模型由用户导入后自行配置，不再为模型选型阻塞。演示输入为明确虚构数据；8个代码节点测试及CLI严格validate/checklist通过，已自动布局。模型未实跑、DSL未导入或发布、网页仍未接入Dify。输入输出及后续接入约定在README。
# 2026-09-07 运营 SOP 归档与 Prompt 统一规则

- 开始：读取用户指定的《国际站运营SOP.docx》，提取市场定位、选品、关键词、发品、推广、优爆品和诊断等正文方法。
- 完成：原件复制到 `docs/references/`，增加同名 Markdown 正文检索版；根目录 `AGENTS.md` 要求全部模块的 Prompt 新增/修改先参考相关 SOP，并记录目标、输入、判断、动作及输出的映射。同步 CONTEXT.md。
- 验证：源文件与项目原件 SHA-256 一致；正文按 Word XML 段落顺序提取，未改写内容。仅增加资料和项目约定，未修改运行代码或远端 Dify。

## 2026-09-07 按 SOP 重写运营待办 System Prompt

- 开始：阅读项目 AGENTS、SOP 正文、现有 LLM 提示词及输出约定。
- 完成：新增可复制 Prompt 文本并用 dify-workflow 更新 llm_node；参考“高质量发品、产品定位/定品、付费广告/场景流量、优爆品打造、询盘质量跟进、店铺诊断”，明确每类所需事实、判断方法、具体动作与复查成果。
- 边界：现有输入不支持独立店铺/广告计划等对象，缺数据不生成虚构待办；SOP 数值和升品时效需适用依据。保持模型配置、用户输入、图结构、输出字段与代码节点不变。
- 验证：Prompt 文本与 YAML 一致，DSL strict validate/checklist、8 项现有代码节点测试及 git diff --check 通过；未实跑模型，输出语义效果仍需配置模型后验证。

## 2026-09-07 交付简化运营待办 Workflow

- 按用户确定的范围生成3节点Workflow，移除对话、记忆、对象绑定与追问设计。输入为经营总览JSON，输出为result.tasks。模型保留占位供用户配置。
- System Prompt结合SOP店铺诊断、流量广告复盘、选品、高质量发品、优爆品及询盘跟进，映射事实到任务依据、步骤和验收成果；不强套原文时效阈值。
- 已检查三节点图、输入引用、无sys.query/记忆、Schema一致性。CLI validate/checklist唯一报错为其未识别LLM的structured_output输出；已对照Dify仓库的结构化输出DSL实例核对该字段：https://github.com/langgenius/dify/issues/35789 。未绕改输出为文本，未声称CLI通过。尚未导入或实跑模型。

## 2026-09-07 Dify 运营待办真实接入

- 用户授权配置应用Key并放到经营总览；凭据保存在被忽略的.env.dify，权限600，日志不记录Key和业务正文。应用info返回200，确认名称为来搜经营总览运营待办、类型workflow。
- 新增后端调用及结果校验、同源POST限制、超时；前端总览自动生成及重试、数量、折叠步骤和验收标准；动态文本转义防止HTML注入。
- 验证：13项现有测试通过；适配函数的成功返回、无效字段和无效输入检查通过；JS语法和diff检查通过。
- 实跑发现：上游成功返回但将最终tasks嵌套于单项tasks且夹杂非约定字段；后端仅兼容明确的一层嵌套，提取后继续严格校验并过滤额外字段。真实返回回放提取3项通过。Key路径HTTP404、跨源POST403、文件权限600及Git忽略均核验通过。
- 最终浏览器实测：真实Workflow调用成功，经营总览显示2条待办（曝光点击下降排查、2星参考差距核查），数量同步到侧栏，首项展开显示依据、执行步骤及验收标准，重新生成按钮可用。

- 2026-09-07：按用户要求将经营总览运营待办改为原生编号列表，每条仅展示任务标题，移除折叠卡片、优先级标签及长篇详情展示；Dify调用和输出校验保持不变。JS语法与diff检查通过。


## 2026-09-08 运营待办每日复用与持久化

- 按用户要求，运营待办按北京时间每天首次访问自动生成一次；当日刷新页面、刷新经营数据及切换周期均复用保存结果，点击“更新待办”才强制重算。未新增后台定时调用。
- GET 读取已保存结果，POST 的服务端日期检查避免仅靠浏览器缓存重复付费；正在执行的自动/手动请求共用 Promise。开始调用前落盘尝试日期，失败或中断当天不自动重试，更新失败保留上次结果，零条待办同样保存。
- 结果按活动账号哈希隔离，保存在 Application Support 目录，0600 文件原子替换；服务重启后恢复，普通清除查询缓存不会删除。页面显示生成时间、原始统计周期及旧结果/失败状态。
- 未改动 Dify Prompt、经营快照内容或运营方法，因此无需调整 SOP 映射；现有输出字段校验保持不变。
- 验证：Node 语法检查通过；15 项 Node 测试全部通过，新增真实 HTTP + 模拟 Dify 测试覆盖并发去重、北京时间跨日、手动更新、重启恢复、失败保留、空结果缓存、账号隔离与中断抑制，前端测试验证缓存命中不等待经营数据、不提交模型请求。受限环境无法监听测试端口，允许本地监听后通过；测试不消耗真实模型 token。
- 页面验收：新服务真实生成并保存 2 条待办（2026-09-08 08:30 北京时间）；刷新页面后仍显示相同两条与原生成时间，接口 shouldGenerate=false、generating=false。更新按钮与原始统计周期显示正常。


## 2026-09-08 运营待办执行深度与界面重做

- 从纯标题列表改为右侧工作摘要：编号、优先级、第一步预览、步骤数量；点击后在同页展开完整执行方案，依据/验收在左、执行步骤在右，手机端单列。生成时间和依据周期移到页脚。展开与关闭不发起模型调用，不新增假完成按钮。
- 参考 SOP“产品定位/定品”“关键词库”“高质量发品”“付费广告数据分析”“询盘质量跟进”“店铺诊断+产品优化+运营规划”：输入补充商品前20样本和渠道原始明细，判断时保持对象/统计范围与缺项；动作要求明确数据位置、核查字段和处理分支，输出沿用 title/priority/basis/steps/acceptance_criteria，并要求可复核的交付表格字段。
- 未覆盖：广告计划花费、关键词转化明细、图片详情内容和询盘正文/跟进状态，均标明不可用；不据此编造问题。渠道真实记录含 statisticsType=30d，保留滚动周期并在提示词中禁止跨日相加或与日曝光直接归因。未核验 SOP 数值阈值、预算与时效，不作为当前通用规则。
- 新增 enrichOverviewTaskSnapshot 在每日缓存检查后执行，默认查询两类只读数据，字段白名单过滤内部ID/图片URL，缺字段不补零；输入体积超限时缩减渠道样本并标记截断。
- Dify 应用“来搜经营总览运营待办”的 System Prompt 已同步新增执行深度规则；保持模型 deepseek-v4-flash、输入变量、输出 Schema 与三节点图不变。首次线上修改被自动审批拦截，用户要求继续后成功修改并发布版本 #2。
- 验证：15项 Node 测试通过，包含并发去重、跨日、重启、失败旧结果、空列表缓存、账号隔离及新增输入字段白名单/缺项保留；页面测试验证展开详情显示完整依据/步骤/验收且不调用模型。

- 真实生成验证：2026-09-08 08:40:56 仅手动生成一次，输入42,323字符（20商品、120渠道），云端运行 2d371bf7-631d-4dd2-9093-d5bed68b4e01 成功，耗时180.917秒、37,605 tokens。本地 blocking 请求收到HTTP504；云端 End 的 result.tasks 为空，但 LLM 最终 JSON 实际包含3条详细工作单。通过日志页面复制最终输出并严格校验，按原运行完成时间08:43:57与原统计周期恢复本账号缓存，保留恢复前备份；没有为恢复内容重复调用模型。
- 修复：线上版本 #3 启用推理标签分离，并在 End 保留 structured_output 的同时追加 raw_result=text。服务端流式接收 workflow_finished，最多等待6分钟；优先校验最终 JSON，结构异常不当成零任务。中间推理不展示、不记录，连接中断保留旧结果且不自动重试。字段 reasoning_format 对照 Dify 官方类型定义核验：https://github.com/langgenius/dify/blob/main/web/app/components/workflow/nodes/llm/types.ts 。本地 YAML 同步输出和分离设置；占位模型仍供导入时配置，未宣称与线上实际模型相同。
- 最终验收：16项 Node 测试全部通过；随后将每日持久化 HTTP 集成测试改为模拟 SSE + 空结构化/有效最终文本，3项相关测试再次通过。覆盖跨UTF-8分片、CRLF、结束事件缺失、上游错误、无效JSON、过长字段及真实空任务。桌面与窄屏排版、详情打开/关闭已经浏览器检查；服务重启及刷新后显示3条、原生成时间不变、shouldGenerate=false。新版云端设置已发布并核验，修复后未额外发起付费生成；真实内容来自本次已完成运行，完整新链路以模拟上游验证。

## 2026-09-08 按指定参考图恢复总览设计一致性

- 读取 design-action-workspace-implementation-cropped.png，对齐白底卡片、紧凑行动行、小图标、标题旁优先级及右侧橙色入口；移除待办独立的米色底色、大字号和大编号。列表单行展示依据摘要，完整内容在执行方案中阅读，长标题保留原文与悬停提示。
- 展开方案沿用总览灰色边线、白底及12/14px字号；保留全部依据、步骤和验收。总览指标与底部三栏的折行阈值从1320调整为1180px，使当前桌面宽度与设计图一致。未修改Prompt、生成逻辑或已保存内容。
- 验证：JS语法、git diff --check、3项待办回归测试通过；浏览器对比桌面列表和展开方案、检查关闭与刷新，窄屏单列且页面无横向溢出。已恢复桌面预览。本轮未调用模型生成。

## 2026-09-08 运营诊断：逐指标双基准与定向优化

- 用户将目标从执行待办改为诊断：与行业均值、优秀对比，解释短板并指向标题、主图、详情和广告。保留原生JS/Node、白底紧凑设计、按账号每日持久化；不把旧待办直接当新诊断。
- 新增 buildOverviewBenchmarks：曝光/点击/询盘/成交用同批日期记录，回复率/时长取最新快照；同时计算均值与优秀差距，正确处理反向时长、百分点、零基准和缺项。平台未返回具体行业/类目范围，页面注明。结果随诊断保存，历史对标不跟随页面新日期混用。
- Prompt先读SOP，按“店铺诊断/运营规划”映射对标与趋势到短板；“产品定位/关键词库”映射真实商品标题到相关主词和属性表达；“高质量发品”映射主图识别/卖点与详情参数/定制/品质承接；“广告/场景流量”映射计划/商品/地域/词到流量规模和匹配；“询盘跟进”区分回复效率、需求、报价与成交证据。禁止将数量少当效率差、把同业值当同行具体做法、用滚动渠道量拼日漏斗。
- 未覆盖证据：商品图片/完整详情、广告花费/归因、询盘正文与报价跟进。相关结论只能是有条件的优化建议，必须说明验证条件；不强套SOP预算、数值阈值或收益时效。仍沿用Schema字段以兼容旧记录，业务含义改为诊断主题/原因/优化方向/验证复查，不表示已执行。
- 线上System Prompt更新并发布 #4，LLM节点改名按SOP生成运营诊断。已发起1次真实生成验收。17项Node测试通过，新增双基准、反向、百分点与缺失日期测试；语法和diff检查通过。实际生成与页面验收结果待下方补记。

- 真实验收完成：运行677a81e2-94d9-4812-82da-2218b708e8e5于09:25:33成功，耗时192.633秒、38,917 tokens，真实streaming链路完成保存5项、6行对标。未再发起模型生成。
- 首稿业务校核发现：模型仍擅自按曝光/点击折算行业点击率，把前后改善当因果证明，并给任意响应/样本时效。已人工逐项修订当前保存的5项诊断，保留真实对标、具体商品标题线索和条件性优化，去除无依据比率/断言/时效；结果附reviewedAt/reviewNote。这是经过校核的模型首稿，不声称原始模型输出完全合格。
- 提示词追加上述实跑失败的明确禁例并发布 #6；应用名称、LLM/End节点名称已统一为运营诊断（#5仅更新名称）。新增确定性校验拦截已观察到的总量折算与因果证明表述，异常保留旧诊断；该校验仅覆盖已知错误，不代表全面语义审核。18项Node测试通过，随后新增双视图切换检查的5项诊断回归测试通过。
- 浏览器：卡片采用“指标对标 / 优化诊断”两视图，避免同时展示拉长总览；原白底、六指标横排和三栏摘要保持。可展开标题/主图诊断阅读具体建议，重启恢复诊断版本2、5项/6行且shouldGenerate=false。细节校核后没有再次付费调用，下一次模型生成仍需关注语义质量。

## 2026-09-08 移除诊断详情标题栏

按用户圈选移除详情顶部“诊断详情 / 编号”和重复主题标题；保留正文，收起按钮移到内容右上角。同步无障碍区域名称，清理废弃样式。JS语法及diff检查通过；未调用模型或改变保存内容。

## 2026-09-08 诊断详情改为弹窗

按用户要求改为原生dialog：移除同页展开和自动滚动，保持白底两栏内容及移动端单列。弹窗隔离背景焦点，锁定背景滚动，支持关闭按钮、Esc和遮罩关闭，关闭后恢复原列表按钮焦点与滚动位置；不调用模型。

- 验证：5项诊断回归测试通过，包含模态打开/Esc关闭；JS语法与diff检查通过。浏览器实测居中遮罩、完整内容和关闭按钮，关闭后焦点回到原诊断按钮、scrollY保持0、背景滚动锁已解除。

## 2026-09-08 合并侧栏经营菜单

按用户要求移除运营诊断侧栏入口和增长运营标题，原增长菜单紧接经营总览并归入经营中心。同步移除已不存在的todoCount DOM写入，避免总览加载报错；保留总览诊断功能与计数。JS语法和diff检查通过。

验证补充：重新启动本地服务后首页返回 HTTP 200；浏览器确认经营中心下依次为经营总览、客户与询盘、商品运营、产品发布、流量分析、关键词与广告、RFQ 商机，经营总览内运营诊断仍正常展示 5 项已保存结果。

## 2026-09-08 顶部经营指标栏改为两排六列
- 按用户选定的局部参考图，仅调整总览顶部指标栏：圆形图标、结果数字层级、轻分隔线与橙色强调；扩展为十二项，保留原诊断与其他布局。
- 使用当前 shop-summary 返回值，新增商机、UV、PV、有效商品与优爆品。缺失字段与真实零值分开，库存取最新快照；比较仍为行业均值，不采用设计稿中的虚构环比。
- 广告花费位置已加入，现有经营汇总无消耗字段且品牌广告不代表全店花费，因此显示未接入，未伪造金额。
- 浏览器在1291px视口实测十二格、六列、两排（顶部位置128/275）；已验证商机与曝光趋势切换、诊断仍显示已保存5项。JS语法与diff检查通过，增加累计/快照/缺失/零值回归验证。

## 2026-09-08 店铺核心词与行业热词对照
- 新增同词/渠道对照表、统一刻度的曝光和点击指数条、三个业务筛选、搜索与排序。保留行业场景独立区，将售卖资源和广告效果历史记录收起。
- 实查WorkCTL search-list schema：cateIdList为array，支持yearImps/yearClk排序；已按当前汇总类目传参。实跑返回100条渠道记录，仍包含跨类目推荐，明确提示来源限制。行业场景返回2026-09-05近90天数据。
- 店铺画像实时调用25秒超时；保留明确日期的9月1日历史样本参考，成功画像另按账号隔离存盘。不以缓存样本声称当前账号已核验。此变更不涉及Prompt和Dify调用。
- 单元及路由回归覆盖渠道错配、未知渠道、热门词来源、指数缺失/零值、<1%原文、排序搜索、空画像、账号隔离及文件权限；总览诊断回归也通过。
- 最终浏览器验收：当前类目推荐词池100条，合并后110条渠道记录；历史样本13个去重词、热门来源10个去重词、重合2词、待核查8词（11条渠道记录）。已实际点击三类筛选，搜索reloj inteligente并按点击指数确认APP632在PC146之前；清空搜索后留在重合词视图。店铺画像25秒回退明确展示历史样本，年度指数及行业场景日期各自可见。

## 2026-09-08 关键词主区改为左右可视化对照
- 按用户反馈移除首屏大表和长段说明：左侧店铺词使用三类信号矩阵，右侧行业热词使用统一0–1000刻度横条，默认点击指数，支持曝光及APP/PC切换。四项词数收成一行；来源、场景与原始资源默认折叠。
- 保留真实口径：店铺无单词流量数值，矩阵仅表达平台列表标签；APP/PC指数独立，缺失不补零也不显示假排名；历史样本日期始终展示。主区只显示有店铺或热门依据的词，计数按词去重。
- 选词双向联动高亮并展示转化参考，保留滚动位置及键盘焦点；筛选搜索无网络或模型调用。未改后端、Prompt或已有诊断。
- 浏览器实测：1291px视口左右各485px、同高同起点、页面无横向溢出；smart watch双侧高亮，APP点击1000、点击率2.40%、商机率3.76%；切PC不借用APP数据。待核查筛选左侧显示无匹配词，搜索reloj得到PC点击146。当前数据13个店铺样本词、10个热门词、2个重合词，主区去重21词。
- JS语法及diff检查通过；关键词5项回归通过，覆盖词归一化、渠道隔离、缺失与真实零值、筛选排序、账号快照边界。

## 2026-09-08 关键词页提高首屏信息密度
- 移除用户圈选的page-intro重复标题区，将更新按钮移到主卡标题栏。压缩指标条、筛选、图表头尾及选词详情，店铺行28px，保留点击和键盘操作。
- 行业热词改为APP/PC双条同屏、同刻度0–1000；切换终端只影响排序与转化详情，不隐藏另一端。新增词群重合环图、店铺信号词数条形图、Top4场景需求与环比。历史样本、指数口径与来源边界保持可见。
- 新增groupKeywordVisualRows去重聚合，跨端标签按词并集判断重合；各端数值保持独立，缺失不补0。摘要统计完整返回词集，不随搜索筛选变化，不生成模型建议。
- 1291×1167浏览器检查：重复标题不存在、无横向溢出、三张摘要图底部约993px，首屏同时看到双栏及摘要；图中21个去重词分为11个仅店铺词、2个重合词、8个仅行业热词；店铺信号7/5/5词。6项回归测试、JS语法与diff检查通过。
- 最终浏览器交互验证：13个店铺词完整落在364px区域（无内部溢出）；PC排序仍保留APP指数，smart watch双侧联动且PC缺项不借用APP；搜索reloj同时显示APP632/PC146，摘要总体仍为21词。已恢复完整APP对照并选中smart watch。

## 2026-09-08 前置行业热词与店铺贡献证据，移除广告资源区
- 合并两条用户要求：把行业需求场景、环比和热门词放在首屏顶部；用绿色/橙色区分已匹配和待核查热词，词按钮联动明细。移除可售关键词资源、广告核验记录的HTML、渲染调用和无用状态，不查询投放效果来填补自然流量。
- 用keywordShopEvidence将高引流/高询盘标签映射为定性正向信号；高P4P只描述消耗。选词详情优先展示店铺曝光/点击缺项与引流/询盘信号，行业指数及转化率放入独立折叠参考，避免误用为店铺贡献。历史样本和使用状态未核实保留明确标记。
- 1291px浏览器验证：场景区位于y181首屏，4场景可见，样本热词匹配2/待核查8，无横向溢出；旧广告区DOM均不存在。点击phone watch显示未匹配、曝光/点击未接入；smart watch显示高引流和高询盘两项绿色证据，行业参考默认收起。
- 7项关键词回归测试通过，包括P4P不算正向贡献、缺项不转成流量增量；JS语法与diff检查通过。未修改Prompt或调用模型。

## 2026-09-08 移除低价值分组与页面重复介绍
- 按用户两条连续反馈，移除关键词页绿色/橙色分组与重合/待核查/特色筛选；全部按钮同步移除，仅保留搜索。场景词改中性色，右侧移除已匹配/未匹配标签和色边，左右词不再显示重合图标。保留实际数据、选词与双端指数。
- 移除流量分析page-intro及其终端选择/查询按钮，流量请求固定全端；保留全局刷新和国家图独立终端筛选。移除商品运营page-intro及样本徽标写入，商品总量仍在指标区。
- 浏览器验证三处旧区块/控件DOM均不存在；流量口径正常显示全端，商品指标364、四象限17/84/7/256正常，页面无横向溢出。关键词7项回归、JS语法和diff检查通过。

## 2026-09-08 RFQ等高、风险明细、团队绩效可视化
- 按8处批注移除RFQ顶部介绍/底部能力说明及风险页重复标题；RFQ主卡桌面760px等高，底部报价历史/权益等高。
- 实查风险汇总totalRiskProdCnt=1、highFreqComplaintProdCnt=1、punishPoint=24；旧版未显示高频投诉及扣分。新增10项点击入口和风险类别条形图，违规记录改为商品卡与纯文本整改说明，移除字段树。
- 实际商品违规接口返回1601210412009（XY13耳机）的美国国别限售记录。风险汇总无productId，不能把它归入高频投诉；点击非零项显示独立近期违规及未关联提示，0项显示明确空状态。平台原HTML以DOMParser转文本，不直接插入可执行标记。
- 团队绩效合为3栏15项指标，总计直接取平台全部账号行；成员回复时长兼容replyAvgTime，金额未知币种时只显示原值，缺失/零值明确区分。点击成员显示三类完整中文指标，保留周期选择。
- 浏览器：RFQ主卡760/760px，底部352.68/352.68px；风险无重复标题/完整字段树，TRO 0与风险总数1明细均可点开；团队三栏15指标、无旧切换按钮，Jerry Yang详情显示5个付款单、20次询盘、449点击、平均回复2.94小时。无横向溢出。
- 9项相关测试通过（2项新验证与7项关键词回归），JS语法与diff检查通过；未改变Prompt、模型调用或平台写操作。
- 补充浏览器验收：10个风险指标均已点击（7项为0、总数/高频投诉/累计扣分3项非零），明细均有反馈；累计扣分24的独立近期记录扣分为0，页面明确不作关联。平台富文本先移除所有标记/资源节点再解码实体，避免执行或装载嵌入内容。

## 2026-09-08 订单物流状态图、合同弹窗与关税结果卡
- 按用户批注移除订单与物流重复介绍；合同、物流各加本页环形图、数量/占比图例与匹配总数。合同表补充商品名与独立币种金额，详情改原生居中弹窗，移除本页完整字段树。
- 真实合同接口返回匹配23条、本页20条，状态5/4/1/8/2；第二页3条，关闭2、待确认收货1，浏览器已验证图表与行数随翻页更新。接口在结束日期9月7日条件下仍返回9月8日记录，保留平台原日期，未擅自改变筛选逻辑。
- 合同弹窗实测166.5 USD、已收0 USD、GV81商品10件；物流弹窗显示阿根廷、运输中、运费696.84 CNY及仓库/单号。1291px下弹窗800px居中，关闭和Esc正常。
- 关税真实测算ET580商品CN至US，平台返回25%、85176200、ByAmount及一般关税[Free]+附加关税[25%]。整理为独立结果卡，缺项不补零，不自行推导应缴税额；切换目的地CA后旧结果已清除验证。
- 浏览器验证2张状态图、无重复标题/原始字段展开区、无横向溢出，选中商品列表收起。新增2项订单业务测试，连同已有相关测试共11项通过；JS语法、diff检查通过。未修改Prompt、调用模型或执行平台写操作。

## 2026-09-08 团队绩效改为三条横向布局
- 按用户反馈，将成交结果、客户响应、商品操作由并排三列改为上下三条通栏区域，桌面内部分别横排3/6/6个指标。平台总计整合至各指标列顶部，与成员图对应，减少重复标题；统一分隔线、色彩、成员行距和响应式换行。
- 保留15项指标、周期选择及成员详情，未修改请求、统计逻辑或金额口径。1291px浏览器实测三个区域同宽990px、同高393.8px、依次纵向排列，无横向溢出；点击Jerry Yang已付款订单仍打开完整绩效，显示5单、449点击、平均回复2.94小时。
- JavaScript语法检查与git diff --check通过；本次为布局调整，通过实际浏览器验收，未新增测试。

## 2026-09-08 团队绩效视觉精修
- 保留三条横向布局，统一标题图标底色、卡片圆角、数字层级、轻分隔线与空数据状态；桌面改为每条共用左侧成员列，右侧指标去除重复姓名，金额仅保留原数值。
- 成员列加入姓名缩写标识，同一成员支持指针/键盘聚焦联动高亮，点击左侧姓名亦可打开原绩效弹窗；窄屏恢复每张图姓名。
- 浏览器发现全局按钮最小高度会造成姓名与图表行错位，已明确设置成员行28px。最终逐项核对三条区域全部9位成员均与图表对齐，15项指标保留、无横向溢出，Jerry Yang弹窗正常打开。
- JS语法与git diff检查通过；仅调整呈现和交互样式，无接口或统计逻辑变更。

## 2026-09-08 经营总览默认显示优化诊断
- overviewDiagnosisView默认改为insights，HTML初始选中态与对标区hidden同步；保留手动查看指标对标，不改变诊断缓存和模型调用。
- 浏览器刷新验证：优化诊断aria-pressed=true，对标区隐藏，已保存的诊断清单直接可见。JS语法与diff检查通过。

## 2026-09-08 总览一排四榜与商品多指标排序
- 用户澄清新增的是商品询盘榜，并将布局从2×2改为一排4个。已形成渠道、国家、商品表现、商品询盘四卡；窄屏在局部横向滚动。
- 商品榜支持7种指标，分页读取364件原始商品数据后本地排序，两榜共享结果；缺失不补零、固定询盘榜仅收正数。当前逐商品询盘均为0，展示无有询盘商品入榜，未把店铺35询盘分配到商品。
- 1291px浏览器实测四卡y相同、各248.8px宽、页面无横向溢出。7种排序逐项通过：曝光最高32、点击7、点击率100%（补充样本点击/曝光）、访客3、TM1；询盘与起草订单目前0。固定询盘榜空状态与364件样本说明正常。
- 新增完整集合TOP5、缺项/0、正数筛选、不改原数组验证；相关5项测试通过，JS语法与diff检查通过。无Prompt或模型变更。

## 2026-09-08 修正商品榜误用默认单日数据
- 用户反馈询盘榜仍为空，已将原因核验转为实际修复：shop-product服务端白名单漏了statDate/statisticsType，前端同样未传，导致默认返回9月6日单日364件商品，询盘全0；先前的空榜判断不代表月度没有询盘。
- 为两商品榜新增共用月份选择，默认顶部开始日期所在月，显式传自然月月首及month；两个榜的标题/页脚同步标注具体月份，切月复用版本检查避免旧响应覆盖。与顶部8月9日至9月7日自定义范围明确分开。
- 通过start.sh重启服务使白名单生效，实际HTTP命令已带--stat-date 2026-08-01 --statistics-type month。浏览器读取846件商品，询盘前五20/9/5/4/3；曝光榜最高877。四卡同排等高、无横向溢出，询盘榜不再空。
- 新增周期参数回归测试，6项相关测试通过，JS/服务端语法及diff检查通过。未修改Prompt或发起平台写操作。

### 2026-09-08 全页面时间输入审计与约束
- 开始：逐页比对全局/局部控件、前端请求、server白名单与当前Workctl 0.1.55动态schema，读取45个schema，补查stars/crowd-insight/messages。按真实params判定，不采用与商品合同不符的通用--time模板。
- 发现：商品页漏传时间；商品week忽略statDate；客户画像仅滚动7/30天；访客超限被静默裁切；行业场景错传90而非90d；国家任意范围混入month；无时间接口显示了全局日期；shop-flow多行30d快照被重复累计。
- 修改：新增前后端共用public/time-policy.js；各页独立日/周/月选择，默认完整周期；支持范围的页面保留受限自定义，订单/违规使用独立日期。输入编辑后应用才查询，非法日期不改已应用状态；后端400拒绝不兼容时间，控制台按日期/枚举输入。
- 数据接线：商品诊断全部分页传日/月；国家和主渠道按day取数；画像固定并注明30d；场景90d；总览渠道改shop-channel日累计，flow来源分类只展示最新同口径快照。
- 实跑异常处理：团队周报返回9/5作为报告标识，未擅自声称是周一起始；合同8月请求返回9月1日记录，增加返回日期核验，移除超范围/未知日期行并显示排除数，保留平台总数只供分页。记录time-policy核验日志，不记录客户正文。
- 页面交互：切页恢复独立时间，阻止迟到响应覆盖新周期；诊断仍每日缓存，日期不同显示手动更新提示；无Prompt修改，无新增模型调用。
- 验证：日期/图表/关键词/运营HTTP/服务端回归32项通过；初版新增合同测试因测试替身漏success:true失败，补齐真实返回契约后通过。真实HTTP验证不支持日期、月初、访客超限、nd错误、90错误、T+2均返回400；8月汇总/地区日记录、商品月/日、行业90d、团队周报、合同已真实读取。浏览器验证日期超限保留原周期、商品月/日、客户/团队独立周期，以及快照页面隐藏顶部时间。服务按start.sh重启至8787。

### 2026-09-08 补齐关键词与广告的真实 HATEOAS 能力
- 开始：用户指出遗漏 searchCampaigns/wholeSiteCampaigns/report/diagnose/accountFinance 等入口，要求全部完善到关键词与广告。上一轮仅查品牌报表与帮助树，范围不足；读取当前插件国际站广告分析 Skill 与报表规则，实跑 Workctl 0.1.55 的 `icbu ads list` 和 schema，确认命令可用。
- 实查：直通车39项（暂停37、结束2）、全站推7项（暂停6、结束1）、普通计划62项（分两页50+12）、省心版计划组1项（已取消）。账户资金返回直通车现金2269.93元、红包0、今日花费0；推荐推广余额0；全站推余额EMPTY。以上是当前快照，不能代替指定周期消耗。
- 报表：8月1—31日及9月1—6日，company_search/company_whole_site 均执行完整 report→report_sql 链，返回花费字段但仅表头无数据。诊断对8月31日结束的7天及9月6日结束的7天分别返回0元摘要；不拿诊断摘要补充空报表。大促行业接口返回智能电子234个样本及比例，推荐接口返回4类方案，2类推荐、2类暂不推荐；均为只读。
- 修改：新增lib/advertising.js和public/advertising.js，服务端固定GET业务路由，0600临时参数文件，65秒查询超时及实体成败日志。嵌套业务失败不作成功；HATEOAS只跟随已核验实体，浏览器不能指定任意SQL/实体或写操作。
- 报表正确性：全局串行完成每个producer/consumer，核对sessionId/tableName/datasource；按真实返回的SELECT语句扩大LIMIT到1000，避免月报被默认10行截断。逐行日期核验、TSV解析、记录完整性检查，区分失败/空记录/真实0/缺字段；两个产品线完整返回金额后才合计总览花费。
- 页面：保留顶部行业场景与左右关键词；下方增加双栏广告效果与可切换指标对比图、四类计划表/状态分布/搜索排序/分页、只读计划详情弹窗、余额与现金红包卡券赔付流水弹窗、近7天平台诊断、行业横条图、推荐方案。没有恢复可售关键词资源和广告核验记录，也没有创建广告或调用mutate。
- 时间与缓存：广告顶部日/周/月/最多100天区间，默认完整上月、最晚昨日；报表/流水按范围，计划余额按当前，诊断独立7天。普通数据5分钟，诊断/方案24小时进程内缓存，单独按钮支持手动更新；并发合并，迟到范围结果被丢弃。原总览诊断每日持久化逻辑未改。
- 状态纠错：普通计划原始onlineStatus=0与直通车列表-2枚举不一致；按同一计划ID联查已核验状态与预算/商品数，仅匹配成功才补齐，未匹配显示未提供说明，不猜测在投。
- 验证：全量49项回归首次发现1条旧测试仍要求默认显示指标对标，与用户已确认的默认诊断冲突；仅更新测试断言并增加切换验证，49项通过。再新增普通计划跨接口状态/预算关联用例，最终50项回归通过；JS语法与diff检查通过。
- 浏览器：1291px下关键词及广告区无横向溢出；直通车39、全站推7、省心版1、普通计划50+12两页均验证；计划弹窗、现金红包流水空状态、推荐4方案已读。切到2026-09-06时效果为单日、诊断为8/31—9/6；输入1/1—9/6超100天被拒绝且保留原结果。start.sh重启后的8787为真实数据验收环境。
- 无本地Prompt修改；只显示平台诊断/推荐返回内容，本轮不调用Dify生成新运营方法或执行投放。

## 2026-09-08 确认 Tauri 插件交付准则

- 用户明确：本地测试可用普通前后端与浏览器，正式 Accio Work 插件必须交付 Tauri 版本；加载插件即自动启动配套后端和 Tauri 窗口。
- 已将准则写入 AGENTS.md 和 CONTEXT.md；保留 Node.js 业务后端，前端框架迁移另行确认。
- 本次仅更新规则与上下文，未实施打包或启动集成；插件自动启动和真实业务验收仍待实现后完成。


## 2026-09-08 React + Tauri + Node.js 首版封装

- 用户确认 React + Tauri + Node.js，并授权通过 computer use 导入 Accio Work 插件。已读取指定 plugin-create Skill、spec.md 和验证器；当前 0.31.1 loader 代码确认插件 stdio MCP 以插件目录为 cwd，sync 时建立连接。
- 新增 React 入口/导航和 Vite 构建，复用已有业务模块；新增 Tauri 单实例窗口、Node 官方运行时、当前账号发现、动态端口/会话 cookie、窗口退出清理、插件 MCP 生命周期入口。移除 server.js 历史账号路径回退。
- 本机 Dify 配置迁至用户私有应用支持目录，未打包密钥；日志仅记录生命周期和原有业务执行状态。没有新增/修改业务 Prompt，因此本轮不改变 SOP 方法映射。
- 已验证 React 构建、50项现有业务回归及3项新增桌面测试；实际当前会话预检成功，Workctl 0.1.55。Tauri 编译与插件安装验收仍在进行，不把源码完成视为交付完成。

### 首版封装最终验收

- 产物：`release/lsou-workbench-1.0.0-macos-arm64.zip`，41,617,519 字节，SHA256 `314fedcc715d984001dfdd6c0b9fa8bb95fbd78c0e78bb3fd2000dd5d4ef7bcc`。React 构建、Tauri release 构建、应用 codesign --verify --deep --strict、用户提供的官方 Plugin Validator 均通过（4 checks / 0 warnings）；最终53项回归全部通过。
- 首次实际导入发现 macOS zip 的中文 .app 目录被 Accio 解码为乱码；包内改用 `LSOUWorkbench.app`，应用显示名保留中文，最终ZIP所有条目路径均为ASCII。又修正 WKWebView 首次导航的 SameSite cookie 行为（Lax + 所有API跨源/Sec-Fetch-Site校验）与后台切页淡入停在透明首帧的问题（桌面关闭淡入，React导航flushSync）。
- 全程通过 computer use 的 Accio“插件 → 添加插件 → 导入插件”完成导入/更新，没有直接篡改 Accio 程序或插件注册状态。当前安装：`~/.accio/accounts/1729094531_212003/plugins/installed/local__lsou-workbench`；版本1.0.0，详情显示“已连接 / stdio / Tools (2)”。本地插件未附全局运营Skill或公共子代理，当前交付侧重桌面工作台，不声称可被所有聊天Agent引用。
- 插件MCP实际拉起 Tauri，后者使用随包Node启动bootstrap及业务后端；最终启动时间19:08，服务动态端口64132。端口每次变化，用户无需填写或访问。不是本地8787浏览器开发服务，也不是手工启动后假称自动启动。
- 退出验收：通过UI关闭前一轮Tauri窗口后，Tauri PID14932、bootstrap PID14938、后端PID14945全部消失；再次导入最终包自动启动了新窗口/后端。父子关系和生命周期日志均与代码设计一致。
- 窗口内实测经营总览真实8月数据（成交9、询盘61、商机302）、商品四象限与分页、产品发布空队列/创建入口、广告查询（直通车39项）、团队绩效。逐页点通流量、客户、订单、风险、装修、素材、知识、权限、RFQ等导航；未执行商品发布、发消息或修改投放，逐页导航不等于全部写链路回归。
- 交付包与安装目录逐文件SHA256比较：36个文件全部一致，ZIP53条目、零非ASCII路径、无环境密钥/运行日志。最终Tauri窗口停留经营总览。
- 交付范围：首版仅macOS Apple Silicon，保留业务兼容模块，React负责入口/完整工作区挂载及导航；未声称全部业务代码重写React。应用为本机ad-hoc签名，未做Apple Developer ID公证，Windows/Intel发行尚未制作。

## 2026-09-08 三平台插件扩展（1.0.1）

- 用户要求 Windows、Intel Mac、M 系列 Mac；Windows 同事不懂开发，测试限定为导入 ZIP → 安装启用 → 查看经营总览及商品运营。
- 保留 React + Tauri + Node.js，共用业务代码；新增三平台资源定位、Windows 私有配置目录、Windows Workctl npm JS 入口调用、Windows Job Object 进程树回收及Tauri 默认静态 VC 运行库（Windows 10/11 的系统 UCRT）。无业务 Prompt 修改，安装说明不涉及 SOP 运营方法。
- 打包脚本按平台下载并校验 Node v22.22.0；官方 plugin-create 验证器原文入库，去掉维护者账号路径依赖；单平台包直接调用随包 Node，通用包通过 Accio 管理的 Node 进行平台分发。通用包缺任何目标或版本不符即失败，不能把 Mac ZIP 重命名成通用版。
- Apple arm64 与 Intel x64 均已编译成功、codesign --verify --deep --strict 通过；file 确认两份 Mach-O 架构不同，Intel 随包 Node 已在本机 Rosetta 下实际运行并输出 x64。Windows 构建正在进行，尚不能视为实机通过。
- 55 项回归通过（含 2 项新增平台/Windows CLI 参数测试）。一次重载编译并行时，原 start.sh 测试的 3 秒超时触发；编译结束后原测试未改阈值，整套 55 项通过。
- 构建机新增 LLVM/LLD；cargo-xwin 使用官方发布的 0.23.1 macOS 二进制并核对 GitHub 发布资产 SHA256。Rust 标准库/部分 crates 官方源慢，镜像下载后分别按 Rust 官方 manifest / Cargo.lock SHA256 校验；没有放宽校验。构建依赖不进入用户 ZIP。

### 1.0.1 三平台构建与本机验收结果

- 三份原生程序全部编译完成：macos-arm64 / macos-x64 为对应 Mach-O，windows-x64 为 PE32+ GUI x86-64；Windows 随包 Node 为 PE32+ x86-64。Windows EXE 与 Node 的导入表不依赖 VCRUNTIME/MSVCP 动态库；应用使用系统 UCRT，WebView2 仍需由目标 Windows 环境提供。链接器只报告缺少 Microsoft 库的调试 PDB，不影响 release EXE 生成。
- 最终通用包 `release/lsou-workbench-1.0.1-universal.zip`：123,682,298 字节，SHA256 `db4019440f7acc85d1fa447ec1fe1c04510548766912568b01ab99f448155e10`，102 个文件，3 份 build.json；ZIP CRC 全通过，无 .env / credentials / gateway-cli.json / 运行日志。官方 Plugin Validator PASS（4 checks、0 warnings）。另生成同版 macos-arm64 / macos-x64 / windows-x64 单平台 ZIP。
- computer use 实际导入通用 ZIP 后发现 Accio 解压丢失 Node 的可执行权限，首次运行 Permission denied；启动器已同时恢复 Tauri 与 Node 的 0755 权限，新增解压权限回归测试。重新生成 ZIP 并经 UI 再次导入修复版，没有手工修改已安装插件。系统保留了首次崩溃的窗口恢复提示，已在本机选择不恢复后重新验证。
- 当前 Accio 插件详情：版本1.0.1、已连接、stdio、Tools(2)。导入自动选择 macos-arm64 启动 Tauri/随包Node后端；经营总览真实成交9、询盘61，商品运营真实四象限与商品列表已检查。
- 点击新版窗口关闭按钮后，Tauri PID27025 / bootstrap PID27090 / server PID27096 全部退出，日志退出码0。随后通过 Accio 插件“重新加载”在20:15重新自动启动，1.4秒后后端就绪；最终窗口停留经营总览（动态端口49897）。
- 102个安装文件与最终通用包 staging 逐文件SHA256一致。最终56项回归全部通过、git diff --check通过；未执行真实商品发布/投放等写入，未提交或推送Git。
- 验收边界：M系列Mac已完成通用包UI安装、自动启动、数据读取、关闭清理及重新加载验收；Intel仅完成构建与本机Rosetta下Node运行验证，Windows仅完成构建/导入表/参数与路径测试，二者仍待真实目标机器验收。Windows同事按包内三步测试和安装Prompt操作，不需安装开发工具。应用尚无正式发布者签名/Apple公证，因此本次称为跨平台测试包，不能称三平台全部实机通过。

## 2026-09-08 全量能力入口与调用适配

- 用户明确授权先加入所有未使用工具，再逐步删减。读取当前活动WorkCTL 0.1.55的105份schema，全部取得；记录缓存stale边界。按现有12个业务页归属，另外总览/团队/控制台可打开全部目录。
- 新增固定能力目录、同源服务端适配、业务功能抽屉、动态参数表单、真实结果引用、预览确认票据、串行执行及账号隔离的任务持久化；重复票据不重复写入，失败/unknown不自动重试。未开放空参数发送消息、用途不明风险接口、两项Token接口及专属Skill接口。
- 现有lib/operations.js仅增加内部任务引用读取，支持扩展功能查询旧诊断优化结果；server.js接入新路由，public/index.html与React入口均加载脚本，CSS独立前缀。新增能力不是原有特定业务页面的全面重写；复杂对象目前采用高级结构化参数表单。
- SOP：读取《国际站运营SOP.md》，业务分组对应产品定位/选品、高质量发品、广告流量、优爆品提升、询盘跟进、店铺诊断。此轮没有新增模型System/User Prompt，平台返回的描述仅作为字段合同说明，不植入未经核验的预算/阈值或效果承诺；3D、Token、拒付等不强套SOP。
- 验证：新增5项隔离HTTP测试通过，覆盖105项目录唯一性、受限工具、确认票据防篡改及重复提交、内部编号引用、跨源拒绝、3D资格前置与凭据脱敏；全套61项通过，React前端构建通过。
- 本地8787真实浏览器验收：各页扩展入口可见；账号与权限→查询成员身份→执行查询，真实WorkCTL返回查询完成并可展开结果。开发验证未提交远端写操作。三平台正式ZIP未重打包，不能把代码入口覆盖率等同于所有工具的业务验收成功率。
- 补充浏览器验收：商品运营18项分组可见，商品交易编辑成功读取真实商品候选（原始编号留在服务端）；计价单位显示业务名称。最终补充参数预览中的对象名称、Host约束及3D关联前置校验后，5项专项测试再次通过。


## 2026-09-09 风险合规工具真实调用审阅

- 按用户要求核验风险8项新增及3项现有命令的schema，使用当前账号商品、真实管控目的国与店铺公司样本实跑。新增8项均有业务响应；现有商品违规V2成功、店铺违规V2上游超时、风险诊断约107秒无结果后终止此次CLI进程。
- 店铺旧违规接口返回40条/37个商品，商品V2提供更完整原因与指引；品牌词库504/417词不能当作店铺命中。供应商召回2个英文同名主体，使用当前公司ID精确匹配后查详情。
- 禁限售分析依次以商品标题、标题加真实属性提交，两次读取返回14/18条且混入其他国家/商品规则，不能直接作为合规结论展示。原始回执暂存本机/tmp/lsou-risk-audit（目录0700、数据文件0600），未入库；摘要补充至已有工具说明文档。
- 本次为业务实跑与文档记录，无界面、业务代码、Prompt或商家资料修改，无提交Git。未执行全店逐商品扫描，未将返回数量称为全店总数。


## 2026-09-09 风险合规能力融入主页面

- 用户授权将全部风险能力放进风险合规。新增页面内违规商品、品牌词、目的国规则、企业核验四区，移除风险页通用抽屉入口；原有风险诊断与按日期违规查询保留。
- 固定业务路由复用WorkCTL执行和脱敏日志，真实对象使用带类型与期限的引用。商品详情的3个查询独立承接失败；品牌词按标题单词边界作候选匹配，规则匹配明确保留适用范围提示，不作法律判定；无新增Prompt。
- 验证：62项测试通过（新增风险路由伪造/错类型对象和固定详情调用检查），React构建通过，diff空白检查通过。8787已重启。浏览器实际展示40条/37商品，商品6图32属性及美国管控与整改指引；旧处罚接口一次超时单独显示。品牌词检查真实504词、当前标题0匹配；完整公司名称搜索返回2个主体，详情接口返回CE/RoHS等资料。网址搜索一次0结果，页面真实显示空态。
- 本地开发验证，不是重新打包或三平台插件验收；没有修改商家商品资料，没有提交Git。


## 2026-09-09 风险合规易用性重排

- 用户反馈界面难看、不知道如何使用。应用frontend-design技能，改为5个任务面板，默认违规处理；移除长页锚点跳转与跨区查找，保留所有已接入能力。
- 违规列表与详情并排；未选商品时明确提示起点，选中行高亮。商品自查呈现“1选择商品→2选择检查项目”，首次进入自动加载目录，未选商品禁用检查动作；公司核验、诊断与历史日期查询各自独立。
- 修复通用ops-form标签flex基准导致纵向表单输入框异常高的问题，限定输入高度44px；窄屏采用上下排列，标签页支持键盘方向键/Home/End。未修改后端、Prompt、平台数据或正式ZIP。
- 浏览器实查：5个任务标签一次仅显示1个面板，真实40条违规列表可见，自查自动读取20件商品，选择商品后可发起品牌词查询；截图确认左右布局及表单高度。语法检查、前端构建及diff检查通过。


## 2026-09-09 风险页标题压缩与工具清单更新

- 按用户标注缩小五个任务面板共用risk-intro：隐藏重复“交易保障”眉题，标题26px降至18px，缩小上下间距和说明字号；移动端标题17px。
- 从未融入清单移出已接入风险工具094—101，待接入总数105→97；同步页面统计、分类总数与阅读口径。文末改为已接入位置与实跑限制，保留原文件名/编号避免已有链接失效。
- 仅样式与文档变更，不修改工具执行逻辑或正式插件包。
- 验证：前端构建、diff检查通过；文档97个工具条目与8项移除编号校验通过。浏览器逐项切换五个面板，标题区均使用紧凑样式，停留历史记录页验收。


## 2026-09-09 RFQ三项工具融入现有页面

- 用户授权接入RFQ三项：商机池勾选→采购需求对比，历史报价单条详情/最多10条对比。同页表格展示数量、需求或价格与交易条款，缺币种明确未返回；移除RFQ通用抽屉入口。
- 实时schema核对3项；批量命令按CLI要求显式--yes用于读取。实跑单条/批量报价及2条商机成功；商机批量首次上游超时，重新查询成功。固定动作与列表对象核对，响应采用业务白名单，保留批量部分失败；未发送报价、未改Prompt或商家数据。
- 文档待融入97→94，3项移至已接入记录。新增测试覆盖批量超限、非列表对象拒绝、逐条失败、币种缺失及联系人/正文不返回。
- 验证：63项测试通过、React构建通过、diff检查通过。浏览器勾选两条商机/两条报价分别返回2/2成功，对比表中价格19.85/18.95、币种未返回、付款T/T和FOB如实显示；文档94条计数核验通过。
- 单条报价页面首次超时、重新查询后1/1成功；截图核验报价表新增选择/操作列无横向裁切，同页详情显示真实数据。最终样式修正后前端构建和diff检查再次通过。

## 2026-09-09 流量分析加入热门商品与供应商

- 开始：核对两项WorkCTL实时schema与真实商品目录；保持当前React/Node/Tauri结构，复用现有能力服务。
- 实现：在流量分析搜索需求对照下方加入紧凑页签与真实业务字段；按类目/周期/排序查热门商品、按类目或店铺网址查供应商、商品一键联动供应商、每日指数展开。设置最后请求版本保护，未知数据格式/空结果/调用失败分别处理。沿用能力服务的开始执行链路与完成/异常运行日志。
- 实测：智能手表30d热门商品10条；供应商类目查询（省略country及TOTAL）均成功但data=null；以商品返回的ztx-tech店铺域名查询取得1条公司数据。没有把空结果伪装成供应商列表；指数不累计为询盘/订单/成交额，未提供币种不猜测美元。
- 验证：65项Node测试通过，新增真实引用/错误周期/非法网址拒绝、全店查询不夹带类目、指数零值与字段隐私、空供应商结果测试；React构建通过。浏览器验证热门商品10条→查看供应商→对应公司1条，并检查页面布局。未重新打包正式插件ZIP。
- 文档：移除待融入目录084/085，加入实际入口与数据限制说明；剩余92项（42查询）。

## 2026-09-09 新增市场洞察导航

- 开始：用户确认把行业供需与竞争情况、热门商品与供应商移入独立侧栏，名称市场洞察，位于流量分析下方。
- 完成：移动两个现有工作区到tab-market，挂接market加载器与行业查询安装器；保留原服务接口、商品选择和结果状态。流量页继续保留本店/国家供需/需求场景/细分类目对照。页面刷新会重新查询市场选品，周期提示改为各区域独立。
- 验证：React构建通过，65项测试通过；浏览器进入市场洞察取得10条热门商品，展开行业研究商品选择取得真实目录，切换到流量分析确认两大工作区已移除、行业对照仍保留，再返回市场洞察交互正常。无新增异常；未重打包正式插件ZIP。
- 同步CONTEXT及原工具说明文档入口，待融入数保持92项。

## 2026-09-09 市场研究结果视觉整理

- 开始：用户反馈五项行业查询的“记录1/字段名”结果难看且难读；使用frontend-design指导，保持已有页面内展示。
- 核验：真实查询供需1组、趋势24点、卖家画像1组、关联商品10件，买家偏好返回[{}]。趋势没有日期，卖家收款分布没有统计周期。
- 完成：新增市场专用结果渲染，供需四指标及百分比格式、四张独立刻度趋势图和可展开数值明细、星级及收款规模占比条、买家偏好明确空状态、带图片及价格/起订量/浏览和商机人数的商品列表。排名口径不明不作结论，不展示内部字段名；没有币种不猜测美元。选择商品后自动收起并显示类目摘要；切换研究项高亮，选择变更使旧请求失效。
- 验证：65项测试通过、React构建通过、diff空白检查通过；浏览器逐项执行查询核验指标百分比、趋势图、同行占比、偏好空状态与商品列表。没有重打包正式插件。

## 2026-09-09 市场研究改为商品驱动入口

- 开始：用户要求默认展示几个商品，明确选中商品和行业查询的关系。
- 完成：首屏加载6件真实商品，默认选择首件并读取供需；选中态、完整商品摘要、所属类目和三个带用途解释的“查看…”按钮形成连续流程。更换商品自动重查当前视图；搜索与翻页保留，目录加载版本及查询版本避免旧数据覆盖。独立买家研究与类目查询分区，空目录/失败有说明和搜索重试。
- 验证：浏览器首次进入无需点击即取得6件商品及智能手表供需；点击第4件后类目变为其他手机配件，查看行业趋势取得不同的24点数据。65项测试和React构建通过。正式插件ZIP未更新。

## 2026-09-09 商品列表与完整市场图表左右布局

- 开始：用户要求点击左侧商品，即在右侧完整展示行业数据及下方买家研究。
- 完成：左侧20件/页可滚动目录、搜索与分页，右侧供需指标、趋势图、同行卖家分布、买家偏好、关联商品五区常驻。初始自动加载，切换商品并行更新三个类目区域；各区失败独立显示并可重新读取。买家研究保留全行业/本店范围说明，避免暗示与所选商品绑定。保留移动端上下排列。
- 验证：React构建、65项测试通过；浏览器确认初始五区均完成读取（偏好为空），真实商品切换更新行业类目。正式插件ZIP未重打包。

## 2026-09-09 市场左栏空白与供需密度修正

- 定位：ops-market继承overflow:hidden成为sticky的滚动祖先，左栏相对布局顶部下移65px。
- 修复：仅该容器改为overflow:visible，保留商品列表自身滚动；供需区隐藏重复标题，缩短时间/指标/说明间距。
- 验证：浏览器测量左栏与右侧布局顶部差值由约65px变为0，供需区高度219px；真实数据仍展示。React构建与diff检查通过。本次仅CSS调整，未重跑业务测试或重打包正式插件。

## 2026-09-09 市场首屏密度与四图横排

- 按反馈去掉市场宿主与卡片顶部额外margin，压缩标题、商品摘要、供需指标和说明的间距。
- 桌面行业趋势改为四列横排，SVG改为260×160的紧凑坐标系，保留各图独立刻度、序号轴和悬停数值；窄屏回退两列/单列。
- JavaScript语法、React构建与diff检查通过；只调整呈现，不改查询与数据计算。
- 浏览器实测1291px宽首屏四图同一行，图表底部约696px，小于1169px视口；供需区压至180px。调整小图坐标文字字号以保持可读。

## 2026-09-09 关键词与广告五项工具核验与资料区接入

- 开始：用户询问五项工具用途并要求加入界面。核对当前目录及实时schema。
- 实测：站内行为返回[]；行业分布10条路径；当前广告公司身份查询主营二级类目，匹配智能电子；用真实商品ID查询广告资料返回标题。首次CLI调用缺少参数文件/误用schema旗标产生挂起，已终止本次进程，改用schema子命令及JSON文件真实核验。
- 实现：资料区三个状态独立加载，商品列表可搜索并逐个查广告标题，旧请求不能覆盖新选择；公司ID不进入浏览器，商品只能真实引用。未核验行为记录不泄露明细，未返回金额/效果不推断。品牌资源search-list返回系统繁忙，update-reserve资源列表元素未声明，保留状态说明并不开放提交；本次未实际预订。
- 验证：67项测试通过，新增账号身份/非法对象/禁止任意预订/标题白名单/未知行为结构测试；React构建、语法、diff检查通过；浏览器读取主营、行业路径、行为空状态及商品目录。文档移出086—089，剩余88项；090仍待融入。未更新正式插件包。

## 2026-09-09 关键词与广告页内双视图

- 页面顶部增加查看关键词/查看广告，默认关键词；关键词保留核心词与市场需求，广告视图包含选品资料、品牌预订状态及广告经营区。
- 切换仅改变可见性，不销毁查询结果和表单。总览花费入口自动切换广告视图后定位广告效果，避免跳转到隐藏内容。
- 浏览器检查默认关键词、切广告、切回均正常，截图确认按钮位于页面内容顶部；JavaScript语法、React构建、diff检查通过。正式插件ZIP未重打包。

## 2026-09-09 广告基础资料密集排版

- 用户明确保留内容并提高密度：更名店铺基础资料，说明只表达主营、类目覆盖和广告侧标题。
- 主营/行为空状态合为紧凑摘要行；类目路径用双列网格与小标签；商品搜索压为一行，目录限高165px，与资料并排。广告返回标题不重复抄写所选标题，以一致性提示替代。
- 语法、React构建、diff检查通过；不改变查询、权限或投放操作。

## 2026-09-09 广告基础资料配色与搜索用途澄清

- 保持紧凑排版，标题与主营类目使用暖橙强调，类目分组使用橙/青/蓝标签，商品选中增加高对比边框与底色；颜色仅区分类目与交互状态。
- 搜索更名“核对商品标题”，就地说明选中商品后查询广告平台标题，不提供商品投放状态或效果；空态与详情标题同步调整。
- JavaScript语法检查、React构建和git diff --check通过。浏览器仍显示刷新前内容，本轮未完成刷新后的视觉验收。未重打包正式插件。

## 2026-09-09 移除广告商品标题核对

- 按用户要求移除整个商品搜索、选择和广告标题核对区域，清理专用样式、事件及自动查询逻辑，保留主营和类目展示。
- 未接入工具文档恢复088广告商品详情，说明实测仅返回标题，缺少投放状态和效果，独立核对标题的运营价值有限，因此主动移除主界面入口；后台能力保留。剩余清单同步为89项（39项查询）。
- JavaScript语法、前端构建与git diff --check通过，未重打包正式插件。

## 2026-09-09 移除品牌关键词预订占位

- 按用户要求移除广告页品牌关键词预订说明卡片，没有绑定查询或提交事件需要清理。
- 未接入工具清单090补充原因：此前资源查询系统繁忙，缺少可售关键词、资源标识和预订条件，resourceList元素结构未明确，暂不能形成可靠业务流程。同步CONTEXT及清单末尾状态；剩余89项不变。
- 前端构建与git diff --check通过；未执行预订，未重打包正式插件。

## 2026-09-09 全面移除通用扩展功能入口卡片

- 按用户要求移除所有页面的“更多业务功能 / 跨模块任务与能力”入口，含经营总览和数据控制台。删除自动安装卡片及目录请求逻辑，React和兼容HTML入口不再加载通用能力抽屉脚本，清理卡片样式。
- 后台能力、历史抽屉代码与已融入页面的业务功能保留；未接入工具文档说明通用入口已撤下，后续按实际用途整合，剩余89项不变。同步CONTEXT。
- JS语法、前端构建、diff检查通过；本地HTTP页面确认不加载扩展脚本，前端源文件确认无cap-launch残留。未重打包正式插件。

## 2026-09-09 经营查询缓存、合并请求与商品并发收敛

- 起因：日志60条中59条与商品查询有关，30次实际调用5次上游连接超时；总览原按4路分页，后台未合并相同在途请求。该证据表明上游不稳定且前端存在请求放大，不推定并发是唯一根因。
- 新增QueryCache用于普通经营端点，成功查询保留至主动刷新，合并相同参数请求，商品最多2路并发；刷新世代隔离旧响应。广告查询与账号工作台快照取消五分钟自动过期。顶部刷新先使查询缓存失效再重载当前页，保留任务、图片库和发布引用；按日期切换复用对应缓存。缓存为进程内，不跨服务重启。
- 总览商品分页降为2路并发，失败统一提示一次；界面显示业务化错误，原始错误仍记录在/api/log。新增单测覆盖重复合并、强制更新、失败不覆盖成功快照、刷新竞态及并发上限。
- 70项测试、前端构建、语法与diff检查通过。本地服务重启后实测：两次并发同参返回相同fetchedAt(09:15:16.232Z)，再次查询cached=true；POST主动刷新后取得09:15:18.970Z的新结果，再读cached=true。正式插件未重打包。

## 2026-09-09 全页面只读缓存补齐

- 按用户要求，将运营READS查询、风险/RFQ/广告辅助/热门商品等能力查询和工作台底层查询补齐为缓存优先，复用QueryCache合并在途请求；缓存业务原始数据，返回时重新登记限时引用，避免过期引用被永久保存。
- 顶部主动刷新及清缓存联动清理新增缓存。市场图表重新读取、广告资料更新支持强制更新；消息查询固定本次快照的上界，主动刷新后更新。任务轮询、风险分析生成、模型状态、写前校验与部分成功结果不进入永久查询缓存。
- 本次为进程内账号范围缓存；服务重启仍重新查询，发布参数与任务有效性规则保留。71项测试、前端构建和语法检查通过，本地服务已重启。
- 真实广告辅助只读接口验证：首次约1.54秒，再次读取约1毫秒；强制更新约1.49秒，随后再次命中缓存约1毫秒。

## 2026-09-09 修复macOS11系统门槛并构建M芯片候选包

- 核对旧包明确最低macOS12；参照Tauri官方macOS bundle文档（https://v2.tauri.app/distribute/macos-application-bundle/），修改目标并重编译，不修改旧产物。Node22.22.0官方arm64二进制otool显示minos11.0。
- 应用/插件版本升1.0.2，显式设置macOS部署目标11.0，加入可测试系统检查，明确MCP ready仅代表后端就绪，替换旧WebKit不支持的Array.at。
- 本机72项测试通过；新Tauri主程序与Node的LC_BUILD_VERSION和Info.plist均为11.0；签名验证和官方插件验证器4项通过。仅构建M芯片版本，不覆盖旧三平台正式包。
- 产物：release/lsou-workbench-1.0.2-macos-arm64.zip，SHA256 47ebde320ce318554839fcc6892ba3622f0a181347dcc9e5b244a42a5be7e9f0。目标macOS11.5.2尚未实机验收，不能承诺全部WebKit功能已兼容。
- 使用打包目录内Node及MCP入口本机启动，状态从starting转为ready，关闭MCP后退出；本机Darwin25.6.0，此结果不替代macOS11.5.2验收。

## 2026-09-09 按当前最新内容重新封装M芯片版

- 按用户要求从当前未提交工作区重新执行build:desktop与package:plugin，目标仅macos-arm64、版本1.0.2，保留macOS11.0部署目标。未构建Intel/Windows，不称为通用包。
- 72项测试与git diff --check通过；Node官方校验、原生签名验证、插件结构校验4项通过。ZIP内30个业务/前端文件逐个SHA256对照当前server/lib/desktop/demo-data/public及dist一致。
- 产物release/lsou-workbench-1.0.2-macos-arm64.zip，40.3MiB，SHA256 3e2c4e6cee5b9962835698d2a4673967e58d9ba1750baed19efbbd48b72d3ac5；覆盖之前同名M芯片构建，本条为最新校验值。macOS11.5.2仍待同事实机验收。
- 使用本次打包目录的Node/MCP入口完成本机启动测试：starting→ready，关闭后正常退出；不等于同事macOS11.5.2实机验证。

## 2026-09-09 打包1.0.2 Intel Mac、Windows与通用版

- 按当前源码顺序构建macos-x64/windows-x64，共享payload/runtime准备目录不并发写入。Windows首次调用cargo-xwin等待下载，停止该构建进程后指定已准备的XWIN_CACHE_DIR=.build-cache/xwin重新构建成功；工具PATH使用现有LLVM/LLD。只出现第三方库缺调试PDB的LNK4099警告。
- Intel主程序Mach-O x86_64、随包Node在本机Rosetta运行返回x64；Windows应用GUI和Node均PE32+ x86-64。72项测试通过，Mac签名验证、官方插件结构检查、ZIP CRC及各平台30个源码/dist文件逐字节一致性检查通过。Intel/Windows尚待目标实机验收。
- 更新安装说明：来搜窗口最低11.0并不代表WorkCTL0.1.55兼容11.5.2，当前Mac整体按12+使用并实测。不变更绑定/凭据，不回退CLI。
- release/lsou-workbench-1.0.2-macos-x64.zip，41.7MiB，SHA256 28fb438e4633efa418299caedc8b84968f370d820b501f4078c4fb00fe8d0a98。
- release/lsou-workbench-1.0.2-windows-x64.zip，36.2MiB，SHA256 6bd61d454bf2c056795ea67a79bb224bfe14dff074293b0a9b8d417452f862f3。
- release/lsou-workbench-1.0.2-universal.zip，118.2MiB，SHA256 8f55130a84f1a58d4425b273a39bb1adc051b854894fe5e0dc0ffe8920758d5c，包含M芯片Mac、Intel Mac、Windows x64三份同版构建。

## 2026-09-10 取消三平台通用版

- 按用户明确要求删除release中所有universal ZIP、sha256和stage-universal，只保留分平台交付。移除npm通用打包入口，脚本拒绝--universal，更新AGENTS/README/CONTEXT与包内说明。
- 使用既有1.0.2原生构建重新封装Mac（M芯片）、Intel Mac、Windows三个独立包，业务代码未变；历史通用包记录保留为历史证据，不再提供下载。
- 三包官方结构校验、签名检查（Mac）、ZIP CRC、单平台内容及新版说明检查通过。更新校验值如下：
- lsou-workbench-1.0.2-macos-arm64.zip SHA256 7b9cb7fe94468d16d849213740f800c012eb11158428e7219f4bdff78d3309cf
- lsou-workbench-1.0.2-macos-x64.zip SHA256 52b28a2cb2b60f19b932dabaa322b71459a4b7a9dcc1cbab1104ff8e2f053d5e
- lsou-workbench-1.0.2-windows-x64.zip SHA256 74ef6efb44835ff595c5b23c6a8d12ee29ee0212e632d504bd05891a31e8aa30

## 2026-09-10 六页 Workflow / Chatflow 本地接入，按用户要求暂停云端接通

- 本地候选：两份既有 DSL 分离结构化分析与自然语言问答；新增六页共用顾问卡、聊天面板、可信证据入口及后端上下文/生成/流式问答接口。保留旧总览接口，启用新顾问时停止旧自动模型调用。账号、页面、日期、快照和会话分别绑定，生成失败保留同范围旧结果；模型状态不能表示已执行。
- Prompt 已先参考国际站运营 SOP：总览映射店铺诊断与规划；客户映射询盘质量跟进；商品映射选品定品、高质量发品、关键词和优爆品提升；流量映射国家偏好、关键词与场景流量复盘；市场映射市场/产品定位和供需竞争；广告映射关键词库与广告报表复盘。各方法均写明目标、输入、判断、动作与成果，缺失广告成本、实际图片、对话关联、供货能力及现行阈值时不编造结论。
- 已执行验证：原72项Node回归通过；新增8项AI服务测试通过；11项DSL代码节点测试通过；两份DSL严格validate/checklist通过；React构建通过。浏览器实际总览数据读取成功，隔离模拟Dify响应完成过JSON解读展示；六页完整交互及真实Dify模型调用尚未验收。模拟输出显式标记联调样例，不是实际经营判断。
- 云端尝试：difyctl列表/导出/一次Workflow导入请求均HTTP500；浏览器导出已有Workflow用于核对模型，文件上传未成功；未执行发布。模型候选沿用已有deepseek-v4-flash，不能证明可运行。
- 用户随后明确：Flow需由其分别优化并选择可用模型，等其发App API Key再接。停止进一步云端操作和模型调整；停止临时8797/8798模拟联调，保留本地代码进度。未重建正式Tauri插件包，未提交Git。

## 2026-09-10 1.0.3 启动通用性与内置排障（实现）

- 现场复现旧 runtime 在 versions/0.1.58 路径报 ENOENT；Accio 0.32.2 日志证实15:22成功安装到 generations，实际0.1.58 health/schema均success。根因是来搜依赖发现耦合旧目录，不是机器芯片或未登录。
- 改为当前空间安装记录优先，兼容注入入口/generations/versions，严格约束当前版本、空间与真实路径；Windows继续通过随包Node执行包声明的JS入口。开发入口共用发现逻辑。
- 增加安全错误分类、阶段日志、启动摘要、临时未就绪有限重试、后端等待超时；MCP增加诊断/恢复工具和独立 --diagnose，恢复合并并发且保留ready窗口，只操作本插件的权限及自己创建的失败窗口。
- 新增启动排障Skill；先读取SOP全文相关“店铺诊断/核心产品数据跟踪”部分用于恢复后验收。SOP不包含CLI/桌面排障，故输入为实时依赖检查、判断为故障阶段、动作为有限恢复、输出为原因/已执行动作/窗口与数据验收状态；不引入经营阈值或经营结论。
- 按测试优先增加新版布局、陈旧记录、跨账号/符号链接拒绝、安装中/禁用、Windows新布局、诊断脱敏、无MCP诊断、恢复失败窗口与保留ready窗口用例；新增缺失行为先失败，修复后15项桌面测试通过。本机新解析器真实诊断通过，source=registry、Workctl0.1.58。完整回归及交付验收另记。

## 2026-09-10 1.0.3 构建与当前验收结果

- 完整Node回归89项通过；Skill frontmatter校验通过；三个独立ZIP官方结构校验5项、CRC与单平台内容校验通过，Mac签名验证通过。Mac M芯片/Intel和Windows x64原生构建完成；Windows使用已有.build-cache/cargo-xwin及LLVM/LLD、缓存的Windows SDK，只有第三方PDB缺失的LNK4099警告。
- M芯片包使用随包Node执行 --diagnose，真实返回environmentReady=true，当前Workctl0.1.58由registry发现，认证与经营schema均通过；没有使用全局旧CLI。
- 用户接手导入。本机第一次实际导入的是macos-x64包，而电脑为arm64，最外层启动器找不到当前平台程序，Accio显示stdio process exited with code 1。已根据安装目录核对并向用户提供macos-arm64包；新增PACKAGE_WRONG_PLATFORM/PACKAGE_INCOMPLETE分类，在随包Node不能启动时也能由shell报告明确原因，补回归后重新封装三包。此时仍待用户换成M芯片包后的插件加载、窗口和数据验收，不能用静态测试或独立诊断代替。
- 独立MCP恢复测试使用模拟原生程序验证了失败→重启→ready、重复恢复保留ready窗口；fixture日志已隔离。测试期间早于08:59Z的plugin.log中部分失败/ready为测试痕迹，不能视为真实Tauri业务验收。
- 最终候选ZIP校验值（旧1.0.2包保留，未发布远端/未提交Git）：
  - macos-arm64: d9ff6071d96d7b8831d740af1299bd090db942099def77cbf0a90de4f0db4419
  - macos-x64: 9ca74ff275bdc7bf8043016bcfbcc0a7f9d98c1cd6e00688552aa1c17e719a87
  - windows-x64: 5808407dc7fb102c9e31acb7a9c93b5e2eac23a959649448b871cb7580fb6d2a

## 2026-09-10 1.0.3 本机 M 芯片正式插件验收完成

- 用户重新导入macos-arm64包后，17:01:51由Accio MCP自动启动Tauri，17:01:52新解析器通过当前会话/插件/Workctl0.1.58 registry入口/认证/schema检查，自动启动随包Node业务后端并进入ready。已完成安装界面最后的“完成”步骤。
- Accio插件详情实见：版本1.0.3、已连接、stdio、Tools(4)、Skill 1（lsou-startup-recovery）及独立诊断CLI。安装目录53个文件与最终M芯片ZIP逐文件SHA256完全一致。
- Tauri真实页面验收：经营总览2026-08显示曝光12.60万、询盘61等实际接口数据；商品运营完整分析846件，四象限与真实商品表格成功显示。没有发布商品、修改广告或触发AI模型调用。
- 已安装包独立 --diagnose 再次通过，environmentReady=true，最新startup摘要ready且原生进程仍存活。独立诊断的desktop字段描述该诊断进程自身（没有创建窗口）；当前正式窗口状态同时以MCP与UI核对，不能单独用该字段判断全局窗口已关闭。
- 业务限制另记：总览广告报表读取失败，星等级显示新版Workctl命令未注册/unknown flag；这与本次启动路径故障分开，不宣称全部业务接口已验收。本轮未扩大修改这些业务模块。Intel Mac和Windows x64完成构建与包校验，尚未目标系统实机验收。

## 2026-09-10 两个本地 Flow 的八份顾问 Prompt 与知识库链路

- 开始：用户明确恢复“先填补进去”，要求参考 `/Users/garden/YD/l-sou` 的既有 Flow 与 knowledge base。工作范围为两份本地 YAML、同步 System Prompt、相关测试和现有维护文档；此前暂停的页面联调、云端导入、模型选择与 API Key 接入继续暂停。编辑前备份相关文件，保留工作区其他任务改动。
- 先读八份运营顾问 Prompt、项目 `docs/references/国际站运营SOP.md` 全文，并只读参考 `来搜运营顾问首次诊断.workflow.yaml`、`来搜八板块运营顾问.chatflow.yaml` 及两份知识资料的版本/适用范围说明。参考 Flow 中的知识库 ID 为空，模型是参考模板值；只复用问题改写、保底查询、知识检索和上下文消费方式，没有复制其模型选型或修改源项目文件。
- SOP 映射：店铺诊断与运营规划对应总览的当前目标/同比口径、关键差距、三项优先事项与复查；市场/产品定位对应市场与流量的国家/类目需求、供应能力、竞争性解释和验证方向；关键词库、高质量发品、选品定品及优爆品提升对应商品与广告的对象参数、词路/素材/投放观察、修补动作及同口径验收；广告与场景流量对应渠道/计划/商品/国家/词的逐层检查；询盘质量跟进对应客户需求、已有记录、阶段、澄清问题和回复草稿。现行阈值、广告归因、真实图片、完整对话、预算、人手、供货与成交关联不足时保留缺项，不将 SOP 经验变成店铺事实或收益承诺。
- 业务方法写入两个 `prepare` 节点的 `PROFILES` 与 `PROFILE_MAP`。总览 01/07/02，客户 08，商品 06/04，流量 07/03/05，市场 03/04，广告 05/04；客户方法删除 RFQ 专属角色/输入/步骤。规划类追问按需加入 02。原 Prompt 的交付表格仅作业务参考，Workflow 仍严格输出既有 JSON，Chatflow 直接回答并在用户需要时产出完整文案，不强制所有问题生成报告或声称调用其他顾问。
- 新增 `retrieval_input`、`query_rewrite`、`query_guard`、`knowledge` 四个节点；Workflow 共 11 节点，Chatflow 共 10 节点。材料保留本轮问题、页面主题、最多六条事实摘要，优先选中对象，仅接收同模块同快照的分析摘要；过长摘要有界，检索材料省略联系方式与内部引用。改写失败使用本轮保底查询，不自动重试；可选分析的主题列表形状异常时忽略该列表，问答继续使用当前事实。
- 知识节点为多库语义检索 Top K 6，无重排模型；主节点启用原生 context 与知识引用。Prompt 将知识方法、当前事实、假设和建议分开，保留《经营方法论第二版》/《运营 SOP》的版本及待核标记；知识片段 ID 不能写入业务证据字段。无命中可给一般建议，服务异常由 Dify 报错，不静默伪装成未命中。节点日志仅记录模块、摘要条数、是否有同快照分析和查询来源，不主动记录原始材料。
- 模型与契约：原主节点 `deepseek-v4-flash` 的模型对象、依赖、输入和 Workflow 输出 Schema 均保持不变；新增改写节点使用同一配置，不代表用户已确认可用。修正原 retry 布尔字段名为参考 DSL 的 `retry_enabled`，保持禁用语义。同步独立 System Prompt，保留 `overview-todo.output-schema.json` 原字节与既有结果校验代码。
- 本地验证发现并修复 Workflow 检索准备入口的参数声明不一致；改为两个 Flow 各自的明确入口参数。新增用例覆盖按页/按问题方法选择、当前选中商品优先、旧快照隔离、畸形可选摘要、检索摘要长度/联系方式省略、改写失败回退、知识片段冒充业务证据拒绝、Dify 参数与图连线。最终 `python3 test/todo_chatflow_test.py -v` 19/19 通过，`node --test test/ai-advisor.test.js` 8/8 通过；所有代码节点编译通过，Prompt 同步与契约不变检查通过。
- 结束状态：两份 `dify-workflow validate --strict` 与 checklist 当前仅报知识库未绑定（strict 同时给出空 dataset_ids 警告）。没有为了通过检查填假 ID，待用户绑定真实知识库后重新校验。测试使用合成数据与替身，未调用真实模型/检索、未连接 Dify 控制台或 difyctl、未导入发布、未接入新 API Key、未修改页面/后端或重打插件包，未提交 Git。用户仍需在每份 Flow 的两个 LLM 节点选择可用模型，并绑定自己的知识库后实测。

### 用户确认结束节点输出命名

- 用户反馈已手动将 Workflow 的数据不足结束输出改为 `error_result / error_raw_result`。读取本地 YAML 时仍为旧名，因此只同步 `no_data` 结束节点的两个公开参数名，正常结束保留 `result / raw_result`；节点内部引用、模型、Prompt 与知识库配置未变。
- 数据不足内容仍是 `needs_data`，不把它解释成真实模型服务异常。README/CONTEXT记录后续接入应分别解析两组字段；遵守暂停接入约定，本次未修改前后端或操作云端。
- 验证：19 项 DSL 代码测试通过；实际执行准备节点并解析结束节点引用，确认四个结束输出名唯一，`error_result` 与 `error_raw_result` 对象/JSON一致。严格校验仍只报本地知识库未绑定及对应警告。未提交 Git。

## 2026-09-10 六页统一 Output Schema 与 Workflow 字段适配

- 用户恢复授权：复用已有 Workflow Key 自行接入/测试，随后要求优先给出六页共用 Output Schema，供其手动配置 Dify Structured Output。保持现有 React/Node.js 栈，本轮不替用户更换模型、调整云端知识库或导入发布。
- App API 实时读取 `/info`、`/parameters` 均200，现有 Key 对应“来搜六页运营分析”，输入为必填 `business_context`，最大50000字符。真实空数据运行经过准备/判断/数据不足结束节点，返回 `error_result / error_raw_result`，status为needs_data，任务为空，模型Token为0；该真实回包通过本地输出适配。Key仅后端读取，没有输出或写入源码/浏览器。
- Schema在既有 `dify-chatflows/overview-todo.output-schema.json` 定为六页统一V2.0：保留summary/tasks/suggested_questions及每项诊断的既有字段、类型、必填与边界，补齐中文业务定义、可空列表/对象引用和来源规则；同步本地Workflow模型节点。系统版本、模块、快照、时间、状态、真实来源/入口与建议状态继续由代码生成，两个结束分支参数不放进模型Schema。README新增字段表、复制位置、版本演进与同步修改约定。
- Schema中的业务说明依据此前已读的国际站运营SOP：店铺诊断与定位对应summary/basis，关键词/高质量发品/广告与场景流量/优爆品/询盘跟进对应具体steps，效果跟踪对应acceptance_criteria；未核实原因用hypotheses，缺项用missing_data，真实事实引用与知识方法分开。SOP不定义JSON版本规则，该部分为程序契约，不强套经营章节；不添加平台门槛或效果承诺。
- `lib/ai-advisor.js` 允许专用分析Key缺省时按用户要求复用旧DIFY_API_KEY；Chatflow保持独立配置。增加正常/数据不足结束解析，支持未执行分支null或空字符串；冲突分支、损坏JSON、对象与文本不一致及错误分支状态不匹配直接拒绝，保留同范围旧结果。
- 对齐action_ids语义：与DSL使用相同的open_page及商品引用哈希规则，重新构造可信入口，保留模型选择的有效列表；空列表不自动生成按钮，拒绝不存在、重复或跨对象入口。页面补优先级显示，Chatflow未配置时隐藏问答与追问按钮，不显示可点击但不能工作的入口。
- 验证：100项Node回归全部通过，20项DSL代码测试通过；标准JSON Schema验证正常/空任务样例及必填缺失/null/额外字段拒绝，通过六页“Node快照→真实Python准备/结果校验节点→Node结果校验”的合成数据往返。新增独立Schema/模型节点相等检查和后端字段长度/数量边界回归，减少后续配置漂移。语法与diff检查通过。
- 已按原start.sh重启本项目8787开发服务；真实配置返回analysisConfigured=true、chatConfigured=false，浏览器“生成解读”可用。本轮未重打正式插件、未提交Git。合成字段测试不等于六页真实模型业务验收。
- 浏览器总览读取18项当前数据依据后，显式发起一次真实分析。App API事件快照显示输入校验、检索词改写成功，知识库检索阶段耗时较长；页面在原360秒上限先显示“AI处理超时，请手动重试”。随后只读查询同一运行`e183aaf7-fdb0-497a-90f6-d05b781c5fac`，实际已succeeded，10步、361.517345秒、30811Token，正常返回result/raw_result和5条诊断。用运行中原始business_context与实际回包执行当前validateAnalysis，版本、范围、完整字段及引用校验全部通过，未重新发起模型调用。
- 据此把Workflow等待上限从360秒调整为600秒，Chatflow上限保持原值，不增加自动重试；补跑14项AI服务测试与语法检查通过后重启生效。真实Workflow结果通过字段适配，但本次页面只验收到超时反馈，未重复付费调用验证新的完整等待流程；六页合成字段通过也不代表其余五页真实分析已验收。

## 2026-09-10 Workflow重新发布后的真实验收

- 用户告知已重新发布后，使用既有App API Key和8787现有页面核验。`/info`返回200，当前应用名为“来搜AW Workflow”、模式workflow；`/parameters`仍为必填business_context、最大50000字符。配置接口analysisConfigured=true、chatConfigured=false，未变更凭据或云端配置。
- 本轮在总览页显式点击生成，并另用空快照测试数据不足分支。读取到发布后两次正常运行记录：`a6fee86e-b512-41dc-81d1-9c090e81cb68`耗时31.88201秒、`8f404012-99bc-4e90-8a0a-d12a59057fdc`耗时27.40078秒，均succeeded、10步、正常result/raw_result、4条诊断；空数据运行`b88a4bb6-526e-4084-b0bd-70f4c6068d22`耗时0.999781秒、4步、0模型Token，返回error_result/error_raw_result与needs_data。通过当前本地接口的空分支总耗时3.713秒。
- 三份真实回包与各自原始business_context通过当前validateAnalysis；业务字段也通过公开V2.0 JSON Schema验证。总览结果status=partial，表示输入样本存在裁剪，不代表Workflow失败；版本、快照、时间、优先级、证据引用及安全入口均可适配。
- 浏览器已显示整页摘要、4条诊断与“更新解读”；展开验证依据、待验证原因、建议步骤、复查标准、缺项和导航入口。点击“经营指标与同行对标”成功打开实际指标、来源、时间与独立统计口径，关闭弹窗后展开“数据范围”正常。未触发商品、广告或客户写操作；其余五页和Chatflow没有在本轮做真实模型验收。
- 内容质量仍有明确待优化点：摘要/依据把独立的商机数与成交订单数推为“成交转化明显偏低/差距更突出”，当前输入没有同批商机的成交归因，不能确认转化效率下降。该问题属于Prompt判断边界，字段/链路已通过；本轮仅验收与记录，未代替用户修改或重新发布Prompt。

## 2026-09-10 六页AI建议彩色列表与左侧气泡

- 用户在8787批注：建议点击后向左弹出气泡，避免展开撑高页面；全部AI建议恢复旧版参考图的彩色图标、标签与紧凑行。已阅读当前代码/CONTEXT并应用frontend-design技能，保持现有技术栈，只修改共用前端组件与现有测试/维护记录。
- `public/ai-advisor.js`将内联details改成可访问的建议按钮；主题图标使用固定橙/绿/紫/黄/蓝配色，按标题展示主题，仅为视觉选择，不改变模型优先级、诊断内容或导航权限。摘要收成可点击预览，完整摘要与数据范围也在气泡中查看，保留原文及全部建议字段。
- 新增共享非模态`advisor-detail-popover`，body挂载避免被卡片裁剪。桌面优先左侧460px气泡，视口边缘自动换侧/贴底，最大高度620px、可见区域边距12px，正文独立滚动。气泡内的依据、假设、步骤、复查标准、缺项、真实证据、受配置控制的追问和安全导航保持完整且动态文本继续转义。
- 交互包含同入口切换、点击外部关闭、Esc/关闭按钮恢复焦点、切换建议、页面导航/快照重绘/入口离屏清理。证据使用既有dialog；Esc先关闭证据并保留建议，再按Esc返回建议行。详情到Chatflow时记录原建议入口作为返回焦点，不把焦点交给已隐藏气泡中的按钮。
- 验证：新增3项回归覆盖左右/窄屏/边缘定位、六页统一入口与文本转义、正文全部字段及未配置Chatflow隐藏追问；完整103项Node回归通过，前端生产构建通过。浏览器使用服务端已有的真实总览结果，展开前后卡片高度均518.890625px（1375×934视口）；左侧气泡、主题色、正文/摘要/范围、证据弹窗与各关闭方式通过。独立预览390×844时气泡366×620、左12/右12、底12px，无横向溢出；切换商品页关闭旧气泡，预览无JS错误。
- 为避免干扰当前页面的人工查看，补充验收使用临时隐藏预览；结束时清除视口模拟并关闭该预览。没有调用新的Dify模型、修改Schema/Prompt/远端配置或业务数据；未重新打包正式Tauri插件，未提交Git。

## 2026-09-10 1.0.4 HTML 启动页实现

- 按用户最新要求制作米白/来搜橙HTML启动页、SVG方向箭头、轻动效、按压按钮、窄屏与减少动态效果适配；内置启动/状态/排障三个交互入口。新增lsou-launchpad Skill与读取固定模板的MCP工具，连接待命，点击后调用open等待实际状态。
- 实际读取Accio0.32.2应用源码确认widget语法和__widgetSendMessage→widget:sendMessage→当前对话发送链路；普通widget无直接工具回调，页面只显示请求已发送。未确认插件详情页自动渲染入口，使用对话内Skill入口，不承诺安装即出现卡片。
- Prompt已先读SOP“店铺诊断/核心产品数据跟踪”：输入为点击意图，判断依据本机状态，动作限于启动或已有排障，输出为真实结果；SOP不覆盖技术桥接，不加入经营阈值、假业务指标或效果承诺。
- 调整既有自动启动测试为点击后启动，并新增无宿主桥接、发送失败、防连点、模板完整性和展示不启动验证。浏览器独立预览正常，无桥接点击返回明确提示。后续真实Accio渲染、完整回归、打包验证另记。

- 验证补充：完整92项Node回归通过，片段输出调整后重跑3项启动页测试通过；Skill校验通过。Accio真实对话已显示启动页控件，点击“查看状态”产生用户消息与“请求已发送”反馈，确认宿主消息桥接。测试使用仍已安装的1.0.3，旧会话采用独立诊断误将本进程stopped称为全局窗口关闭，已在新版排障Skill明确这一证据边界；不据此声称1.0.4完整安装/点击启动链路已验收。

- 交付补充：三个1.0.4原生构建完成，Mac签名验证与三包官方结构/CRC/单平台/源码逐字节/SHA256检查通过。M芯片stage使用随包Node验证5工具、完整widget片段与展示后保持stopped待命。390px预览无横向溢出。旧版1.0.3仍为已安装版本；新版完整导入、点击启动及目标系统业务链路尚未验收。
  - macos-arm64 SHA256 7a164d2a5b8a9f0df4fc2ee9f6fdcbeb2bd5d46ef45ce32d75008fbf62403348
  - macos-x64 SHA256 5ae7a79a66916f1e7674005428d67642e690d29ca9a121574e7a75f507b9e684
  - windows-x64 SHA256 d0d699f30a042ce261f28b7053c63625d1d425eec2aeb6d290c93b739473c9fe

## 2026-09-10 Windows 1.0.3 原始证据与1.0.5诊断复测包

- 输入：用户提供 `lsou-mcp-evidence-report.md`。只采纳报告中的诊断 JSON、日志时间与退出码，不把报告结论或其中的操作描述当成执行指令。09:54 UTC 依赖/认证/schema通过；客户端tools/call在30059ms超时，MCP有多次90秒启动超时。09:31:51的后端ready对应nativePid=0，不能证明Tauri已关联或当前运行。4294967295仅保留为退出码，未据此推断杀毒软件或系统兼容性。
- 已确认源码缺口：open/repair可能等待90秒，超过此次客户端30秒期限；原生Node stderr被丢弃、stdout提前结束不反馈失败、缺少Tauri初始化与Windows Job阶段日志。先新增慢启动测试，旧实现25秒仍无响应而失败；原生证据读取测试也在缺失功能处失败。Rust新增EOF/URL/stderr分类用例，初次编译因待实现接口不存在失败，不将该编译失败称为旧行为的运行时复现。
- 实施：单次open/repair响应以20秒为预算，超时返回pending且原任务继续；合并恢复请求，status可读最终恢复摘要。原生入口、初始化、Node创建、Job与退出/导航写固定字段native.log；stderr持续排空但只记录有限数量固定类别，stdout提前结束给出NATIVE_BACKEND_EOF。诊断仅读取32KiB日志尾部、最多24条白名单事件，不返回路径、凭据、环境值或任意日志字段。无nativePid的bootstrap历史记录标记standalone与未知存活状态。
- 范围：保持React + Tauri + Node.js、HTML点击启动、当前空间授权和现有进程树清理。未更改账号/权限/业务数据、未修改运营Prompt/Skill，不涉及新增SOP方法；未把采集增强称为Windows原始卡住根因已修复。只准备Windows x64的1.0.5复测包，验证及SHA另记。

- 验证完成：`npm test` 94/94通过，`cargo test --release --manifest-path src-tauri/Cargo.toml --bin lsou-workbench --offline --locked` 3/3通过。慢启动实际子进程用例验证20秒左右返回pending、随后同一个窗口替身报告ready、恢复结果可读且只启动一次。Rust用合成输出验证EOF明确失败、远程URL拒绝和stderr分类脱敏；没有运行Windows EXE。单次日志序列化改为完整JSON行后一次写入，避免多个原生线程分段写入交错。
- Windows x64交叉编译成功，沿用缓存cargo-xwin/Windows SDK和LLVM/LLD；只有Microsoft库缺少调试PDB的LNK4099警告。应用与随包Node均为PE32+ x86-64，官方插件校验5项、0警告；ZIP 52个文件、CRC通过、仅包含windows-x64、关键启动源码和二进制与构建输出逐字节一致，未含运行日志/账号凭据文件。
- 交付：`release/lsou-workbench-1.0.5-windows-x64.zip`，38,052,791字节；SHA256 `5291e880eb764c0e5d013beb12c42226307b629597b25bf9021d5beb44dadcb2`。原1.0.3/1.0.4发布ZIP保留；本轮未导入本机Accio、未发送给他人、未提交Git。Windows原始故障仍待该目标机导入、点击启动并反馈`nativeStartup.events`；`runtimeVerified=false`。


## 2026-09-11 按用户晨间续办：AI缓存、气泡与WorkCTL兼容

- 范围：继续前夜明确的五项需求；保持React/Tauri/Node与已发布Dify配置。修改前备份至系统临时目录 `lsou-cache-compat-20260911-8d5c81qt`，保留当前其他桌面交付改动。
- 缓存：从快照内容唯一索引改为业务范围持久索引+原分析快照，version3原子0600文件，无30份淘汰或自动TTL。新增只读cache路由，内存立即恢复，日/周/月及页面互不串结果。覆盖重启、指标变化、同范围并发、显式更新失败保留、旧总览迁移与迟到响应；初次测试发现旧月粒度推断的日期截取错误，修正后迁移测试通过。
- UI：增强气泡标题与业务卡片层次，保留左侧非模态布局与键盘关闭；移除原始证据/JSON/数据范围入口和聊天引用按钮。内部校验与模型输出schema保持不变。当前只使用已保存真实结果验收，不触发新的付费分析。
- 星等级根因：旧 other 分组只有upload-file，真实schema查到相同工具已迁至advisor。新命令真实返回1星、统计日期2026-09-09、商机/交易两条赛道各63分；浏览器实际显示能力项、升星参考和建议。
- 兼容：新增只读命令发现/合同核验/一次受控替代层，用于运营READS和扩展能力。真实验证从旧other路径开始，顺序为解析拒绝→schema发现→advisor完整schema核验→只读业务成功；合成测试覆盖歧义、字段变更、mutating变化、认证、网络超时和写任务不重放。不能自动解决任意业务语义变化。
- Skill：新增lsou-workctl-compat及探测脚本；启动Skill增加分流。参考SOP末段“核心产品数据跟踪、店铺诊断”：对应输入为实际店铺/周期，判断为真实业务字段恢复，动作为只读核验，输出为页面可读结论。SOP未覆盖CLI/桌面版本兼容，故根据当前机器schema和错误处理，不应用阈值或保证升星。Skills结构校验及插件官方验证器通过。
- 验证：npm test 114/114通过，npm run build:frontend通过。浏览器验证缓存早于图表显示、日→周→月恢复、商品页→总览恢复，原生成时间2026/9/10 23:00:28保持不变；星等级真实数据显示成功。插件源码与打包同步规则完成，本次未重建或导入三平台原生ZIP。

补充验收：缓存增加稳定商品身份，覆盖临时编辑引用更新后同商品复用、不同商品隔离。增加生成准备期间切页不误发模型请求的回归。最终114/114通过，前端再次构建通过；390px视口弹层366px、x=12、底部=832（844px视高），内容独立滚动，无证据按钮/原始JSON，无页面横向溢出。

最终复核：总览卡片展开前后均503.89px高，气泡位于左侧，页面证据/数据范围入口数为0。新增防护：数据尚未就绪时，显式更新不会用空输入覆盖已有正常解读，也不会产生模型调用；相关35项定向回归通过。最终服务重启后cache端点仍返回4条昨晚解读，generated_at=2026-09-10T15:00:28.484Z，结果与原分析快照ID一致。

## 2026-09-11 流量来源图并入渠道趋势卡

- 用户要求把来源大类及同行参考与右侧趋势放在一起并可视化。修改前读取代码与CONTEXT，备份至系统临时目录 `lsou-flow-charts-20260911-b4p6wl0_`，沿用既有前端与Node后端，应用frontend-design技能。
- 删除下方独立展开表，右侧改为「渠道洞察」，提供每日趋势、访客规模、商机率对比三个视图，默认显示本店/类目TOP双条图和百分点差。入口名称从当前响应提取，点击大类在卡片内切换具体入口，返回时恢复原按钮焦点；点击左表渠道自动切到同源日趋势。
- 保留来源的单一滚动周期快照和日趋势的所选日期，不计算互斥占比。来源图不再累加访客或平均商机率，非法/缺失字段显示缺项，真实零仍显示0；重复且冲突的字段不挑选或累加。浏览器实读来源为2026-08-31近30天，自增706、搜索609、场景585、互动142；自增具体入口访客合计789，不能替代平台总值706。
- 验证：23项图表/时间口径定向回归通过（新增3项），前端构建、语法和diff检查通过。浏览器验证三视图、左表→系统推荐TM日趋势、来源下钻、键盘返回焦点；网络观察只出现既有日志轮询，没有新的业务查询或模型调用。1280px和1728px宽度无横向溢出；390px视口卡片366px、左右12px，图表改为纵向排布，三个切换入口和来源下钻可用。未发现页面JS错误。
- 本轮只改展示、相关字段解释及现有文档/测试；未改Dify配置、运营Prompt、后端查询或正式三平台ZIP，未提交Git。

## 2026-09-11 渠道卡片等高与全页中缝对齐

- 按用户四处批注，将渠道排原1.15:1改为与上下排共用1:1网格、16px间隙及同一响应式断点；移除卡片底部额外margin，桌面按每排最高内容等高。渠道表改为固定列比例，渠道/访客累计各19%，移除旧460px裁切，保留表内横向滚动。
- 浏览器实测1375px：全页四组分栏边界均为789.5/805.5px；商机率视图两卡均581.445px，访客及日趋势两卡均559.195px。前两列从132.375/125.414px收紧至102.5/102.5px。1728px时全页四组分栏边界均为966/982px，两渠道卡均581.445px。
- 仅调整HTML/CSS和维护记录，未改业务字段、查询、AI配置或正式安装包；本次用实际浏览器几何与构建检查验收，不新增样式镜像测试。
- 验收补充：前端构建与diff检查通过；390px窄屏各组均为366px单列，整页宽390px，380px最小表格宽度仅在内部滚动，无页面JS错误。已刷新用户当前1375px页面，实测两卡均581.445px，全页四排中缝边界一致。

## 2026-09-11 AI 解读使用业务中文，按来源核对字段含义

- 开始：用户反馈摘要和建议中出现uv/pv/qzt/camp/abCnt等字段名。检查本地主回答与检索改写节点、快照构造及页面字段，发现事实正文保留原始键名、主Prompt缺少明确展示语言约束。修改前备份至系统临时目录 `lsou-advisor-language-20260911-u8c2fc5l`。
- 字段证据：只读查询当前WorkCTL 0.1.58的经营汇总、客户画像、渠道及商品效果schema。前三项没有输出明细定义；商品排序说明确认qztImpsCnt/qztClkCnt为全站推曝光/点击、p4pImpsCnt为标准推广曝光；p4pClkCnt说明与命名冲突，未猜译。国际站生意助手的competitor-analysis参考资料明确shop_keyword的pv为平台搜索指数，当前页面用“平台热度”；camp未见可靠定义，不直接扩写为活动或广告计划。
- 修改：在Workflow「生成页面解读JSON」与Chatflow「回答当前页面问题」的System Prompt加入共用规则及分来源术语对照，同步既有独立Chatflow Prompt和README复制段。覆盖所有用户可见文字，保留机器字段、枚举/引用以及真实搜索词、品牌、型号和外语草稿。未知字段跳过相关判断；禁止将指数变次数、每日人数累加变周期去重人数，以及样本广告零值推断整月无投放。未改检索改写、模型配置、知识库、Schema或代码节点。
- SOP映射：已读取用户《国际站运营SOP》正文，参考“关键词库”“付费广告/广告数据分析”“金品场景与场景流量”“核心产品数据跟踪/询盘质量跟进/店铺诊断”。输入仍为同来源/周期的词、渠道、商机及产品事实；判断先核对指标与统计范围；动作给出具体检查和同口径复查；输出为老板和运营能理解的中文摘要、建议及追问。SOP不定义WorkCTL编码，本轮翻译以当前字段说明和页面语义为依据，不套用原SOP预算、阈值或效果时效。未知camp、冲突指标说明、广告覆盖/归因不足仍作为缺口。
- 完成与验证：20项既有Python DSL代码测试通过；解析前后结构对比确认只有主System Prompt变化，三份Prompt及README增补段相同。两份CLI validate --strict及checklist均仍只报本地knowledge未绑定（含空dataset_ids警告），不填假ID，也不据此判断用户云端知识库。diff检查通过。未改远端Dify、未调用真实模型、未清缓存或自动重跑、未重打插件包；发布后的模型措辞仍需在目标模型上验证。

## 2026-09-11 六页字段覆盖补查

- 开始：用户追问“所有界面可能出现的参数”是否覆盖，明确此前只完成常用字段与通用约束，没有全量字段翻译。本次按`beginRead/finishRead`和`getAdvisorPageContext`追踪首批六页实际AI输入，读取public/app.js、public/operations.js、public/advertising.js及对应Node归一化/白名单。只读核验7项当前WorkCTL schema，未调用业务生成或模型；备份为系统临时目录`lsou-advisor-sixpage-terms-20260911-zujhoux4`。
- 补充：在既有语言规则内增加A—G词典，涵盖通用范围/状态及六页，加入星级门槛、客户行为/会话、商品四象限和商品资料、国家维度、行业榜单/指数、广告计划枚举、资金流水、推荐原因。机器标识、媒体路径和游标专列禁显，不盲目译成指标；仍保留真实业务外语内容。
- 语义差异：实时渠道趋势schema确认fb_uv为TM咨询人数、fb_mc_uv为询盘人数，区别于渠道效果的fbUv；地区维度区分询盘人数/询盘数、搜索/全站。点击下单不写为成交，询盘篮不写为购物车成交，未读不推导卖家未回复。dAbRate仅解释为平台市场转化指标，未核公式不作为本店转化；crtOrd单位说明存在差异时只称起草订单指标；camp、p4pClkCnt等仍待核。原始行业画像/自由属性不具完整输出schema，不能承诺所有未来字段均已逐一翻译。
- 固定字段审查：从9组现有源码定义抽取104个去重字段；初查漏列的visitorId、campaignId、campaignGroupId和媒体位置字段补入“内部关联/不展示”规则。README新增六页范围表及明确待核项；这104个是有界审查清单，不是平台全量字段覆盖率。
- SOP映射：沿用本轮已读的“市场定位/选品、关键词库、高质量发品、付费广告、优爆品、询盘质量跟进、核心产品数据跟踪/店铺诊断”。输入为各来源的真实业务对象与范围，判断先核对定义与口径，动作仍为对象检查和复查，输出为可理解的中文分析和追问。接口参数、枚举和字段冲突不由SOP补定义，不套用原SOP预算、阈值或升品承诺。
- 完成与验证：9组固定定义的104个去重字段全部通过显式处理检查；四处词典文本一致，独立Chatflow全文与节点一致；对比本次备份，两个Flow仅语言段变化，模型/图/代码/Schema不变。20项既有DSL代码测试通过，diff检查通过；两份严格校验与发布清单仍只报本地knowledge未绑定，不据此推断云端配置。未发布云端、未调用真实模型、未改后端/页面或缓存，未知动态字段仍按待核处理，不声称未来字段或模型输出100%已验收。


## 2026-09-11 Windows 1.0.6 stderr/panic 增强诊断交付

- 输入：用户提供`lsou-startup-failure-report.md`，读取全文并复核当前Rust/Node源码与原1.0.5 ZIP。报告的同文件手动成功与环境布尔值是目标机证据；“未执行任何JS”“必然是tao竞争/管道配置错误”仍属推测，不作为修复前提。
- 实施：新增原生有界错误采集模块与regex直接依赖（沿用锁内1.13.1）；保留前32KiB stderr并持续排空，旧阶段事件仍随前8行到达写入。错误正文脱敏后写native-evidence.log，保留真实nodePid/nativePid和时间；panic hook用Backtrace::force_capture采集脱敏消息/堆栈。记录固定spawn配置布尔值，不记录环境块、命令绝对路径或就绪URL。日志按约512KiB轮换，只保留当前/上一份；读写失败不会阻断启动。MCP只报告证据文件是否存在，不直接输出正文。
- 隔离：基于原1.0.5 ZIP解压到系统临时目录lsou-windows-diagnostic-qq18wyz1，使用当前增强Rust源码、冻结payload/runtime、原Cargo.lock依赖，在隔离Cargo/tauri配置设置1.0.6；使用已有cargo-xwin、LLVM/LLD、XWIN_CACHE_DIR和原target缓存离线构建。工作区主版本未批量升级，避免混入其他未发布业务变更。构建目录记录在.build-cache/diagnostic-build-path；后续常规重建会覆盖共享target二进制，不应直接把它认作冻结诊断EXE。
- 验证：最终源码Rust测试6/6通过（错误分类、EOF/本机URL、脱敏、无换行大stderr持续排空及短正文/阶段行）；桌面Node测试18/18通过，覆盖诊断不外泄正文和20秒pending/同进程恢复。语法与diff检查通过。Windows锁定依赖离线交叉构建成功，EXE为PE32+ GUI x86-64。官方结构校验5项、0警告，ZIP 52文件/CRC通过。
- 包差异仅6文件：desktop/diagnostics.cjs、plugin.json、resources/windows-x64/build.json、原生EXE、payload/desktop/diagnostics.cjs、scripts/desktop-mcp.cjs（仅版本常量）。Node SHA256 bae898add4643fcf890a83ad8ae56e20dce7e781cab161a53991ceba70c99ffb；bootstrap 955dace48d30e88d082b086969242222c575c34661669aa311ef9ae302b1335b；server f967e916c0927e6c96e915b686b857d45f5cb42948ebefda64c5a1122b883b02，均与目标机报告及原ZIP一致。未新增Prompt/Skill，因此不涉及运营SOP方法变更。
- 交付：release/lsou-workbench-1.0.6-windows-x64.zip，37,932,981字节；SHA256 901a788ca8d1ea0fc659e86a8c0810c5a0d386f1e964146eeb47536d041c8ed1。原1.0.5 ZIP保留。未在Windows运行EXE、未验收窗口/真实业务，runtimeVerified=false；未发送给他人、未提交Git。下一步仅需目标机确认1.0.6加载、启动一次，读取本次native-evidence.log的node_stderr/native_panic摘要；故障根因尚未确定。


## 2026-09-11 Windows 1.0.7 原生入口路径兼容修复候选

- 开始：收到用户转述1.0.6 native-evidence.log中的`EISDIR/lstat C:`和同文件手动bootstrap可ready，未得到完整目标机argv，故不把“路径截断为C:”当已确认事实。只读核对tauri-utils 2.9.3的current_exe/resource_dir、随包同版本Node22.22.0内置fs.realpathSync；检索官方Node issue https://github.com/nodejs/node/issues/60435（22.20.0的verbatim长路径同类错误）和#61165（22.21.1同类错误）。这些是相似案例，不是本目标机根因的实测复现。
- 修改：新增node_launch.rs，将Windows常规磁盘/UNC verbatim资源路径转换为普通完整绝对路径，使用资源cwd+固定相对bootstrap入口，执行文件仍为同包node.exe。拒绝相对/设备/特殊尾部路径；参数数组保留空格和Unicode，不使用shell或手工引号。Mac保持原命令和cwd。main.rs记录安全的路径形态布尔值，不记录真实命令/用户路径。未改Node/Workctl、认证、权限、业务payload、stdio或Job控制。未新增/修改运营Prompt或Skill，不涉及SOP方法。
- 验证：本机Rust测试9/9通过（新增磁盘中文/空格/长路径、UNC、危险/歧义路径拒绝）。Windows专属Command的可执行路径/cwd/单一相对参数断言仅通过cargo-xwin check --tests编译，未在Windows执行，不能计为运行测试通过。Windows release离线构建成功，EXE确认PE32+ GUI x86-64；官方结构校验5项0警告，52文件ZIP CRC通过。diff检查通过。
- 构建隔离目录：系统临时lsou-windows-path-fix-w10d13gj，路径指针.build-cache/path-fix-build-path；复制源Rust/Cargo锁与1.0.6冻结payload/runtime，隔离版本1.0.7，使用已有cargo-xwin/LLVM/XWIN缓存与共享target。工作区package/Cargo/tauri版本保持1.0.5，避免把其他尚未交付功能打入本候选包。
- 包对比：相对1.0.6仅4文件变化：plugin.json、resources/windows-x64/build.json、lsou-workbench.exe、scripts/desktop-mcp.cjs（版本常量）。除1.0.6已有diagnostics.cjs外，所有payload文件与1.0.5逐字节一致，node.exe也一致。实际ZIP内EXE与此次编译产物一致。保留旧版ZIP。
- 交付：release/lsou-workbench-1.0.7-windows-x64.zip，37,931,379字节；SHA256 5d8ce4068230f63050578da1bcdf8c2d73cc52faaf8f12601ce9ff716fa0e03f。build移除diagnosticOnly，增加releaseCandidate/windowsNodePathFix=true，runtimeVerified仍false。没有Windows真机验收、没有向他人发送、没有提交Git。待对方确认1.0.7实际加载，点击启动核对Tauri与只读店铺数据；若失败读取本次spawn_configuration/node_stderr/native_panic，而非重复repair。


## 2026-09-11 1.0.8 清理跨商家样本并封装内部AI配置

- 触发：用户确认1.0.7 Windows正常启动，随即反馈工作台出现开发账号数据与smart watch。现场读取1.0.7 ZIP确认workctl-demo.json 45,476字节（37处smart watch）、workctl-command-audit.json、app.js静态经营对象/商品图片/固定类目，以及React编译模板中的固定文案。启动修复冻结旧业务载荷时未完成跨商家交付数据检查，是本次污染来源；没有证据显示对方拿到了开发者当前账号授权。
- 清理：停用/api/demo为410，商品质量统计不再读快照；广告画像不回退开发样本；RFQ空词不调用商机搜索，显式产品词才查询，历史报价仍取当前账号。行业读取当前shop-summary的cateId，缺类目不发固定行业查询。移除静态经营样本对象及Legacy渲染器、固定手表/手环/耳机参数、商品图片URL和新草稿的样本卖点/价格/包装/物流属性。缺项保留空值，真实账号参考商品映射与发布确认边界继续保留。页面权益无实时数据时标为未查询，不展示旧数字。
- 防复发：prepare不再复制demo-data；prepare和package接入verify-release-data.mjs，阻断已知样本目录、关键词/类目编号、静态商品图和日志。保留开发历史文件供审计，但HTTP无法读取，也不能再次打包。1.0.7及更早ZIP保留本机，不应再用于跨商家交付。
- 新增用户授权：明确要求当前内部包封装Workflow和Chatflow两把APP API key，并保留原配置入口。新增lib/internal-test-config.js，只在后端读取payload/internal-test-dify.json白名单，.env.dify/.env.advisor与进程/桌面本机配置优先；状态只返回配置布尔值。真实密钥仅写临时stage与最终内部ZIP，源码、前端、日志未写值；普通交付检查拒绝此JSON，显式内部检查可放行，生产时移除文件即可。没有改运营Prompt/Skill及其SOP方法。
- 验证：最终npm test 124/124通过；初次完整回归唯一失败是MCP版本测试硬编码1.0.5，改为校验package版本一致后全量复跑通过。新增测试覆盖样本打包阻断、两个商家的类目选择、缺类目不查询、RFQ空词/显式词、空白草稿、/api/demo关闭、内部配置可选/不向状态泄露/本机覆盖/普通包拒绝。React构建通过；模拟独立商家浏览器检查RFQ、流量、发品、关键词四页，未显示旧样本，缺数据保持空态；非真实店铺验收。
- 密钥网络核验：Python请求/info收到Cloudflare 403；改用与实际后端相同的Node fetch，两把Key分别返回HTTP200，应用名为来搜AW Workflow/来搜AW Chatflow。只读应用信息，没有触发模型生成或付费分析。包内加载createAiAdvisor验证两项Configured均true；不是生成结果验收。
- 构建：按清理后的当前源码整体生成payload与React模板，避免旧模板/新JS混用；保留已跑通Windows路径修复。源码package/Cargo/Tauri/插件/MCP/前端统一1.0.8。Windows原生1.0.8离线交叉编译通过；仍未在Windows运行新1.0.8，因此runtimeVerified=false。阶段目录为系统临时lsou-clean-delivery-8p4826d7，指针.build-cache/clean-delivery-path。与此前1.0.5冻结修复包不同，本次带当前已测业务代码。
- 交付：release/lsou-workbench-1.0.8-windows-x64-internal.zip，37,922,731字节，54文件；SHA256 6e0757a3925a2db72c299fc527edc7d4284968bd3e70b67fd7183e72b602d507。官方结构校验5项0警告，交付数据检查36个文本文件通过，ZIP CRC通过；逐字节检查两把真实Key只出现于后端internal-test-dify.json，未出现在public或其他文件。未发送给他人、未提交Git。待用户导入内部包，复测Windows窗口/当前店铺数据与AI生成。

## 2026-09-11：成功启动经验落实为启停增强 lifecycle-r1

依据用户分享 hu9crq54qMIV：1.0.7 直接 open 返回 ready，但工具发现绕路，关闭缺少 stop。更新 plugin/scripts/desktop-mcp.cjs：新增 lsou_desktop_stop，状态含 stopping 与 current_mcp_session 范围，等待持有子进程 exit 后报告停止；1.8 秒升级信号、5 秒有界超时，不批量按名称杀进程。停止请求递增生命周期编号，取消已在途的恢复，合并重复停止，停止期间 open 不创建新进程。MCP 保持待命，允许随后重新打开。后端仍由已有 Windows Job / Mac 父进程监测机制清理，不将窗口 exit 冒充逐一核验后端退出。

状态新增 backendReady/windowVerified/businessDataVerified；后两项保持 false，表示 MCP 未完成界面或业务验收。buildRevision=lifecycle-r1，沿用 1.0.8 原生程序，不伪造新原生版本。修改启动/排障 Skill：先发现当前注册 MCP，延迟发现无结果时读取实际 accio-mcp-cli 帮助，不猜路径和命令；用户直接启动时跳过 HTML，停止意图优先，不自动再次恢复。旧版无 stop 不猜参数。

SOP 参考 docs/references/国际站运营SOP.md 的“核心产品数据跟踪、店铺诊断”：输入为当前会话状态与用户意图，判断分开后端、窗口、当前店铺数据，动作限启停/有限恢复/只读验收，输出实证和未验证项。SOP 不覆盖 MCP/桌面进程，不强套经营阈值，未修改云端 Dify Prompt。

验证：npm test 124/124通过，覆盖停止后重开、重复停止合并、停止期间open阻断、在途恢复取消、状态证据边界；git diff --check通过。Windows启停增强ZIP仅替换4个脚本/Skill/README条目与build.json修订信息，原生EXE和业务payload逐字节保留；官方插件验证、样本数据门禁、ZIP CRC与密钥仅位于后端配置的扫描通过。SHA256 fd1bc08edfec25572a0179d1392b762f9399bf300899f79d3f90ddb7a9ab7ccb。Windows实机停止和再次启动尚未验证，本轮未实现自动更新。

## 2026-09-14 1.0.9 产品图片接入 Accio 原生 CDN

- 开始与证据：用户要求实测上传接口后直接接入插件。核对当前 Accio Work 0.32.2 的本机网关源码，确认 POST `/api/image/cdn/upload` 使用 `{data_uri}` 和当前本机 Basic 会话认证，内部经 ADK 上传；并非 WorkCTL upload-file。先用无业务内容的 512×512 测试图片真实调用，返回 CDN URL，匿名 GET 200，原图与下载字节 SHA256 一致。此前“没有可调用上传入口”的结论不完整。
- 修改：server.js 保留既有浏览器上传契约与 JPG/PNG/WEBP、8MB、图片签名校验，改为上述网关调用。认证复用 desktop/runtime.cjs 的当前账号环境，限制回环 HTTP、固定路径、禁止重定向、90 秒超时；不落盘图片或凭据、不透传上游正文、不自动重试。按登录失效/旧版本/限流/网络等情况返回固定中文错误；日志仅记录开始、结束、成功/失败、状态和耗时。返回值必须是非本机/IP 的 HTTPS URL。
- 页面与启动：保留已有上传状态、预览、封面、失败重试和批量商品编辑；成功地址进入 `basicInfo.images[].newImageUrl`，发布仍走原 WorkCTL 队列。更新上传提示和能力缺失错误，移除旧存储配置及启动时 upload-file schema 预检，同步 plugin/desktop/runtime.cjs。源码与三平台原生版本统一 1.0.9；未新增或修改 Prompt/Skill，不涉及运营 SOP 方法变更。
- 自动化：npm test 125/125 通过；网关测试覆盖真实请求形态、Basic 认证、无 bucket、预检不上传、格式/大小/非同源拒绝、401/403/404/413/429/500、非 JSON、异常 URL、重定向和断线，并断言无旧 WorkCTL 上传调用、无重复尝试和无凭据泄露。原发布集成验证 CDN 地址原样进入 WorkCTL material，但 WorkCTL 写调用使用测试替身。版本修改后 MCP 初始化版本一致性测试通过，语法与 git diff --check 通过。
- 页面真实上传：使用 Mac arm64 构建包内 Node、后端和 React 前端，经 Playwright 选择合成测试图片；仅其他业务读取使用测试数据，上传能力/上传接口保持真实。收到 HTTP 201、页面“1 张已上传”、预览与序列化 JSON 为同一 CDN URL，匿名 GET 200 且与原图字节一致，无页面异常、无真实商品发布请求。证据在系统临时目录 `/tmp/lsou-cdn-qa/` 的 browser-result.json、upload-success.png；该目录不进入交付。
- Tauri 检查：通过 arm64 stage 包内 MCP 的 lsou_desktop_open 启动原生窗口，工具返回 1.0.9/ready；另用桌面可访问性读取确认真实窗口、当前店铺经营数据与产品发布空工作区，不把 MCP 的 ready 单独当成窗口验收。未在 Accio 中重新导入安装新版 ZIP，也未实际发布商品。原有 8787 开发服务未重启。
- 构建：Mac arm64/x64 原生构建与签名检查通过；Windows 首次构建因 cargo-xwin 不在 PATH 失败，随后使用已有 `.build-cache/cargo-xwin`、LLVM/LLD 与 XWIN_CACHE_DIR 构建成功，仅已有第三方 PDB 警告。Intel Mac/Windows 未真机运行。三个 ZIP 各自官方结构检查 5 项、0 警告，发布样本数据检查、ZIP CRC、版本和关键 payload/MCP 源码逐字节检查均通过。标准包不含 internal-test-dify.json，沿用已有本机 AI 配置读取规则；旧内部 1.0.8 ZIP 保留。
- 交付：`lsou-workbench-1.0.9-macos-arm64.zip`，42,873,298 字节，SHA256 `ec76f6edc8184d6e6715ca9e1683f52a81d21fcb00073f7255ac3ebd4528dd52`；`lsou-workbench-1.0.9-macos-x64.zip`，44,457,431 字节，SHA256 `531b96c5e062089e10d6606098633274f1b976194bc3f7326f38f417945117c3`；`lsou-workbench-1.0.9-windows-x64.zip`，38,616,584 字节，SHA256 `66afced46273f07164fb285663b3c288e75261632d74489fff94025c662f50ca`。均在 release/，没有通用版，没有发送给他人或提交 Git。

## 2026-09-14 本地发品缺少 SKU、失败原因被零分覆盖的修复

- 触发与定位：用户在 8787 本地页面批量保存 3 条草稿失败。只读 jobs/log 确认均为 `JSON_VAL_EMPTY_SKU`，没有商品号。`normalizePublishProduct` 未生成 trade.sku；参考参数查询/裁剪、浏览器映射与序列化也漏了规格。页面用 `Number(null)` 判断评分，错误详情又只使用顶层泛化 message，最终弹窗只显示“质量分 0”。这是本地 JSON 校验失败，不是 CDN 上传失败或平台质量评分为零。
- 真实合同：读取当前安装 WorkCTL 0.1.58 的 publish-from-json 帮助与插件内 sample_stripped/sample_edit_result、excel-direct 规范。用真实 `--validate-only` 验证：缺列表报 EMPTY_SKU；仅加空 skuAttributes 仍报 SKU_NO_ATTR；必须至少一个规格对象且含至少一个属性。没有按通用默认颜色/型号构造虚假规格，没有触发商品创建。
- 数据修复：server.js 新增 normalizePublishSkus，参考查询增加 sku component，裁剪只保留可编辑属性及其 ID/图片、skuCode、stock、unitPrice，去掉旧 SKU ID；限制规格/属性数量并拒绝损坏形态，不静默截断。前后端都阻断缺少规格属性的草稿/发布；单规格库存缺失可沿用已填总库存，价格可沿用已填首档价格，多规格保留独立报价/库存。空库存与空起订量保持缺失，不能变成 0。
- 页面修复：public/publish-product-utils.js 深拷贝参考 trade.sku 到 product.skus；原“复制同类”继续深拷贝。app.js 增加规格/属性增删、商家编码、独立单价与库存编辑，serializePublishProduct 带上 trade.sku；改规格文字后清除不再对应的旧枚举 ID 和规格图片。新建商品规格值为空，要求用户填真实资料。没有修改 Prompt/Skill，不涉及运营 SOP 方法。
- 结果修复：后端优先读取 pending_fix.details[].error，识别 sku 待检查区域；后端/前端均保留未知评分为空，真实 0 分仍可显示。队列/结果弹窗共用 publishResultDetail，失败先显示具体原因和错误码，编辑器同步提示；移除“每条均得到国际站返回结果”这类不适用于本地校验失败的文案。
- 测试：显式启用真实 CLI 的全量 `LSOU_VALIDATE_REAL_WORKCTL=1 npm test` 130/130 通过；最后补改空总库存与起订量处理后，8 项相关纯转换/真实 CLI/后端集成测试再次通过。新增覆盖单规格、参考多规格保留、深拷贝、空/错误规格、独立库存价格、pending_fix 明细、空/空白/零评分。原 WorkCTL 替身改为必须收到非空 SKU/属性；实际商品写入仍仅由替身执行。最初真实校验测试误期待 ready=true，按实际 status=validated 修正测试断言。
- 浏览器：隔离 54879 后端与 Python Playwright 检查从零创建阻断空规格、编辑值进入 JSON、添加/删除规格、参考多规格映射与改值后 ID 清理；三条失败弹窗逐条显示错误码/原因，不出现假零分，无页面异常与真实入队请求。截图与报告在 /tmp/lsou-sku-qa/。React 构建、Node 语法与 git diff --check 通过。
- 当前账号核验：更新后实际参考商品接口返回 5 个非空规格，旧 SKU ID 未公开。把同一参考资料经当前后端组装，真实 `--validate-only --compact-output off` 返回 validated；默认 compact 输出首次省略 data 导致测试解析失败，关闭压缩后完成核验。临时商品 JSON 为 0600，校验后清理；此证据仅证明本地 WorkCTL 校验通过，未验证平台创建草稿或审核。
- 本地运行：确认队列无 queued/running 后重启 8787，当前进程 64378，health=ready、WorkCTL authenticated=true、CDN configured=true；日志位于系统临时 lsou-sku-fixed-f76xxn9b.log。没有刷新用户当前标签页，旧页面待发布商品缺失的 SKU 不会自动恢复，需重新读取参考资料。隔离测试后端在验收后停止，用户服务继续运行。本轮未重打三个 1.0.9 ZIP、未提交 Git、未再次保存或发布用户商品。

## 2026-09-14 待发布列表新增删除按钮

- 开始：用户标注“复制同类”，要求复制后能移除多余商品。阅读现有列表、复制、图片上传、队列轮询与空态渲染后，在同一操作列加入“删除”，竖排避免挤压窄表格标题。
- 修改：仅前端修改 public/app.js、index.html、style.css。新增 isPublishProductBusy/removePublishProduct/undoRemovePublishProduct；删除保留原对象及列表位置到当前页 removedProducts 栈，顶部持续提供撤销入口，包括最后一件删除后的空态。恢复保留图片、规格、编辑内容和勾选状态，清除筛选并切回恢复商品。删除当前项后选择剩余可见项，同步列表、编辑器和数量；不触碰服务端队列、已保存/已发布商品或 CDN 图片。
- 在途控制：正在上传图片或有 queued/running 任务时按钮禁用，实际移除时复核；队列轮询刷新列表按钮状态。没有添加平台删除接口、自动撤销提交或确认弹窗。撤销历史仅保留于当前页内存，刷新后不保留。
- 验证：按 webapp-testing 既有流程使用隔离浏览器和模拟业务响应访问当前 8787 静态页面，所有入队请求拦截。完成复制后删除、当前/非当前项删除、连删至空与逐次撤销、SKU 数据和原顺序恢复、选择计数/底部状态、在途阻断、保留历史任务检查；无页面错误或真实写请求。测试初次点击时仍在输入框 blur 重绘阶段，先结束编辑后成功触发既有复制；顺序断言按既有“新副本插入头部”的实际语义调整。证据在 /tmp/lsou-remove-qa/，不进入交付。
- 完成：React 构建、Node 语法与 git diff --check 通过；此为可逆本地交互，没有新增单元测试或重复全量测试。无需重启后端，未刷新用户当前页面、未操作其真实商品、未重打 ZIP 或提交 Git。没有改 Prompt/Skill，不涉及 SOP 方法。


## 2026-09-14 产品发布读取缓存持久化

- 开始：用户反馈发品页反复读取。核对当前服务、CONTEXT、缓存和API后确认：只有图库落盘，目录/类目/业务选项为30分钟内存缓存，账号参考模板与交易详情每次重新查询；当前页有草稿，不刷新用户标签页。
- 实现：新增 `lib/publish-read-cache.js`，只缓存已裁剪的成功只读资料，按账号哈希隔离系统缓存文件、0600原子保存；默认无浏览过期，合并同键请求，返回副本，清除/失效隔离旧在途请求。磁盘损坏降级重新读取，保存失败显示内存状态并写安全日志。账号不明时禁用持久化，记录不含凭据/浏览器引用/写任务。目录原始经营指标和负责人不落盘。
- 接入：server.js统一目录、类目、Schema、交易选项、参考模板与参数缓存；图库取消24小时自动过期，仍按缺失/修改补齐。分页与业务选项部分失败抛错防止长期缓存残缺成功。专用手动更新绕过经营查询旧快照，标记其他详情按需刷新，后台更新图库；写前类目归属/规则/业务选项仍按30分钟重新核验，失败不入队。短期引用进程内登记24小时有效，不持久化。
- 页面：新增保存状态、原读取时间与“更新店铺资料”；顶部刷新在发品页走相同入口。更新成功或失败保留待发布商品的已填标题/价格/图片/规格/库存/选择/队列，更新已编辑类目的字段定义；不自动保存或重放浏览器草稿。清除缓存后重建页面参考资料，避免继续使用已清理的引用。
- 测试：新增4项缓存行为测试并扩展现有真实HTTP+假WorkCTL集成测试，覆盖两天前缓存、新进程禁用上游仍恢复、旧令牌不落盘、显式刷新真读与失败响应；全量134项，133通过、1真实CLI validate-only测试按默认配置跳过，无失败。浏览器最初用networkidle断言更新结束过早，改为等待实际sourceRefreshing=false后通过；导航不重读、更新/失败不丢编辑、顶部刷新分流和按钮恢复均通过，无页面错误/真实发品请求。最后补充非默认编辑类目的同步后重新通过浏览器与React构建。
- 现场：确认原8787队列无queued/running后，通过原desktop/runtime.cjs启动方式更换服务，当前PID69506，日志为系统临时 `lsou-publish-cache-ugfihkfq.log`。首次把当前334件商品资料写入本地，目录+业务选项23.4秒；第二次并行读取目录/业务选项/规则/参考详情11毫秒，新增WorkCTL调用0次。独立新服务禁用WorkCTL后从真实缓存恢复同样资料31毫秒，0次上游调用、5个参考SKU；隔离服务已停止，8787保持运行。未刷新用户原标签页。
- 完成：验证截图/脚本/报告在 `/tmp/lsou-publish-cache-qa/`。没有修改Prompt，不涉及SOP变更；没有发布真实商品、重打ZIP或提交Git。已有正式包仍需后续单独重建和安装验收。


## 2026-09-14 草稿提交前规则读取超时与确认按钮修复

- 开始：用户上传新图后保存草稿报 `连接下游服务超时（已等待3133ms） / mcp_upstream_rejected`。按systematic-debugging核对当前8787、缓存时间和请求链。日志05:48:36Z上传成功，随后交易选项和类目列表成功；队列为空，旧Schema仍为03:58Z，说明发生在写前重读类目规则阶段，尚未调用发布流水线。旧代码不记录失败的属性子查询，因此无法历史还原具体是list-attribute还是list-attribute-options。实际独立读取二者返回成功，耗时1491/1079ms，证明当时连接故障已恢复，不是图片上传被拒绝。
- 先复现：依据截图返回值，把现有集成替身的第一次类目属性读取改成同样超时，原HTTP测试出现502而非200。隔离浏览器在上传成功后模拟503，原确认按钮无法恢复，捕获 `Cannot set properties of null (setting disabled)`。按test-driven-development先保留失败证据，再修改代码。
- 实现：runPublishRuleRead只允许四个固定只读命令，运行20秒上限，只对明确超时/连接中断最多重试2次并退避500/1000ms；上游拒绝码本身不构成重试条件，权限/认证/参数错误立即停止，不能传入上传或发布命令。成功资料仍落原账号缓存；连续失败不利用旧Schema绕过写前核验。安全日志记录阶段、尝试、耗时及成败，不记录查询载荷或凭据。
- 页面与协议：enqueue的规则错误返回明确submitted=false、阶段、错误码与503/502，说明商品尚未提交且图片可复用；确认弹窗持续显示错误。onclick提前捕获button，失败后恢复文字和可用状态，不在await后访问被清空的currentTarget。浏览器编辑数据不清空，自动重试不涉及再次上传或publish-from-json。
- 验证：现有集成测试扩展连续三次超时，断言503、任务数0、发布命令调用0、上传次数不变；新增3项权限/普通拒绝不重试、写命令白名单阻断、属性选项超时恢复。全量137项：136通过、1真实CLI validate-only按默认配置跳过，0失败。隔离浏览器模拟上传后连续两次保存失败，图片URL/完整商品JSON保留，按钮每次恢复，0页面异常、0真实发品。React构建及静态/diff检查通过。
- 真实只读核验：用新代码强制重新读取当前类目的账号归属、属性与选项，分别6969/1527/2605ms，均首轮成功；返回21项属性、20项官方选项覆盖，写回本账号缓存，没有创建草稿/发布商品。证据在 `/tmp/lsou-preflight-qa/`。
- 完成：确认旧69506进程没有queued/running任务，替换为74364，仍使用desktop/runtime.cjs --start-development；日志 `lsou-preflight-fixed-fjv9tffu.log`。未刷新用户当前页面，以免丢失编辑；当前旧确认框可先关闭再重开以解除旧按钮失效，新页面才加载按钮修复。未修改Prompt/Skill，未重打ZIP或提交Git。


## 2026-09-14 GW75草稿的多选属性长度错误

- 开始：用户再次保存后，队列明确返回JSON_VAL_ATTR_TOO_LONG，basicInfo.attr[4].attrValue长度278超过50，失败发生于WorkCTL本地JSON校验。通过当前账号参考缓存核对，Function有23个独立属性项；页面参考映射正确保留数组，但normalizePublishProduct将前20项用分号合并，既变成超长文本又丢掉后3项。这是本项目转换错误，不是用户图片错误。之前验证的简单参考没有覆盖完整前端多选序列化链路。
- 实证：使用实际GW75参考资料、当前Schema、生产mapReferenceProductToDraft及serializePublishProduct生成输入。旧生产转换真实--validate-only复现完全一致278字符错误；临时逐项展开探针23条、最长18字符，返回validated。没有实际保存或发布商品；临时JSON0600并在验证后清理。
- 先测后改：新增23项保留/官方ID与单值长度回归，旧代码分别以1!=23和缺少应抛异常失败。新增normalizePublishAttributes，无损展开多选，按本次服务端Schema匹配各项官方ID；不拼分号、不截断选项/文字。真正单项超长入队前说明字段名和长度。validatePublishProductSchema返回规则，enqueue逐品传入normalizePublishProduct；普通分号文本保持原样，不误拆。前端业务逻辑不变，只修正旧注释。
- 验证：修复后完整生产转换的GW75材料真实CLI返回validated，Function23条、最长18字符。现有HTTP集成替身要求最终JSON保留23条和对应官方ID，证明enqueue确实传递Schema；加入单值超长校验。LSOU_VALIDATE_REAL_WORKCTL=1 npm test为139/139通过、无跳过；React构建、Node语法和diff检查通过。证据位于/tmp/lsou-multi-attr-qa/，未重打ZIP或提交Git，未更改Prompt/Skill。
- 本地：确认74364队列无在途任务后更换服务，当前PID 75594，日志 lsou-multi-attr-fixed-8x0nqiry.log；沿用desktop/runtime.cjs --start-development。未刷新用户页面或删除其编辑数据，原页面提交的多选数组可直接由新后端处理，已上传图片无需重传。只验证JSON校验和本地业务链路，不把validated当作平台草稿保存成功。


## 2026-09-14 发布历史与参考商品选图位置调整

- 开始：用户要求已完成队列不再常驻页面、可查看历史队列；移除全店图库缓存统计；删除提示仅在删除后显示；创建标题上移，复用旧图紧跟所选参考商品。
- 实现：主页面按尚有queued/running的批次过滤进度与队列，完成自动隐藏但保留原回执和一次性结果通知。新增发布历史弹窗，按批次显示时间和成功失败数，完整结果可以返回历史；不再限制12条明细，长失败原因换行。当前历史仍来自本地Node服务的进程内队列，未扩展跨重启恢复。
- 布局：创建标题从滚动内容移到左卡片首部，删除提示8秒后或开始创建时收起；撤销还原原商品对象。移除全店图库缓存状态块；旧图选择区和创建按钮放到选中参考行下方，单一外层滚动，异步回填与勾选重绘保留位置。具体商品图片读取错误仍就地显示，图片加载期间阻止确认创建。
- 验证：隔离Playwright使用受控API回执，覆盖15条历史/14条完整批次、2条任务运行到完成、结果只通知一次、可撤销删除及超时不复现、第1/5/20行就地选图、10张选图上限内更换图片保持位置、搜索重新选择和900px窄屏；0页面异常、0真实写操作。139项现有测试138通过、1项显式真实CLI校验按默认配置跳过，React构建、Node语法及git diff --check通过；证据在/tmp/lsou-publish-ui-qa/。
- 完成：8787直接读取更新后的HTML/JS/CSS，各返回200且含新入口/实现；未重启服务，当前6条已结束任务保留，0条在途。未刷新用户标签页，避免丢失未保存编辑。未改Prompt、Skill、后端发布行为，未打包ZIP或提交Git。


## 2026-09-14 商品主图上限10改6

- 开始：用户指出商品编辑器出现8张主图，允许数量应为6。源码定位到旧图选择/参考映射/上传/文件夹归组/后端转换均使用10，表格5/5只显示完成度又掩盖真实张数。
- 实现：共用MAX_PRODUCT_IMAGES=6；新参考默认选6、手动选择最多6、文件选择超剩余名额整批拒绝、文件夹超量整批停止并点名。列表与编辑器显示真实X/6，满6隐藏上传入口，删除恢复。保留旧草稿所有图片以便用户挑选，取消渲染/排序/删除/序列化的静默截断，超量阻止草稿与发布。后端对去重后超过6的远程主图拒绝，不提交裁剪后的素材。未影响历史图片资料库、规格图及商详图。
- 验证：6张通过/7张草稿和正式发布均拒绝的纯转换测试通过；HTTP集成直接传7张回400，任务0、发布调用0。140项全量139通过、1需显式开启的真实CLI校验默认跳过；无失败。隔离Playwright验证6张选图、更换旧图、8张旧草稿完整显示/序列化/重排、减到6解除超限、5张再选2张0上传请求、选1张成功、满额继续触发也不上传、7张文件夹不创建。0页面异常、0真实写入；最终隐藏满额上传占位后复验浏览器。React构建、Node语法和diff检查通过，证据/tmp/lsou-image-limit-qa/。
- 当前交付：更新public静态资源与后端源码。为保留主服务已有进程内发布历史，不重启8787；页面刷新后加载前端限制，后端额外拦截随下次启动加载（隔离新进程HTTP已实测）。用户原页面未强制刷新，避免丢失未保存编辑。未改Prompt/Skill，未打ZIP、未提交Git。

## 2026-09-14 参考关键词、合并类目控件与橙色插入线

- 开始：用户要求类似商品带入关键词、类目搜索与已选值放在一起、拖图时明确显示插入位置。检查当前代码发现参考关键词和本地复制已保留数组；当前 E900 缓存关键词为空。真实只读查询首次遇到上游连接超时，再查 `list-information --queryType trunk` 返回 `basicInfo.productKeywords: ""`，另一详情查询没有关键词字段；未执行保存草稿或发布。
- 关键词：保留原有来源和深拷贝，新增参考来源/原词为空状态。平台未返回原词时显示可补充提示，手动填写后隐藏；不生成或编造原关键词。扩展既有测试验证参考复制和本地复制互不污染关键词数组。
- 类目：将两个并排输入改为单个可搜索下拉控件，展开后才出现搜索框；保留已选路径，支持键盘和外部点击关闭。加载/错误/无匹配状态在控件中提示，搜索编号与商品ID过滤过期响应，编辑器重绘取消旧搜索。
- 拖拽：新增独立橙色竖线，通过指针在目标图左/右半区判断前后插入；源图移除后修正右侧索引。鼠标手柄与原生拖图片实测顺序一致，Esc或图库外松手不改序；触屏手柄禁用原生平移，其他区域仍可滚动。
- 验证：全量140项测试139通过、1项真实CLI校验按默认配置跳过；React构建和Node语法检查通过。隔离Chrome真实鼠标验证关键词带入/独立修改、类目搜索迟到响应/选择/关闭、橙线坐标/前后插入/原生拖拽/取消，0页面异常、0真实写入。首次颜色断言误写为 #ff6600，修正为项目实际主题色 #ff6200 后通过。截图、报告与真实只读查询摘要在 `/tmp/lsou-editor-detail-qa/`。
- 完成范围：更新本地静态资源，不重启8787以保留其发布历史，不刷新用户原页。没有Prompt变更、真实发品、重打ZIP或提交Git。
- 补充验收：多行图库目标行的竖线坐标、末尾插入和Chrome触屏事件下的手柄排序通过；缺关键词提示经实际参考映射字段生成后显示，手动补词后隐藏。最终diff检查通过，8787仍为原PID75594。

## 2026-09-15 复制上传保护与中文质量分结果

- 开始：用户指出未取得URL前复制会产生临时地址副本，并要求发布后的分数更显眼、显示中文扣分原因。只读当前6条历史回执，均为草稿保存成功、3.9分、lowScore=true、扣分项title_word_miss_core_error。核对当前已安装平台插件的评分规则，确认其含义为“标题缺少核心词”；评分规则包含加分项，与发布文档的满分写法不同，界面因此显示原始数值和“分”，不臆定满分或达标线。
- 复制：列表禁用态、duplicatePublishProduct、纯函数clonePublishProductDraft三处共用图片就绪判断；等待/读取/上传/失败及旧blob均阻断，成功或删除失败图片后恢复。成功副本沿用远程图和顺序，清空上传记录，避免复制File对象、被释放的预览或重复上传。零图片本地草稿仍可复制。
- 质量展示：前端整理平台状态，已知编码按平台对照表翻译，中文原因保留，未知编码用中文解释并保留可展开原文。完成弹窗、历史明细和编辑器共用大号质量分卡片，低分提示与真实发布成功/失败分开；缺分不显示0，也不标为达标。显示全部接收到的扣分项，未修改服务端原有8条归一化上限。轮询仅更新结果区域，相同内容不替换DOM，保留正在填写的字段、展开状态和焦点。
- 验证：新增2项纯函数测试，覆盖上传各阶段/失败/旧blob/URL复用/副本无上传任务及中文映射/未知原文/0分/缺分/平台布尔标记；全量142项，141通过、1项真实CLI校验默认跳过。React构建通过。隔离Chrome使用真实历史评分字段，验证大号3.9分和中文原因、4条原因全部显示、5.8分不截断、缺分/0分、原文展开、HTML转义、窄屏及轮询保留编辑；模拟两图上传的部分成功、失败、重试、复制复用URL且没有额外上传、删除旧blob后恢复。0页面异常、0真实写入。初次QA选择器写错导致等待超时，改用当前DOM的标题与文件输入选择器后通过；证据 `/tmp/lsou-quality-qa/`。
- 交付：修改前端、既有测试与上下文日志；保留8787服务及历史，不刷新用户原页面，不重新发布商品、上传真实图片、打包ZIP或提交Git。没有修改Prompt或Skill，不涉及运营SOP方法变更。

## 2026-09-15 移除产品发布顶部重复操作

- 用户要求删除顶部“发布新产品”和“批量发布已选”，询问图片文件夹导入含义。核对当前分组逻辑：图片按所在子文件夹名称归组，创建本地商品、以文件夹名作为初始标题并上传图片，每组最多6张；之后仍需补齐资料并确认保存/发布。
- 删除public/index.html两个顶部按钮，同步移除app.js的点击绑定、数量、禁用状态与提示更新，避免缺失DOM导致页面初始化失败。保留左侧空态/列表新建入口、底部批量操作及文件夹导入；导入按钮补充简短用途提示。
- 验证：Node语法、React构建和diff检查通过。隔离Chrome验证初始空态、左侧创建商品、列表再次新建与返回、选择数量/底部按钮状态、队列刷新以及文件夹选择器打开；0页面异常、0真实写入。截图和验收脚本在 `/tmp/lsou-toolbar-qa/`；此次仅低影响入口删除，不新增单元测试或重跑全量业务测试。
- 本地静态资源已更新；不重启服务、不刷新用户原页，不改Prompt、不打包或提交Git。

## 2026-09-15 移除文件夹导入、调整发布历史及刷新入口

- 开始：用户要求彻底移除“导入图片文件夹”、移动“发布历史”并删除重复的“更新店铺资料”。先确认全局“刷新数据”已按当前页面路由到发布参考资料刷新，因此沿用该业务链路。
- 实现：删除文件夹按钮、隐藏选择器、点击/文件变更绑定、左侧拖入建品事件及handlePublishFolderFiles函数；移除对应拖入高亮样式。普通商品图片上传和图片排序保持原逻辑。“发布历史”移到顶部状态栏右侧，窄屏靠右。缓存状态渲染解除对旧按钮的依赖，初次读取失败提示改为使用右上角刷新。
- 刷新：btnReload统一在请求期间禁用、finally恢复；发布页仅刷新参考资料，其他页仍刷新经营缓存。历史和当前商品编辑不因刷新被清空。
- 验证：Node语法、React构建及git diff --check通过。隔离Chrome覆盖五个已移除DOM入口不残留、文件夹处理函数/面板监听移除、历史明细及中文3.9分扣分项、正常图片选择器打开、全局刷新加载/成功/失败/重试、标题/图片/关键词/规格/选择状态及历史保留、900px/600px状态栏布局；0页面异常、0真实写入。截图及报告在 `/tmp/lsou-toolbar-final-qa/`，构建日志 `/tmp/lsou-toolbar-final-build.log`。本次为入口精简，未新增单元测试或重跑全量业务测试。
- 完成：前端静态资源已更新；保留8787服务及其进程内历史，不刷新用户原标签页；未改Prompt、真实上传或发布、打包ZIP或提交Git。

## 2026-09-15 顶部统计移除与草稿箱查看入口

- 开始：用户同意移除顶部四项流程统计，并询问草稿箱查看能力。删除统计容器、函数、四处调用和响应式样式；商品检查逻辑继续由原表格/底部计算，执行进度仍按原队列展示。
- 接口核验：最初全局WorkCTL帮助未加载当前空间的动态能力，随后使用desktop/runtime.cjs的resolveAccioEnvironment解析活动账号与安装版本，未输出凭据。当前schema确认search按商品号查询、list-information支持draft组件读取、list只有商品号/名称参数。真实保存回执样本的search结果为空；修正componentList的JSON数组格式与平台组件名称后，draft详情返回Record does not exist。无条件list查询未完成，已终止本轮专属子进程；不触碰8787或其他业务进程，不把读取失败当成空草稿箱。
- 入口：从当前安装国际站插件url-direct-publish.md提取官方完整草稿箱地址，新增“查看草稿箱”外链与新窗口标记，target=_blank、rel=noopener noreferrer；与发布历史置于缓存提示同一行。此版只提供官方后台入口，未声称有插件内完整草稿清单或重新校验每条历史商品存在性。
- 验证：Node语法、React构建、git diff --check通过。隔离Chrome覆盖统计节点/函数完全移除、输入和运行→完成进度、历史中文质量分、普通上传选择器、统一刷新成功/失败/重试、资料与历史保留；草稿箱链接与官方文档完全匹配且新开标签，目标页面由测试替身响应，0页面异常、0平台写入。1375/900/600px工具行检查通过，截图与报告在/tmp/lsou-drafts-qa/。未验证官方站点登录后的列表或Tauri外链，未增加低影响移除的单元测试。
- 完成：前端静态资源和构建已更新，主服务不重启、用户原页面不刷新，未改Prompt、上传/发布商品、重打ZIP或提交Git。

## 2026-09-15 撤回错误的草稿箱外链方案并核验WorkCTL

- 开始：用户明确纠正“查看草稿箱”必须走WorkCTL。承认此前跳转官网方案偏离需求，删除外链HTML与CSS，保留统计移除及原工作区。
- 发现：使用当前Accio运行环境的WorkCTL执行schema搜索draft/sketch/草稿/search，并检查完整ICBU命令目录、product list/search完整输入与RPC映射。cache status提示stale后，执行仅当前分区的cache refresh并重新查询草稿命令，仍未发现整箱列表接口。仅查看文档/Schema及执行list/search/list-information，不运行任何编辑、提交或发布命令；目录里某些写工具误标mutating=false，未据此执行。
- 根因修正：上一轮list无业务参数时等stdin而未调用完成；本轮临时查询器明确结束stdin、传入{}的JSON文件，使用独立进程组和30秒硬超时，避免残留子进程。无条件list与按真实标题list均成功返回空数组。另一条真实保存回执的search为data=null，list-information queryType=draft返回Record does not exist；不能据此推断整箱没有商品或过去保存失败。
- 验证：Node语法、React构建、隔离Chrome验证外链/统计移除、发布历史与中文质量分、当前商品创建/编辑、普通文件选择、刷新加载/成功/失败/重试、资料与历史保留及窄屏；不访问官网、不触发平台写入。证据/tmp/lsou-workctl-drafts-qa/。
- 状态：外链方案已撤回，通过WorkCTL读取完整草稿箱尚未接通，当前阻碍为未发现可验证的全量/分页草稿查询能力。没有用本地保存记录、空响应或模拟数据替代真实草稿箱。未重启8787、刷新用户页面、改后端、打包或提交Git。

## 2026-09-15 修复 M5 参考商品关键词属性漏带

- 开始：按用户截图定位当前账号的 M5 手环，读取现有参考接口、WorkCTL原始list-information trunk和query-product-by-id。标准关键词字段为空，但该商品的Keywords自定义属性包含“M5 Smart Watch Band”；模板texts和旧缓存已有同一文本。query-product-by-id的描述还包含其他推荐商品关键词，因此不能无差别递归搜词。
- 修复：参考映射优先保留标准关键词，标准字段无值时严格匹配当前商品模板内的Keyword/Keywords/关键词属性，兼容“Keywords::”双冒号。来源标为reference-attribute并在编辑器说明；不会改造标题或抽取描述来填词，也不需要刷新店铺缓存。复制沿用原深拷贝，编辑副本不影响来源。
- 验证：新增2项行为回归，覆盖线上双冒号、标准字段优先、复制隔离、去重、5词/40字符上限、空属性/非文本/标题/多行描述/推荐商品排除。首次空双冒号测试发现正则回溯会把冒号当值，收紧值的首字符约束后7项辅助测试全部通过。Node语法、React构建、git diff --check通过。
- 浏览器：隔离Chrome通过真实“选择参考商品→使用所选商品创建→复制同类→编辑副本”流程，回放刚读取的该商品响应；确认关键词及来源说明可见、标准字段优先、来源确实为空时继续显示补充提示，0页面异常、0平台写入。图片为只读显示，类目Schema为测试替身，未声称验证真实发布。截图和报告在/tmp/lsou-m5-keywords-qa/。
- 完成：8787静态资源已返回新版关键词读取逻辑，原PID75594继续运行；保留用户原页未保存内容和内存历史。未改Prompt、重启服务、发布商品、重打ZIP或提交Git。

## 2026-09-15 历史结果点击继续编辑

- 开始：用户要求点击历史结果里的商品直接加载编辑。核对发现原发品队列仅有摘要入口，写入仍是publish-from-json新建；因此接到已有商品编辑流程，不能恢复成新商品再发。
- 只读实查：从当前6条成功草稿回执取样，WorkCTL list-information的draft/trunk/draftFirst均返回success=true但data为Record does not exist.。当前已安装平台编辑文档指定draftFirst，写入应走product-edit-draft-basic-info/detail再submit-draft；没有执行写调用，也不把缺失记录解释为全店草稿箱为空。
- 实现：server新增按历史jobId读取的同源JSON路由，从平台回执确定原商品号，拒绝客户端额外目标编号、失败任务和失效历史。operations先校验WorkCTL读取结果，再发放短期编辑引用及当前资料。前端历史标题和“继续编辑”可点击，复用原商品弹窗；保存仅提交改动的受支持字段，保持原ID。属性/价格/物流沿用只读显示，不声称全字段可写。
- 交互：加载时禁用重复点击，错误保留在原条目，关闭历史后不响应迟到结果；编辑弹窗关闭后原明细仍在，当前待发布列表和未保存字段不变。弹窗新增本次编辑任务，可单独确认提交已保存草稿，打开历史不会产生写任务。
- 历史恢复：完成回执按账号哈希、0600临时文件原子落盘，不存素材/凭据，不恢复在途写任务；恢复失败记录不允许重试。迁移先备份原6条回执，并用原服务与当前账号专属目录缓存的精确时间核对账号，再把75594更新为58637，验证6条完整保留、0条重放。首次通过ps检查原进程时取不到启动后才注入的Accio变量，检查主动中止且未重启，随后改用上述缓存证据核对；未声称账号切换。
- 验证：新增运营模块与真实HTTP回归覆盖原商品号保存、同名商品分离、草稿优先读取、记录不存在/空/异常结构拒绝、伪造ID/跨源拒绝、终态恢复与不重放。全量146项测试145通过、1项真实CLI跳过；Node语法、React构建和diff检查通过。隔离Chrome用受控API验证完整点击/加载/修改/确认保存链路、独立提交按钮、读取失败、关闭后的迟到响应及本地工作区保留。首次浏览器测试把首页其他运营查询误算为重复商品读取，收窄到product-info后通过，0页面异常、0真实写入。
- 交付边界：入口与原商品编辑链路已实现，本机后端已更新；这批历史的真实商品资料读取仍受WorkCTL“记录不存在”阻碍，不能声称真实草稿已编辑成功。保留用户原页面，未改Prompt、提交Git、重打ZIP或执行真实商品更新/发布。证据/tmp/lsou-history-edit-qa/。

## 2026-09-15 补齐新发品的商品详情

- 开始：用户确认原商品关键词确实为空后，发现发品缺少详情，并明确要求加入。阅读当前源码、上下文、平台结构化发品示例及实时详情编辑 Schema，确认当前接口支持商详图、公司介绍/图片和 FAQ；未修改 Prompt，无需新增 SOP 方法映射。
- 实查：WorkCTL 读取当前 AOKE 参考商品，得到 10 张商详图、18 张公司图片、1321 字公司介绍、8 条问答及卖点。不同图片分组内的 imageIndex 会重复；新读取沿用原数组顺序，不能像旧图库那样按 index 全局排序。原 HTML/装修结构不作为执行内容或混入其他商品的推荐资料。
- 实现：前端新增独立详情数据、图注/公司介绍/问答输入、详情上传/重试/删除/排序和内容预览，复制同类保持独立对象。上传沿用 CDN 接口，section 固定所属分组与商品；统一阻止未成功上传图片的复制/草稿/发布，删除后迟到回调不恢复图片。预览转义用户/来源文字，最终平台装修样式与本地内容预览分开表述。主图与详情图分别管理。
- 后端：新增按需结构化详情查询，使用独立版本缓存键补齐旧缓存，不全店重读；成功资料落盘，失败记录安全日志并提示重试。选择已有商品和参考链接导入均接入。素材提交映射 newImageUrl、imageText、顺序号、公司介绍与问答，移除原页面和分组标识。服务端拒绝临时地址、非法结构、半条问答和超出整体 50KB 的正文，不静默删除/截断；100 项为工作区资源保护，不宣称平台详情限制。
- 验证：新增详情映射/复制隔离、非法输入/空值校验回归，真实 HTTP + 假 WorkCTL 覆盖分组重复下标保序、缓存恢复和最终 detail material（7 张详情图，分别走草稿及发布）及无效输入拒绝。首轮全量测试的 5 个失败来自原 VM 测试装载器未注入新纯函数依赖，补齐真实依赖后 148 项中 147 通过、1 默认跳过。单独启用当前安装 WorkCTL 的 validate-only，7 项通过，新增10图详情 material 被校验且内容未丢失；无平台写入。
- 浏览器：使用 webapp-testing 的隔离 Chrome 回放刚读取的真实详情，覆盖 10/18/8 数量、主图6张保持、图片手柄橙色竖线和顺序、修改图注/公司/问答、删除与复制隔离、HTML 字符只作为文本、上传失败→重试、等待时禁用、删除后迟到响应不回插、草稿/正式发布确认与 JSON 字段、600px布局。首次颜色断言误用了 #ff6600，改为核对项目实际 --acc=#ff6200；补充图像解码等待后截图发现原生 dialog 受全局 margin:0 影响靠左，增加 margin:auto 并验证居中。0 页面异常、0 实际发布写入；API替身和平台实读证据分开保存。
- 本地：确认队列无在途任务、账号缓存一致并备份历史后，将 8787 服务从 PID58637 更新为 PID65793，6 条历史完整保留、0 条重放。新端点真实读回所有详情，首读3899ms，重复3ms，完整响应一致且图片顺序匹配上游。静态资源与 React 构建已更新；用户原标签页未刷新，未打包正式插件或提交 Git。证据和脚本在 /tmp/lsou-detail-qa/。


## 2026-09-15 规格空字段收起与名称来源补齐

- 开始：用户要求已读取值显示在输入框、源数据空项不默认展示。沿用已有React/Node技术栈，聚焦当前商品规格，读取上下文及前后端渲染/序列化/参考缓存代码；未修改Prompt。
- 数据依据：两条真实只读WorkCTL查询显示M5有5个颜色、4档阶梯价，独立stock/unitPrice和skuCode未返回可用值；完整商品SKU销售属性名为color，组件接口返回p-191288010。不能据空接口值断言平台后台未填写，更不能把内部outerId当成商家编码。
- 实现：有值的编码/单价/库存直接显示，空项收于“补充规格信息”。WeakMap维护SKU各自的展开及已编辑字段，清空后不突然消失，0不误判为空；所填数据继续使用原序列化，复制不共享界面状态。库存缺项在入口和正式发布校验中提示，草稿仍可保存空交易字段。必需规格属性仍直接可编辑。
- 读取：占位名才补读query-product-by-id，精确核对目标productId，仅提取当前SKU销售属性的propertyId/propertyText；同ID冲突不任取，正常业务名称不覆盖。按账号缓存名称映射，旧参考缓存直接升级，无需全店刷新。失败不缓存并记安全日志，不把原商品号/内部SKU编号写入公开响应。
- 验证：新增纯转换用例覆盖跨商品/推荐内容/冲突拒绝、原对象不变、零库存/空值保留及不重复查询；HTTP集成以占位名称真实经过补读回填并验证跨重启离线缓存。全量150项中149通过、1项真实CLI默认跳过（未改发布material，无需新增平台调用）。React构建/语法/diff检查通过。
- 浏览器：webapp-testing隔离Chrome回放真实M5结构，15个空输入默认隐藏；键盘展开、填写编码/价格/0库存、重绘/清空输入保留、添加删除规格、复制深拷贝与各自折叠状态、必需库存仍阻断、600px无溢出全部通过，0页面异常/真实写入。实拍截图位于/tmp/lsou-sku-fields-qa/。初次CSS补丁因文件为压缩单行未匹配，未改变原样式，随后在文件末添加局部样式并通过构建。
- 完成：核对活动账号缓存精确时间和空闲队列、备份并校验6条历史后，本地服务65793→69804，历史完整恢复且0重放。当前M5真实端点5个名称均为color，空数值未变化；首次4193ms、重复3ms，完整响应一致。用户原页未刷新、无实际上传/保存/发布；未提交Git或重打ZIP。


## 2026-09-15 右侧编辑区克制的视觉细化

- 开始：用户认为右侧编辑器过于朴素，随后明确“先轻轻美化，不要用力过猛”。阅读polish及其要求的frontend-design技能、当前CSS/渲染和上下文，决定保留现有布局、字体、橙色和全部业务，只细化密度及控件；未修改Prompt或后端。
- 实现：局部样式全部限定#publishEditor，字号小幅提升、主要表单32px、7px圆角，边框/标签/辅助按钮与留白统一，焦点沿用橙色。关键词/多选继续原位操作，未添加额外卡片导航或装饰动画。SKU删除属性变为行末小图标，保留原数据属性/禁用规则并补充aria-label/title；减少旧的大块按钮造成的视觉干扰。窄屏包装输入两列、底部按钮保留原重排。
- 验证：使用webapp-testing方法在隔离Chrome回放当前M5完整参考和真实缓存Schema；前后序列化精确相同、左侧卡片尺寸相同。编辑标题/关键词、焦点、类目搜索展开、原产地搜索/选择、主图手柄与橙色插入线、规格新增/删除属性/空库存展开填写、详情预览及1375/900/600px无溢出通过，0页面错误/平台写入。第一轮窄屏断言使用单元素locator误匹配5条SKU导致strict模式报错，改为逐项检查后通过，未误判为产品故障。截图经视觉检查，Node语法、React构建、diff检查通过；本轮未新增低价值单元测试。
- 完成：静态资源与React产物已更新；本地后端保持运行，用户正在编辑的原标签页未刷新。未改素材提交合同、提交Git、打包正式Tauri/ZIP或执行真实商品操作。证据与前后截图在/tmp/lsou-editor-polish-qa/。


## 2026-09-15 常见问答保持轻量的样式优化

- 开始：用户认可编辑器轻量美化，并希望常见问答区域也优化。沿用已阅读的polish/frontend-design及webapp-testing方法，先核对当前详情渲染、样式和事件；保持既有布局和数据合同，不添加折叠等额外流程、不改Prompt。
- 实现：FAQ专属标题与头部样式类，轻边框/8px圆角/小幅留白，低强调删除按钮保留文字和可访问名称。问题切换为rows=2的textarea，长句可以换行并手动调整高度；问题/回答字体均继承页面，回答行高1.7。所有内容继续esc转义，沿用原data-publish-faq-field输入和序列化。新增后focus新问题以定位条目。
- 验证：真实M5读取快照的4条问答经过隔离Chrome原创建流程回放，初始提交内容与原始FAQ完全一致；多行中英文和HTML字符保留、focus样式、添加定位、半条FAQ校验、删除重排与空态、复制独立和预览通过。1728/1375/600px无FAQ溢出、0页面异常/实际写请求。测试脚本首次导入既有fixture时split匹配到嵌套字符串导致SyntaxError，改成按独立with行rsplit后通过；产品无该错误。Node语法、React构建、diff检查通过，未为此可逆UI修改新增单元测试。
- 完成：静态资源及React构建已更新，后端和用户原页面保持运行，未实际上传/保存/发布或打包ZIP/提交Git。截图与验证脚本在/tmp/lsou-faq-polish-qa/。

## 2026-09-15 移除商品规则匹配横幅

- 开始：用户明确要求删除右侧编辑器的绿色规则匹配提示；读取当前渲染、样式和上下文后移除对应HTML、仅供横幅使用的变量和CSS。
- 验证：Node语法、React构建与diff检查通过。隔离Chrome确认横幅和文字消失，标题可编辑、详情序列化保持一致；账号上下文未加载时仍显示原阻断界面并拒绝提交。0页面错误、0真实平台写入，证据在/tmp/lsou-remove-banner-qa/。
- 完成：仅更新静态资源，未刷新用户正在编辑的原页、重启服务、提交Git或打包ZIP；未修改实际规则读取和发布校验。


## 2026-09-15 接入图片图集并上移批量操作栏

- 开始：用户确认接入已查明的imageSetId，工作中追加批量操作栏移到顶部的标注。读取源码、上下文和当前WorkCTL资料；沿用既有技术栈与轻量风格，未改Prompt。字段依据来自实时详情Schema、官方sample_stripped示例及本账号完整商品mediaInfoDTO，不将样例1001/2001误用作固定图集号。
- 数据：图片补充imageSetId；参考读取缓存升为detail:v2，避免v1缺失分组资料持续复用。共用工具校验ID/选择结构、保留未识别源ID，按勾选筛图，服务端各图集独立imageIndex并传入真实material。仅保留源图集/已核实平台分类，不发明自定义图集创建接口。
- 界面：勾选图集启用上传区，取消勾选保留本地内容且明确不随本次提交；图集内排序、逐图移组、说明和上传重试沿用原流程。上传迟到结果只替换URL不覆盖分组，复制独立保留未勾选图片。预览按当前分组展示，确认数量使用相同筛选结果。移动现有批量操作栏DOM到顶部，发布历史并列，刷新失败提示保留，窄屏重排。
- 验证：全量152项测试151通过1默认跳过；当前安装WorkCTL的validate-only单独启用后10项通过，material中图集编号/每组排序保留，未执行平台写入。HTTP集成覆盖参考ID带回、跨重启缓存和最终草稿/发布material。React构建/语法/diff检查通过。
- 浏览器：隔离Chrome回放真实AOKE的10张商详图、18张公司图和分组，验证勾选恢复、准确预览数量、复制隔离、橙色拖动提示与组内排序、移入未选图集自动勾选、上传失败重试及等待时移组、未知/无分组、安全图注、提交选择、顶部栏位置和1375/600px无溢出，0页面错误/实际写入。首次断言将人工追加未知图集误认为应自动纳入已显式选择列表；修正为主动勾选该测试图集后通过，未更改排除语义。图像解码后截图已视觉检查。
- 完成：核对账号缓存与空闲队列、备份并核验历史后，8787服务69804→76207，6条历史保留且0重放。真实参考接口补读图集首读1790ms、再次3ms响应一致。用户原标签页未刷新，未提交Git或打包正式插件。证据和截图 `/tmp/lsou-image-groups-qa/`。


## 2026-09-15 删除无可选项的队列模式控件

- 开始：用户要求移除顶部“发布队列模式”。阅读当前HTML/CSS、引用与上下文，确认只有固定sequential选项且没有JS依赖。
- 实现：删除label/select及专用样式，窄屏历史按钮由第4行改到第3行；保留实际队列策略和发布确认流程。
- 验证与完成：React构建、Node语法、diff检查通过。隔离Chrome验证选择器不存在、1375/600px布局和发布历史/批量草稿确认正常，0页面异常/真实写入。未刷新用户原标签页、重启后端、提交Git或打包；证据 `/tmp/lsou-remove-queue-policy-qa/`。


## 2026-09-16 替换 ALI 运营顾问界面并保留完整产品发布

- 开始：按用户批准的八页设计与参考 HTML/业务说明实施，先将旧工程和源码分别归档到 `/Users/garden/YD/ReverseAccio/lsou-ui-backups/20260916-155623/`。
- 实现：八模块侧栏、业务二级导航、新规划/定位/基建/优爆品页面，补充真实业务员表现与店铺健康指标，保留经营趋势、排名等丰富图表。新规划与确认记录只存本地；没有增加或修改模型 Prompt。
- 产品发布：完整原版作为独立核心入口；原发布 HTML、style.css、publish-product-utils.js 与 server.js 对备份字节一致，app.js 仅新增运营顾问挂钩。React 切走再返回后 140 个发布控件的值与勾选状态一致，参考商品图片、商详、公司图片和 FAQ 均可见；未执行保存平台草稿或发布。
- 调试：修正商品分析统计月份契约、异步旧请求覆盖、窄屏/折叠菜单尺寸。React 首次异源预览被原同源保护拒绝后改同源挂载验证，安全限制保持不变；临时预览文件与8790服务已清理。
- 验证与完成：全量155项测试154通过1默认跳过，React构建、语法、diff检查通过。九入口桌面与600px主要页面无外层横向溢出；测试本地任务已删除；浏览器视口已恢复。未提交Git、未重启业务后端、未构建正式插件ZIP。


## 2026-09-16 修正与已确认原型图不一致的问题

- 开始：用户明确要求按先前生成的图实施；承认上一轮只统一风格不足以视作按图还原。再次备份到 `/Users/garden/YD/ReverseAccio/lsou-ui-backups/before-image-match-20260916-162810.tar.gz`，原完整备份继续保留。
- 实现：重建八页专属布局和密集看板；规划目标/SOP/任务、定位矩阵、关键词表、投放组合图/诊断表、核心品跟进、商品漏斗/AI演示、线索分配和经营看板均按对应原图组织。支持本地筛选、关键词编辑、商品选择和演示导出。生成六件示例商品的图集。没有修改Prompt或业务后端。
- 边界：顶部显式标识设计预览和示例数据，原真实工作区可访问；图中AI对话和建议为演示，不声称真实生成或执行。产品发布原嵌套section与备份逐字一致，style.css、publish-product-utils.js、server.js逐字节一致。
- 修复：解决旧全局样式导致的控件过高、表格行距和窄屏grid内在宽度溢出；八页分别同视口对照原图，保留字体、图标、示例照片和小幅垂直间距差异。
- 验证：155项测试154通过1默认跳过；React构建与JS语法通过。1448×1086八页视觉检查，600px九入口无外层横向溢出；关键词筛选/编辑、商品搜索/选择、德国线索筛选与详情通过；React同源构建入口可进入完整原产品发布和新建方式选择，控制台无错误，未执行平台写入。临时构建页面和文件已清理，8787/8765服务未停止。未提交Git、未打包正式插件。


## 2026-09-16 按用户要求接入真实 WorkCTL

- 开始：用户指出新版为示例数据，要求通过WorkCTL接真实数据。读取现有服务/接口/时间合同，健康检查ready/authenticated，沿用Node后端与React，不修改Prompt。
- 实现：四个advisor脚本全部切换当前账号只读接口，保留新UI布局；移除固定示例店铺、储能词库、虚构客户/任务/AI回复/图表。真实数据缺项为空态，人工规划与平台事实分开。共享读取有超时/并发合并；重新挂载/切页隔离迟到响应。
- 核验：8月31个经营日合计询盘61、成交9、曝光125983，浏览器与HTTP确定性加总一致。真实关键词19条；商品分析837/846，明确非完整全店排名；访客总记录651，实际每页10，实测1/2页ID无重叠。投放计划39条可读，8月/9月直通车报表空，金额保持缺失不画假曲线。部分读取为当前账号真实缓存，页面标明时间与口径。
- 修复：真实长标题造成表格遮蔽/定位网格溢出，增加列宽、换行及内部滚动；访客API忽略20条参数，改为10条分页避免提前结束。产品发布HTML、后端和工具未修改。
- 验证与完成：158项Node测试157通过1默认跳过；新增共享读取失败可重试、并发合并、只读路径、缺值与完整月测试。构建/语法/diff通过；浏览器八页600px无文档横溢出，原生商品搜索GH30返回2条、选择详情更新，日/月切换生效，React真实看板与原发布入口正常、0控制台错误。临时React检查页清理，未重启服务、未执行发布/平台草稿/广告写操作、未提交Git或打包。


## 2026-09-16 移除顶部两项辅助信息

- 按用户浏览器批注，删除advisor顶栏“更多业务工具”按钮和WorkCTL连接标签，同时移除只用于更新该标签的health请求，避免删除DOM后异步报错。其余模块数据查询与发布逻辑未改。
- JS语法、React构建和diff检查通过。


## 2026-09-16 统一刷新与界面文案

- 按用户要求清理八页、帮助及原发布提示中的工具名称，改为平台/店铺业务文案；刷新集中到顶部，八页共享请求状态，禁用重复刷新并保留日期及本页人工记录。删除卡片内重复读取按钮，窄屏保留顶部刷新。
- 验证：八页浏览器检查均无工具名称且只有一个顶部刷新入口；基建点击刷新后真实词表正常返回。共享读取3项测试、JS语法、React构建和diff检查通过。


## 2026-09-16 看板指标双版本切换

- 按用户对经营指标区域的批注新增简洁版/详细版；简洁版保留现有六项，详细版恢复原十二项及行业均值、优秀值对比。复用原指标定义与当前真实数据，不增加平台调用，不改产品发布。缺失广告花费明确显示未接入。
- 验证：JavaScript语法、React构建和diff检查通过；浏览器切换分别显示六项/十二项，顶部刷新后保留详细版；600px视口无文档横向溢出，已恢复桌面视口。未执行平台写入、Git提交或正式插件打包。

## 2026-09-16 压缩详细版指标区域

- 按用户反馈将十二项大卡片改为桌面六列两行紧凑面板，压缩字号、内边距与间距；保留行业均值/优秀值，统计口径和比较说明通过悬停查看。
- JS语法、React构建、diff检查通过；浏览器确认十二项紧凑展示，首屏可完整看到趋势与渠道图。

## 2026-09-16 统一简洁版指标图标

- 简洁版按指标键复用详细版图标定义，TM访客使用相同访客图标；读取态也保持一致。未改变数据与布局。
- JS语法、React构建及diff检查通过。

## 2026-09-16 简洁版增加行业参考

- 六项指标下方新增行业均值与行业优秀，使用同周期、同指标的真实同行字段，缺项保留破折号；以两行小字替代原脚注，口径移至悬停提示。
- JS语法、React构建、diff检查通过；浏览器核验六项同行值正常显示，首屏图表保持可见。

## 2026-09-16 移除运营规划读取提示卡

- 按页面批注移除运营规划顶部成功读取状态、来源及读取时间卡片，经营目标直接衔接标题。查询失败仍用简短行内信息提示。
- JS语法、React构建与diff检查通过。

## 2026-09-16 界面整合提交前验证

- 全量158项测试初跑156通过、1失败、1跳过；失败为指标定义在无window测试环境加载，已增加环境判断。对应overview-todo集成测试重跑通过，React构建和diff检查通过。

## 2026-09-16 产品发布界面一致性

- 按用户批注移除辅助工作区，提取共用顶栏创建函数，产品发布沿用运营顾问外壳；保留原发布编辑器、队列与提交逻辑，仅调整外层样式。
- JS语法、React构建、共享读取/导航相关测试及diff检查通过；浏览器检查统一顶侧栏、新建方式选择，以及数据看板往返后新建界面状态保留。未保存草稿或执行平台发布。

## 2026-09-16 看板视觉舒适度调整

- 按三处批注统一健康度图标，业务员表增加姓名首字母标识和行距、数字右对齐，产品榜单增加名次及两行标题。全局恢复来搜logo、改称运营顾问，背景与表头采用轻暖橙色，保留紧凑KPI。
- JS语法、React构建及diff检查通过；浏览器检查真实列表、Logo、暖色背景和八项健康度图标，未改数据查询或发布业务逻辑。

- 2026-09-16 按反馈进一步降低暖色背景饱和度，主背景调整为接近白色的 #fdfbf9，表头和健康度卡同步淡化；保持品牌橙色点缀。构建及diff检查通过。

## 2026-09-16 移除各页读取元信息

- 移除看板与商机的顶部数据读取行、营销定位和运营基建成功读取卡、推广及商品页来源行和广告图缓存时间。保留日期选择及必要的失败/缺项提示。
- JS语法、React构建与diff检查通过。

## 2026-09-16 恢复Workflow/Chatflow用户界面

- 开始：按用户要求恢复原型七页报告与追问体验，读取原业务交互说明、现有顾问接口及国际站运营SOP。新增30个主题入口，打开报告/问顾问抽屉，支持真实分析、真实报告下载、独立会话和错误展示。
- 方法依据：运营规划映射SOP店铺诊断与建设顺序；定位映射市场/产品/公司定位及装修；基建映射选品/关键词/高质量发品；推广映射付费广告与品广；核心品映射优爆品打造；分析映射店铺诊断与产品优化；商机映射询盘质量跟进和RFQ营销。每页将所需资料、判断动作和交付要求作为limitations传入现有分析，不作为事实证据，不套未核实现行阈值。大促资格、外部背调及缺失询盘正文不编造，未修改Dify节点或创建专用流程。
- 验证：JS语法、React构建、7项共享读取/导航测试和diff通过；浏览器核对七页30个入口、报告/聊天切换。已真实执行新店规划主题分析，返回4条建议及复查条件，下载按钮可用。实测后端聊天未配置，输入框与快捷问题可见、发送禁用并明确提示；未声称完成真实对话验证。未执行任何商品或广告写操作。

- 2026-09-16 按侧栏批注将桌面导航文字从16px降为14px、图标22px降为19px，常态400/选中500字重，保留点击区域。构建与diff检查通过。

## 2026-09-16 业务页表格视觉统一

- 按连续批注将营销定位、基建关键词、投放计划、核心商品/已选商品、诊断依据及访客/国家画像统一为白底、近白暖色表头和柔和分隔线；适度增加单元格行距，定位图标、步骤圆点和访客说明同步去除冷灰蓝底。
- React构建与diff检查通过；浏览器检查定位、诊断及访客视觉，核对基建、推广、核心品和商机表头实际颜色一致且页面无横向溢出。数据与操作逻辑未调整。

## 2026-09-16 商机页直接展示RFQ

- 开始：按批注将levelTag文案改为买家等级；读取旧RFQ搜索实现及服务端脱敏结构，替换空泛的跟进资料提示卡。
- 完成：直接嵌入站内采购需求搜索、五条需求卡及采购说明折叠区域，展示国家、数量、时间和报价名额。默认词来自店铺真实搜索词；支持换词、空结果和错误提示，请求版本阻止旧词结果覆盖新搜索。不执行报价或联系买家。
- 验证：JS语法、React构建、advisor-live/release-data测试与diff检查通过；浏览器真实读取smart watch的5条需求并展开首条，核对采购说明、已报价8与剩余名额2。未改后端脱敏策略。

## 2026-09-16 业务入口与固定结果区

- 开始：对照原HTML左侧业务列表和既有七页结构，移除独立运营服务栏，把30个主题入口整合到定位菜单、基建步骤和对应业务卡片。点击直接等待真实数据就绪并运行既有分析接口，右侧展示固定结构的结论、依据、建议及复查内容；保留下载与对话入口。
- 固定页面视口高度，左右卡片独立滚动；定位原数据与任务/历史折叠保留，未改产品发布。修复长报告把网格行撑高的问题，明确限定网格行和卡片高度。
- 验证：JS语法、React构建、9项advisor-live/release-data测试及diff检查通过。七页入口数为4/5/4/3/3/4/7；浏览器实测市场与客群定位自动分析成功、结果下载可用，报告内容2757px而容器719px，页面935px保持视口935px，截图确认卡片内部滚动。对话后端仍未配置，未声称完成问答链路。本次未修改提示词、未创建专用Dify节点、未执行平台写入。

## 2026-09-16 按原型恢复固定业务结果板块

- 开始：读取原HTML市场与客群定位等业务输出定义，复核国际站运营SOP全文。用四个固定维度替代通用长报告，市场定位采用目标市场分析/客群画像/痛点分析/决策链，其他29个主题分别配置业务标题。
- Prompt映射：市场/客户定位对应国家、搜索词和客户需求证据；公司/装修对应实力、定制、品质、服务和素材；产品/基建对应选品、关键词、发品；推广对应计划、商品、地域与词；核心品/诊断对应层级、表现、动作、复查；商机对应需求、匹配和跟进。将固定标题、70字依据、最多两条55字动作写入现有Workflow输入的输出约定。继续使用真实evidence_ids，缺失角色、客群、询盘原文或公司能力不补造；没有套用SOP阈值、预算或时效承诺。未修改远端DSL或后端schema。
- 完成：短结论和动作卡片化，依据/复查与整体判断默认折叠，真实国家占比直接呈现；旧格式诊断只放补充分析，不按顺序误配业务维度。增加展示缓存版本隔离旧长报告。
- 验证：JS语法、前端构建、8项渲染与共享读取测试、diff检查通过。真实Workflow返回“目标市场分析”短结论与两条动作；客群、痛点和决策链因缺证据显示待补资料。浏览器确认四块卡片、真实美国20.3%/印度11.7%/科特迪瓦6.0%，固定719px结果区，页面935px与视口一致。未将缺失客群生成虚构画像。

## 2026-09-16 分析积分显式触发

- 移除业务项open后的自动生成调用；新报告与重新分析按钮均标明消耗积分，显示结算提示。准备资料与分析状态分开，未开始时四块卡片等待用户点击。问顾问发送区补充积分说明。
- 保留忙碌、数据就绪与配置检查，阻止重复点击并发调用。构建、语法、diff检查及4项报告测试通过；新增请求测试确认切换业务不会调用analysis，主动开始调用一次且重复点击不并发。
- 浏览器核验已有公司定位显示重新分析提示、未生成批量标题显示开始分析按钮和待开始状态。未点击付费生成按钮验证，未发起新的模型分析。

## 2026-09-16 合并Workflow与Chatflow交互

- 删除报告/问顾问双页签，固定Workflow结果下直接接续讨论，保留分析与发送积分提示。只在用户发送问题时触发Chatflow，自动使用已展示报告对应快照；后端原有analysis_result包含报告summary/tasks与事实上下文，不要求用户复制。
- 重新生成报告后重置旧会话；区别问答忙碌与生成分析状态。未修改Prompt与远端配置。
- 构建、JS语法、22项报告及顾问测试、diff检查通过；浏览器验证0个旧页签、已有报告下显示继续讨论。当前Chatflow仍未配置，因此未执行真实问答或产生额外分析调用。

## 2026-09-16 营销定位默认展示平台资料

- 定位菜单切换不再打开顾问结果区，先显示已有国家画像、搜索词和商品资料。资料区增加显式开始分析按钮，点击才准备context并调用Workflow；切回菜单会清除旧结果区显示归属，防止迟到报告覆盖资料页。
- 产品定位直接呈现商品；公司/装修待确认说明下也保留当前商品资料，不推断未返回的企业事实。构建、JS语法、4项报告测试及diff检查通过；浏览器检查市场与产品切换，真实数据可见且无分析卡自动出现。未点击付费分析按钮。

## 2026-09-16 丰富市场与客群真实图表

- 对照旧版市场机会查询，接入买家身份、来源渠道、行业国家、细分类目及需求场景；cateId来自当前店铺，不硬编码。默认展示六图一表：国家/渠道/关键词/国家商机/类目商机横条，买家身份环图，场景需求表。
- 区分店铺近30天画像、行业接口默认统计期和近90天场景；行业图显示返回日期。环图仅在占比近似完整时启用，缺失和负数不伪装为零；无数据/错误保留明确提示。等待级联查询后再准备用户显式触发的分析快照。
- 真实读取买家身份35.61%线下零售/33.40%线上零售/22.40%批发/8.60%制造，行业数据返回2026-09-14记录；未调用Workflow。构建、语法、7项图表和读取测试、diff通过；浏览器核验7卡片、环图与国家/类目/场景值、719px卡片内部滚动以及935px固定页面。

### 2026-09-17 公司定位真实资料与可视化
- 开始：核对现有公司资料、店铺装修工作区、只读能力目录与实际返回字段。沿用现有技术栈与只读数据通道，无提示词改动。
- 实现：读取 companyProfile 基础/扩展/出口/生产/认证/素材/产品分组字段，增加六块事实面板、9 个销售区域占比、证书类型计数、证书原件入口、产品系列及企业相册。当前返回 6 份证书、8 个产品系列、3 张公司照片、11 张展会照片。未将平台类目、257 国及16种语言选项库误用为企业覆盖。无模型自动调用。
- 异常边界：旧装修工作区附加的云端页面查询有超时记录，但公司资料成功；公司图表不依赖云端页面。企业自述/上传证书与核验结论区分，未提供资料留空态，图片地址限制 HTTPS，外部文本转义，PDF 不作为图片加载。
- 完成与验证：JS 语法、前端构建、6 项图表/付费分析行为测试及 git diff --check 通过。浏览器确认公司信息和占比真实渲染；页面高度 935px 等于视口，右卡 741px，内容 2566px 内部滚动。未执行付费 Workflow 或 Chatflow。

### 2026-09-17 公司定位排版收紧
- 按用户反馈，仅收紧公司资料面板：卡片内边距、卡片间距、指标上下留白、事实行距、标签和图表间距；保持原字号、完整资料与固定高度内部滚动。
- 验证：前端构建及 git diff --check 通过；浏览器确认新样式已生效，935px 页面未撑高，右侧保持内部滚动。纯 CSS 调整，未调用分析服务。

### 2026-09-17 公司优势与介绍固定高度
- 移除介绍折叠开关，默认显示公司优势与介绍，正文置于 180px 高的独立滚动区；完整文本保留，并支持键盘聚焦滚动。
- 验证：语法检查、前端构建及 diff 检查通过；浏览器确认正文可内部滚动（内容约897px），同排证书卡片不再被长文撑高。未调用付费分析。

### 2026-09-17 产品定位真实可视化
- 开始：核对既有商品分析聚合接口、前端统计口径及实际返回，复用当前只读数据链路，无新增模型或平台写调用。
- 完成：新增核心指标、商品分层条形图、流量四象限、带缩略图的询盘/曝光 TOP6、产品系列图库与待关注商品数量。数据按自然日/月读取；当前实测837/846件，明确为已返回范围。长排名内部滚动，页面不随内容拉长。
- 验证：JS语法、前端构建、6项既有图表/付费门控回归和diff检查通过；浏览器确认真实数字与图片渲染，页面935px与视口一致，右卡719px内部滚动。未执行付费Workflow或Chatflow。

### 2026-09-17 产品定位移除流量分组
- 按用户要求，产品定位默认资料页移除流量表现分组及其阈值说明；商品分层占整行，两张排名卡并排，避免留下空位。
- JS语法、前端构建及diff检查通过；浏览器确认卡片已移除。其他分析页面与数据接口保持原有行为。

### 2026-09-17 营销定位横向页签与数据/AI双栏
- 按用户要求将五项定位移到顶部横排；下方左侧约65%展示原真实资料，右侧约35%展示分析入口、Workflow结果和携带结果上下文的继续讨论。
- 迁移 ensureDrawer 挂载到 wfPositionAI，保留左栏资料可见；继续使用原付费门控、主题会话与异步重绘恢复机制。未更改提示词或后端。
- 验证：两份JS语法、前端构建、4项报告/积分门控测试及diff检查通过。浏览器确认顶部五项、公司资料左栏、AI右栏；1094px宽视口下左右约541/293px，页面935px未超出视口。未实际消耗积分测试模型。

### 2026-09-17 店铺与详情页装修资料面板
- 核对只读公司资料、素材结构与现有商品参考读取入口。用现有 storefront 聚合数据完成资料展示，不调用素材生成、装修发布或商品写接口。
- 店铺文案增加8系列/20商品/14图片/6证书素材指标、图片分类图、系列与商品图库、介绍、服务、企业实景。详情文案增加商品选择、主图/标题/价格/MOQ预览及公司公共素材。公司证书不归因到所选商品；规格等未返回字段不编造。
- 选择商品写入后续分析事实和独立缓存范围，避免不同商品混用报告；无提示词变更，无自动付费调用。
- 验证：两份JS语法、构建、6项图表及分析门控回归、diff检查通过。浏览器确认两页真实素材可见、切换第二件商品同步图片/标题/价格，页面935px等于视口。

### 2026-09-17 运营基建入口和三种词工具
- 修正步骤高亮一直停留第二项的问题，前三项实际切换对应Workflow准备区，第四项直接进入产品发布，不再打开批量发品规划。
- 替换两块提示为买家搜索词、行业场景热词、广告关键词三个选项；复用旧词工具只读端点与实际主营类目，导出随当前来源切换。
- 实测额外店铺关键词画像端点25秒后失败且无历史快照，未拿示例数据代替；选用实际成功返回的买家画像、行业场景和广告词池。广告返回100条，展示渠道、曝光/点击指数和点击率，不涉及预定/投放。
- 验证：构建、6项图表与积分门控回归及diff检查通过。浏览器逐项确认一键选品/整理关键词/批量标题标题切换、行业及广告词表真实渲染，以及第四步进入完整产品发布工作区；未触发付费生成、保存或发布。

### 2026-09-17 词表宽度和完整页展示
- 三种词表使用固定列宽和单行省略，悬停保留全文，原始数据和CSV导出保持完整。取消横向滚动；买家词表取消内部高度截断，其他词表扩至560px纵向浏览。
- 基建步骤压缩为横向四项，为买家搜索记录留出空间，不改变Workflow付费入口。
- 验证：JS语法、前端构建通过；浏览器1094×935下19条买家词全部可见（表格底部894px），行业15条、广告100条均有480px可用宽度且无水平溢出。未调用付费分析。

### 2026-09-17 恢复词表舒展行距
- 按用户澄清恢复三种词表12px上下内边距、1.65行高，买家词表也使用560px纵向滚动，不再为一屏展示压缩行距。保留固定列宽、长词省略与完整导出。
- 构建和diff检查通过；浏览器实测行高44.8px，内容881px纵向滚动，可视与内容宽度均480px，无横向溢出。

### 2026-09-17 三种词表点击复制
- 买家搜索原词及归一化词、行业热词、广告关键词改为可键盘操作的复制按钮，复制完整原文，保留单行省略及舒展行距。成功和失败均提供明确提示。
- JS语法、构建、diff检查通过。浏览器实际点击长买家词、行业词、广告词并比对剪贴板，三项均与完整原文一致；测试后恢复原剪贴板文本。

### 2026-09-17 国家场景词、广告词分页与顶栏积分演示
- 行业词不再只筛全球5场景：保留接口各国家/地区记录，每页10场景展开最多30热门词。广告词改为每页20条真实接口翻页。两来源独立页码，加载时防重复点击，导航版本保护，末页空返回不清空已有内容。保持全文复制和无横向溢出。
- 投放计划恢复12px上下内边距与1.65行高。共用顶栏最右常驻积分Demo；用户明确自有积分系统暂不可接，展示10,000并标记演示，不伪称真实余额或执行扣费。
- 验证：构建、6项图表/积分门控回归、diff检查通过。浏览器行业第二页出现美国等国家30词，广告第二页20词内容与第一页不同，480px容器无水平溢出；投放计划实测行高44.8px。无付费模型调用。

### 2026-09-17 缩短关键词卡片，首屏显示分页
- 表格滚动区改为随视口限制180–380px，为卡片底部分页预留空间；表头固定，行距及每页记录数保持不变。
- 构建、diff检查通过；1375×935浏览器确认上下页按钮位于772–806px，首屏直接可见，行高仍44.8px。

### 2026-09-17 运营规划浅层固定规则第一版
- 新增经营关注点卡片，读取shop-summary所选区间最新经营日；严格使用同记录同指标RivalAvg/RivalGood，日值与近30天接待值分别标注，不累加比率或虚构询盘转化率。
- 规则覆盖曝光/访客、搜索点击率、商机转化率、首次回复率和回复时长。低于均值优先，同链路合并，最多三个方向。缺少有效对标、分母、数值不判断；优秀基准方向矛盾则弃用。提示仅排查方向，不定位原因或承诺结果。
- 原始资料与派生规则结果一起带入用户主动选择的规划Workflow上下文，明确派生结果非原因诊断；未修改模型Prompt或调用付费服务。参考SOP产品定位/定品、高质量发品、店铺诊断与运营规划；不使用其中固定阈值、预算和升品承诺。
- 9项图表/规则/付费门控测试及构建、diff检查通过；新增边界覆盖日期排序、缺失与零、分母不足、反向时长、同组去重、最多三项、矛盾优秀值。浏览器真实2026-08-31显示曝光1715/9098、搜索CTR1.45%/2.36%、商机转化0%/6.65%的浅层方向。

### 2026-09-18 紧凑关注点与组合排查规则
- 三张大卡改为三行可展开摘要；对标数字同排展示，整个区域最高390px，建议在卡片内滚动。
- 增加5分钟回复率、搜索曝光与CTR组合、访客达均值但商机转化偏低、回复覆盖与响应速度区别，以及同类目连续三个自然日低于均值的观察。每项补两条具体排查建议，不推断原因、不自动修改业务或调用模型。
- 规则仅延续SOP主图/标题/详情、流量与接待排查方法，无Prompt修改、无通用百分比阈值。新增连续日期/缺日组合测试；10项相关测试累计通过，构建及diff检查通过。浏览器实测区域390px，展开点击方向可见组合依据与两项建议。

### 2026-09-18 经营关注点视觉美化
- 使用图标与正文分栏、轻分隔线替代套层卡片，本店数值橙色突出，同行值与说明降低视觉权重；免费速览改为轻标签，保留暖白配色及390px高度限制。
- 纯展示调整，规则与数据不变；语法、构建、diff检查通过，浏览器确认折叠布局及展开后固定390px内滚动。

### 2026-09-18 扩展规则覆盖与全部关注点
- 增加有效商品、优品、爆品与搜索渠道对标，商品样本曝光无点击/访客无询盘及TM，两个连续7日窗口趋势、经营日期覆盖、广告有花费无点击/询盘的浅层复查规则。
- 商品与广告使用各自只读接口及独立周期。缺少广告报表、成本基准、长期逐商品记录或其他渠道可比数据时列出未判断，不以零补缺、不认定投放无效。比率展示分母并提示单日波动。
- 首页按低于均值、相对差距选择三个不同分组，支持查看全部；排序是展示参考，非风险分数。保持390px局部滚动。
- 12项相关测试、构建、diff检查通过。浏览器当前数据生成15项对标/观察和4项未判断，展开全部后仍为390px。未调用付费模型，未改Prompt。

## 2026-09-18 营销定位 → Accio Work 数据交接

- 用户明确要求定位页按钮打包左侧数据并交给AW分析。替换五个定位入口及右栏说明；快照由当前模块内存提供，避免全局请求记录混入其他页面；只打包已返回数据，保留缺项、周期和所选商品。
- 新增本机账号隔离JSON交接模块及受现有同源/桌面认证保护的POST路由。去除凭据字段，限制体积，UUID文件路径，不经过shell；相同请求幂等，前端并发互斥。保存和启动有脱敏日志，失败不冒充已分析。
- Prompt参考SOP“市场定位、产品定位、店铺定位和装修、高质量发品详情”。输入映射国家/类目/搜索需求/产品/公司服务素材；判断区分画像、行业样本、指数、周期；输出按当前主题四板块，事实与建议分开。SOP阈值不作为通用规则，痛点/认证有效性缺证据不推断；不执行平台写操作，不调用Dify。
- 15项测试通过（含快照零值/缺失、凭据剔除、权限、体积/路径、幂等与失败不重发、前端仅调用AW端点），前端构建通过。浏览器真实点击市场定位按钮，AW自动新建“来搜工作台市场与客群定位分析”对话，并确认文件存在约18.9KB后调用Read读取快照。未将Windows或正式插件包视为已验收。

## 2026-09-18 移除分析占位区域

- 根据用户要求撤下业务页Workflow/Chatflow右侧占位，数据区恢复全宽；定位页已工作的AW按钮移至顶部工具条。停止给其他业务卡片注入旧侧栏分析入口，保留基础数据交互。
- 构建和7项相关测试通过；浏览器确认营销定位无旧侧栏、顶部AW入口存在，运营推广无“选择一项业务开始分析”占位。未调用模型进行本轮布局验收。

## 2026-09-18 关键词卡片固定高度与右栏对齐

- 三个词表共用视口自适应固定高度，表格内部纵向滚动，分页留在底部，不改变行距。右侧交付成果与发品核对两张卡片等分同一高度，整体顶底与左卡对齐，内容过长各自滚动。
- 前端构建及diff检查通过；浏览器逐一切换三种词表，当前视口左卡/右栏均595px，底部一致，无横向溢出，行业词与广告词分页在卡片内可见。

## 2026-09-18 经营关注点三列布局

- 默认三个关注点横向并排，查看全部时按三列续排；窄屏单列，点击展开建议保留。按用户批注移除“未判断的指标”折叠栏，底层缺项保护和规则计算不变。
- 前端构建、JavaScript语法与diff检查通过。

## 2026-09-18 关注点卡片等高

- 移除“查看全部关注点”按钮，界面固定显示三个重点；三列网格stretch使同排卡片等高，建议展开交互保留。底层完整规则结果不删除。
- 前端构建、语法与diff检查通过。

## 2026-09-18 移除经营目标与店铺阶段占位

- 按用户明确要求移除运营规划中的经营目标填写块和店铺阶段占位，保留商品总数、搜索词记录、买家国家记录，改为三列等宽。未修改原型HTML。
- 前端构建、语法及diff检查通过。

## 2026-09-18 弹窗居中与日期下拉

- 所有原生模态dialog恢复fixed/inset:0/margin:auto，避免全局reset导致左上角定位，并限制在视口内滚动。
- 统计周期由遮罩弹窗改为日期按钮下方原生popover：起止日期、取消/应用，保留最近90天及最多31天校验；支持点击外部/Escape关闭。
- 构建、语法、diff检查通过。浏览器确认日期浮层位于按钮下8px，无模态遮罩，点击外部关闭；帮助弹窗中心为(547,467.5)，与1094×935视口中心精确一致。

### 2026-09-18 日周月选择恢复工具输入合同
- 对照旧版 public/app.js 的 timePages/renderTimePicker/applyTimeSelection 与公共 time-policy.js，移除新版弹出层自由起止日输入：日选单日，周选完整 ISO 自然周，月选完整自然月，提交保留所选粒度。
- 商品分析与优爆品页面仅日/月，使用 shop-product 校验近90天、自然月月初；切入商品页若继承不支持或过旧周期则恢复最近完整月。访客范围复用最长1个月校验。其余端点保留既有独立口径，不将 week/month 传给 shop-summary。
- 修正周默认值在周一时不应额外退一周；最晚日期按北京时间昨天，日默认前天。删除误加的所有接口统一近90天/31天限制。
- 验证：时间合同7项测试通过，JS语法/前端构建通过；浏览器月份输入、W37对应9/7—9/13、商品仅日/月及可选7—8月边界均已核对。未调用付费分析。

### 2026-09-18 运营推广接入 WorkCTL 0.1.58 广告只读能力
- 在现有运营推广内加入实时账户余额/资金流水、大促行业参考图表、直通车/全站推/普通计划/省心版计划组分页，计划详情支持商品与关键词查询翻页、定向、自动选品、搜索词/已降权词、屏蔽项、地域与多语言、标签推荐、计划报表及主动诊断；增加主动账户诊断、推荐方案、广告知识问答。
- 后端固定 section→只读实体白名单，校验计划ID与平台导航归属，拒绝任意实体/SQL/写参数；剥离写操作元数据。生成型入口仅用户点击读取，没有投放修改动作。资金余额来源未成功时显示缺失，不能补0。
- 实查纠正前一轮口头推断：company_search仍可用，为账户汇总；campaign_search为计划口径，不能互相替代。报表新增独立tempTableName，并校验返回数据源/会话/日期，按平台返回是否为日序列决定绘图，不伪装区间汇总为每日曲线。
- 实机只读验证：当前WorkCTL0.1.58，资金、行业样本234、39个直通车计划及7个全站推计划可读；样本计划109关键词、20条/页以及2推广商品真实显示；全站推搜索词返回空，定向/自动选品各1条、地域8条、语言17条。账户诊断与4类推荐、知识问答实际返回。查询期报表/资金流水为空，保留空状态。
- 平台异常：全站推导航标记屏蔽词可用，但上游拒绝“campaign_forbidden_keyword only supports standard promotion campaigns”；已保留入口与明确错误提示，不能宣称该类计划已读到屏蔽词。
- 验证：广告13项+时间7项共20项测试通过，包含白名单、越界归属、分页、报表隔离、问答入参；语法/前端构建/diff检查通过。浏览器确认弹窗无横向溢出、详情/查询交互。仅本地浏览器验收，未重新发布正式ZIP。
- 未新增或修改业务Prompt；平台诊断原文直接展示，不套用SOP数值阈值。参阅SOP“营销推广/付费广告”的计划、商品、关键词、地域数据检查维度组织入口，建议与执行分开。

### 2026-09-18 顶栏显示真实公司身份
- 移除各业务页标题右侧重复的“当前登录店铺”占位；全局顶栏改为当前公司名称与公司Logo，长名称省略并提供完整悬停提示，图片加载失败退回店铺图标，不再使用固定A头像。
- 新增精简 /api/workspaces/identity，复用当前账号公司资料读取与受控资料JSON解析，避免等待装修页面/云端查询；仅返回公司名/Logo，沿用进程内账号缓存，无写操作。跨页复用，读取失败可用顶部刷新重试。
- 验证：真实返回Dongguan Aoke Electronic Co., Ltd.，浏览器头像naturalWidth有效，重复入口已消失且无页面横向溢出；前后端语法/前端构建/diff检查通过。

### 2026-09-18 全局顶栏与标题区收紧
- 按用户注释将日/周/月及日期选择移到全局顶栏品牌右侧；移除积分Demo展示，唯一刷新按钮放到最右侧，保留真实公司身份。
- 八个顾问页面标题区统一紧凑单行，隐藏重复面包屑并缩小上下留白；历史规划等独立操作保留。产品发布不展示无效经营周期。原时间校验和日/月限制保持不变。
- 验证：构建、JS语法、diff检查通过；浏览器顶栏仅1个刷新、无积分节点、无横向溢出，看板标题32px，规划34px；日期浮层在新按钮下方8px，商品页仍只有日/月。

### 2026-09-18 移除顶栏帮助与通知
- 按用户标注移除所有顾问页共用顶栏的帮助中心和通知铃铛，并删除对应占位点击处理；保留日期、公司身份与刷新。
- 验证：JavaScript语法、前端构建通过。

### 2026-09-18 互换公司身份与日期位置
- 公司头像/名称移到品牌右侧，日周月及日期整体移到右侧刷新之前；保持真实资料读取、时间约束与刷新逻辑。
- JavaScript语法、前端构建通过。

### 2026-09-18 公司全名完整展示
- 取消顶栏公司名称固定最大宽度、省略号及窄屏隐藏规则；身份区域使用剩余空间，长名称可换行，日期与刷新保持独立。
- 前端构建与语法检查通过。

### 2026-09-18 移除重复页面标题行
- 八个顾问页面不再渲染顶部页名、面包屑及说明，内容直接上移，无空标题占位；移除营销定位重复的资料说明，历史规划入口并入经营关注点卡片。
- JS语法、前端构建通过。

### 2026-09-18 修复产品发布混入上一页内容
- 原因：固定高度工作区的ID选择器覆盖了低优先级的hidden样式，导致已经隐藏的商机/营销页面仍display:flex，并把产品发布区挤到0高度。
- 修复：将顾问根节点hidden设为强制隐藏，保持现有发布状态及路由逻辑。
- 验证：前端构建通过；浏览器从商机转化、营销定位分别切到产品发布，旧页面display:none且高度0，发布区恢复约749px；截图确认发布空工作区正常。未执行创建、保存或发布商品。

### 2026-09-18 商机转化首屏重排
- 将访客国家画像、买家等级分布与RFQ采购需求整体提至页面顶部，RFQ列加宽；三卡等高、卡内滚动，访客与询盘明细下移，保留查询、分页与RFQ入口。
- 验证：JS语法、前端构建通过；浏览器截图确认三卡首屏可见且等高，真实画像、等级、RFQ数据正常显示。

### 2026-09-18 RFQ整合到商机转化下方
- 将完整采购商机池、详情、最近报价与权益区域嵌入商机页下方；两个RFQ入口改为平滑滚动，并提供返回画像入口。复用唯一DOM及只读查询/详情/对比事件，导航重绘前归还节点。
- 统一暖色卡片、字号及行间距，左右详情卡等高内部滚动；多次搜索以请求版本防止旧结果覆盖。
- 验证：语法及构建通过；浏览器点击后保持商机转化选中并滚至下方，真实返回站内262/站外50/报价7；往返数据看板后仍只有一个RFQ节点，入口正常，无横向溢出。未执行任何报价提交。

- 2026-09-18：RFQ页内滚动入口箭头由→改为↓，与向下查看的行为一致；前端构建验证通过。

### 2026-09-18 产品详情替换诊断占位
- 移除数据分析页诊断执行清单、诊断依据、旧AI提示及工作区跳转；原底部详情移至右侧，指标双列展示，无横向表格。读取覆盖提示改为明确的已读取/平台商品数。
- 验证：语法、构建通过；浏览器点击第二件商品，右侧名称及曝光295、点击12、询盘20正确更新，底部无重复详情和执行清单。

- 2026-09-18：按用户标注移除商品分析页独立的读取数量提示条，不留占位；保留原统计和导出完整性字段。JS语法与前端构建验证通过。

- 2026-09-18：数据分析页象限筛选移至待分析产品标题同一行，移除该页商品搜索框与多余筛选标签；保留优爆品页原搜索。语法和前端构建验证通过。

- 2026-09-18：优爆品提升列表移除数据分组列（高曝高点等），保持其他字段及数据分析页分组列不变；JS语法与前端构建通过。

### 2026-09-18 优爆品详情改为行内展开
- 移除单品历史趋势和跟进动作清单，移除底部独立详情；点击商品操作在相邻下一行展开，再点收起，不影响数据分析页右侧详情。
- 验证：语法、构建通过；浏览器第二件商品详情紧跟对应行，真实指标正确；再次点击展开行数量为0。

- 2026-09-18：数据看板业务员表现与TOP5访客国家卡片统一340px，长名单卡内滚动；前端构建通过，浏览器实测两卡上下边缘一致。

- 2026-09-18：RFQ指标改为带12px间距的四张等高独立卡片，放大数值与标签、说明允许换行；权益无值显示未查询，不把null当零。浏览器实测四卡同高约123.5px，语法/构建通过。

- 2026-09-18：移除商机转化访客列表上方四项概览指标及渲染逻辑，筛选和列表上移，分页总数保留。语法与构建通过。

- 2026-09-18：国家表格、等级条形图改为并排饼图并合并卡片，附百分比图例；国家剩余返回项合为其他国家，缺失比例单独标示。浏览器验收两图和RFQ正常，语法/构建通过。

- 2026-09-18：RFQ推荐词就绪即加载完整商机池，自动核对选中首条详情；入口改为纯滚动，保留用户输入。浏览器仅进入商机转化即显示20条商机及第一条详情，未点击RFQ入口；语法/构建通过。

- 2026-09-18：访客翻页保留原表格，锁定已渲染高度并覆盖非布局加载提示；请求完成清除busy状态，避免空态缩短引发页面跳动。语法/构建通过，浏览器第一页约504.85px、第二页505px，分页数据正常更新。

- 2026-09-18：移除运营规划商品总数/搜索词记录/买家国家记录整条计数卡，SOP紧接经营关注点显示；语法与前端构建通过。

- 2026-09-18：看板渠道分布改饼图与双列百分比图例，覆盖全部返回渠道；移除日累计说明，沿用顶部所选日期范围筛选并聚合渠道数据。趋势/渠道卡统一260px，产品TOP5/健康度卡统一340px，长内容卡内滚动；业务员表头sticky固定。语法与构建通过，浏览器实测两组等高，业务员滚动268px后表头仍贴合容器顶部。

- 2026-09-18：按标注移除店铺装修文案页的已有装修页面、人工定位记录及执行记录与历史区域，保留其他定位页入口。

### 2026-09-18 Accio Work 结构化任务回传可行性实测
- 经用户授权，在 Accio 原生对话发起隔离测试；输入为截图中的三项单日经营指标，文件 `/tmp/lsou-task-return-probe/input.json`，不是全店诊断。
- 提示词参考运营SOP“店铺诊断+产品优化+运营规划”：输入指标及缺项→先核对同周期链路和商品口径→提出核查动作→输出任务标题、依据、动作、验收产物。负责人/日期缺失留null，状态pending，不执行业务动作。
- Accio 实际写出 `/tmp/lsou-task-return-probe/result.json`（1631字节），本侧独立读取并校验request_id、test_only、2条任务、必填字符串、pending、空负责人/日期均通过；Accio界面回执亦确认写盘与回读。
- 证明当前Mac环境的“Accio生成→约定JSON文件→本机读取”可行；尚未改造添加任务按钮、账号隔离持久化、自动导入去重或跨平台验收。测试任务不导入正式列表。

### 2026-09-18 执行任务持久化与Skill绑定
- 新增账号隔离JSON主数据、唯一请求、原子保存、回传校验去重、重启恢复、最近/历史互移、编辑完成/重新打开、删除恢复和请求取消。
- 生成按钮无弹窗，绑定lsou-planning-tasks v1，每请求保存Skill副本及hash；编辑采用页内表单。
- Prompt/Skill参考SOP“店铺诊断+产品优化+运营规划”：真实经营记录与旧任务→核查同周期转化链路和商品口径→具体动作→依据与验收产物；负责人日期缺失不编造，不套用未核实阈值，不自动执行业务写动作。
- 真实按钮已唤起AW并导入2条任务；后端重启后浏览器恢复，历史互移及页内编辑验证通过。真实生成发生在Skill绑定调整之前，Skill快照路径通过本地测试；未做正式三平台插件包验收。
- 4项相关测试通过（持久化、重复导入、账号隔离、旧版本写入拒绝、取消后结果忽略及原交接回归）；JS语法、前端构建、diff检查通过。

- 2026-09-18：执行任务简化为工作事项/为什么要做两列直接阅读，仅保留完成与编辑操作；移除详情折叠、负责人日期状态列、回收站入口、请求编号和搜索。编辑只填work/reason，Skill v2同步采用两字段业务短文案，参考原店铺诊断SOP并保留依据/缺项；旧任务兼容读取，现有两条试运行任务已改成简洁业务表述，原字段保留。两项生命周期/两字段测试、构建、浏览器显示及页内编辑通过。

- 2026-09-18：任务卡固定390px，标题/页签不动，列表与页内编辑共用内部滚动容器；保留各页签滚动位置，取消主动scrollIntoView，预留滚动条宽度并冻结表头。浏览器实测历史/最近/编辑/取消/完成/重新打开六种状态均为top429.54、height390、外层scroll342；完成测试后恢复原待执行状态。语法及前端构建通过。

- 2026-09-18：任务空态隐藏表头，改为居中细线图标、标题及简短说明；有数据表头取消有色色块，保留白底吸顶。390px固定卡片不变。浏览器1094px视口空态验收无表头且高度390，语法与构建通过。

- 2026-09-18：移除产品发布顶部标题与日期口径说明，内容直接上移；定位记录改为286px固定卡片内编辑，取消弹窗，输入草稿保留在内存以兼容异步重绘。浏览器确认发布topbar不可见、定位编辑无dialog、编辑/取消高度均286px；语法及前端构建通过。
